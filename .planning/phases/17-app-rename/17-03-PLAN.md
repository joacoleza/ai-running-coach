---
phase: 17-app-rename
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "README.md title reads '# AI Training Coach' (not '# AI Running Coach')"
    - "README.md logo alt text reads 'AI Training Coach Logo'"
    - "README.md badge URLs reference 'joacoleza/ai-training-coach'"
    - "README.md prose contains no references to 'running coach' or 'Running Coach'"
    - "README.md DB name examples use 'ai-training-coach' (not 'running-coach')"
    - "README.md includes instructions for user to rename GitHub repo and update git remote"
    - "CLAUDE.md architecture notes use 'ai-training-coach' for DB name references"
    - "CLAUDE.md heading is 'AI Training Coach'"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      contains: "AI Training Coach"
    - path: "CLAUDE.md"
      provides: "Architecture guidelines"
      contains: "ai-training-coach"
  key_links:
    - from: "README.md"
      to: "GitHub Actions badge URLs"
      via: "badge image src"
      pattern: "joacoleza/ai-training-coach"
---

<objective>
Update README.md and CLAUDE.md to replace all "Running Coach" / "ai-running-coach" / "running-coach" references with their "Training Coach" / "ai-training-coach" equivalents. Add GitHub repo rename instructions for the user.

Purpose: Documentation accurately reflects the renamed app and repo. Badge URLs will be correct after the user performs the manual GitHub rename. DB name examples are consistent with the new default.
Output: 2 files updated; no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-app-rename/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — title, badges, prose, DB examples, and GitHub rename guide</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — need to see all occurrences before editing)
  </read_first>
  <action>
    Make the following substitutions throughout README.md. Read the full file first to locate each occurrence.

    **Title (line 1):**
    - Before: `# AI Running Coach`
    - After:  `# AI Training Coach`

    **Logo alt text (line 4):**
    - Before: `alt="AI Running Coach Logo"`
    - After:  `alt="AI Training Coach Logo"`

    **Badge URLs (lines 7 and 9) — replace the GitHub repo path in both:**
    - Before: `https://github.com/joacoleza/ai-running-coach/actions/workflows/azure-static-web-apps.yml/badge.svg`
    - After:  `https://github.com/joacoleza/ai-training-coach/actions/workflows/azure-static-web-apps.yml/badge.svg`
    - Before: `https://github.com/joacoleza/ai-running-coach/actions/workflows/ci.yml/badge.svg`
    - After:  `https://github.com/joacoleza/ai-training-coach/actions/workflows/ci.yml/badge.svg`

    **Prose paragraph (line 21):**
    - Before: `A personal web app that acts as an AI running coach. Set a goal, get a training plan, log your runs, and receive coaching feedback that adapts the plan over time.`
    - After:  `A personal web app that acts as an AI training coach. Set a goal, get a training plan, log your sessions, and receive coaching feedback that adapts the plan over time.`

    **Seed user DB reference (line 84):**
    - Before: `insert this document into the \`running-coach.users\` collection`
    - After:  `insert this document into the \`ai-training-coach.users\` collection`

    **E2E note in Run tests block (line 84 trailing):**
    - Before: `E2E tests use a separate \`running-coach-e2e\` database`
    - After:  `E2E tests use a separate \`ai-training-coach-e2e\` database`

    **Run tests comment (line 119):**
    - Before: `reuses it (running-coach DB) while global-setup seeds into running-coach-e2e, causing login failures.`
    - After:  `reuses it (ai-training-coach DB) while global-setup seeds into ai-training-coach-e2e, causing login failures.`

    **Atlas deploy step (line 148):**
    - Before: `insert the document from the **Seed your first user** step above into your Atlas \`running-coach.users\` collection.`
    - After:  `insert the document from the **Seed your first user** step above into your Atlas \`ai-training-coach.users\` collection.`

    **Add a new section after the "Deploying" section** (append before `## Roadmap`):

    ```markdown
    ## GitHub Repository Rename

    The GitHub repository has been renamed from `joacoleza/ai-running-coach` to `joacoleza/ai-training-coach`.

    After the rename is complete (GitHub Settings → General → Repository name), update your local git remote:

    ```bash
    git remote set-url origin https://github.com/joacoleza/ai-training-coach.git
    ```

    The badge URLs and workflow references in this README already point to the new repository path.
    ```

    Do NOT touch any other content (stack table, access model, cost section, getting started steps, etc.).
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -n "running coach\|Running Coach\|ai-running-coach\|running-coach" README.md && echo "FOUND ABOVE - CHECK EACH" || echo "CLEAN - PASS"</automated>
  </verify>
  <done>
    - grep returns "CLEAN - PASS" OR only matches inside code blocks/examples that are intentionally showing old values for migration context (there should be none — all have been updated).
    - `grep "AI Training Coach" README.md` returns the title line.
    - `grep "ai-training-coach" README.md` returns the badge URLs, DB references, and remote URL example.
    - `grep "GitHub Repository Rename" README.md` returns the new section header.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md — heading and DB name architecture references</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — search for all occurrences of "running-coach" and "AI Running Coach" before editing)
  </read_first>
  <action>
    CLAUDE.md contains "running-coach" DB name references in the Architecture Decisions section and "AI Running Coach" in the heading. Make the following substitutions:

    **Heading (line 1):**
    - Before: `# Claude Code Guidelines — AI Running Coach`
    - After:  `# Claude Code Guidelines — AI Training Coach`

    **Architecture note about MongoDB DB name** — the multi-line note that starts with "MongoDB DB name is derived from the connection string..." contains several examples. Update all occurrences of the DB name strings within that note:
    - `defaulting to \`running-coach\`` → `defaulting to \`ai-training-coach\``
    - `defaults to \`running-coach\`` → `defaults to \`ai-training-coach\``
    - `uses \`running-coach\`` → `uses \`ai-training-coach\``
    - `use \`running-coach-e2e\`` → `use \`ai-training-coach-e2e\``
    - `seeding into \`running-coach-e2e\`` → `seeding into \`ai-training-coach-e2e\``
    - Any other occurrence of `running-coach` or `running-coach-e2e` in the architecture notes.

    Do NOT change:
    - The actual logic descriptions (how the regex works, what MONGODB_CONNECTION_STRING does)
    - Any code snippets showing function signatures
    - The GSD Workflow section (it references the repo path indirectly through /gsd: commands — those are not repo URLs)
    - Any section that does not reference "running coach" or "ai-running-coach"
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -n "AI Running Coach\|running-coach\|running coach" CLAUDE.md && echo "FOUND ABOVE - CHECK EACH" || echo "CLEAN - PASS"</automated>
  </verify>
  <done>
    - grep returns "CLEAN - PASS" (no remaining "AI Running Coach", "running-coach", or "running coach" occurrences in CLAUDE.md).
    - `grep "AI Training Coach" CLAUDE.md` returns the heading line.
    - `grep "ai-training-coach" CLAUDE.md` returns the DB name references in the architecture notes.
  </done>
</task>

</tasks>

<verification>
Confirm no stale references remain across both files:

```bash
cd /c/dev/ai-running-coach && grep -n "AI Running Coach\|running coach\|ai-running-coach\|running-coach" README.md CLAUDE.md
```

Output must be empty (no matches). Then run a final full-repo scan to confirm nothing was missed in the planned files:

```bash
cd /c/dev/ai-running-coach && grep -rn "AI Running Coach\|ai-running-coach" package.json api/package.json web/package.json web/index.html web/public/unauthorized.html README.md CLAUDE.md api/src/shared/db.ts api/src/shared/prompts.ts playwright.config.ts e2e/global-setup.ts web/src/pages/LoginPage.tsx web/src/pages/ChangePasswordPage.tsx web/src/pages/PasswordPage.tsx web/src/components/layout/Sidebar.tsx web/src/pages/Coach.tsx
```

Output must be empty.
</verification>

<success_criteria>
- `grep "AI Running Coach" README.md` → no output
- `grep "running-coach\|running coach" README.md` → no output
- `grep "AI Running Coach\|running-coach\|running coach" CLAUDE.md` → no output
- `grep "AI Training Coach" README.md` → matches title line
- `grep "GitHub Repository Rename" README.md` → matches new section heading
- `grep "ai-training-coach" README.md` → matches badge URLs and DB references
- `grep "ai-training-coach" CLAUDE.md` → matches DB name references
</success_criteria>

<output>
After completion, create `.planning/phases/17-app-rename/17-03-SUMMARY.md`
</output>
