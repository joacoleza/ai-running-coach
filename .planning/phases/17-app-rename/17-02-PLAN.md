---
phase: 17-app-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - api/package.json
  - web/package.json
  - api/src/shared/db.ts
  - api/src/shared/prompts.ts
  - playwright.config.ts
  - e2e/global-setup.ts
  - e2e/auth.spec.ts
  - api/src/__tests__/chat.integration.test.ts
  - api/src/__tests__/exerciseWeights.test.ts
  - api/src/__tests__/messages.isolation.test.ts
  - api/src/__tests__/messages.test.ts
  - api/src/__tests__/plan.isolation.test.ts
  - api/src/__tests__/plan.test.ts
  - api/src/__tests__/planArchive.isolation.test.ts
  - api/src/__tests__/planArchive.test.ts
  - api/src/__tests__/planDays.isolation.test.ts
  - api/src/__tests__/planDays.test.ts
  - api/src/__tests__/planPhases.isolation.test.ts
  - api/src/__tests__/planPhases.test.ts
  - api/src/__tests__/runs.isolation.test.ts
  - api/src/__tests__/runs.test.ts
  - api/src/__tests__/usageCapture.test.ts
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "Root package.json name field is 'ai-training-coach'"
    - "api/package.json name field is 'ai-training-coach-api'"
    - "web/package.json name field is 'ai-training-coach-web'"
    - "db.ts fallback DB name is 'ai-training-coach' (not 'running-coach')"
    - "prompts.ts JSDoc says 'AI training coach' (not 'AI running coach')"
    - "playwright.config.ts E2E DB connection string uses 'ai-training-coach-e2e'"
    - "global-setup.ts DB name fallback is 'ai-training-coach'"
    - "e2e/auth.spec.ts heading assertion matches 'AI Training Coach'"
    - "e2e/auth.spec.ts DB URI and dbName fallbacks use 'ai-training-coach-e2e' and 'ai-training-coach'"
    - "All 15 API integration test files use mongoClient.db('ai-training-coach') instead of 'running-coach'"
    - "package-lock.json files regenerated to reflect new package names"
    - "API unit tests pass with no regressions"
  artifacts:
    - path: "package.json"
      provides: "Root package name"
      contains: "ai-training-coach"
    - path: "api/package.json"
      provides: "API package name"
      contains: "ai-training-coach-api"
    - path: "web/package.json"
      provides: "Web package name"
      contains: "ai-training-coach-web"
    - path: "api/src/shared/db.ts"
      provides: "MongoDB fallback DB name"
      contains: "ai-training-coach"
    - path: "api/src/shared/prompts.ts"
      provides: "JSDoc comment"
      contains: "AI training coach"
    - path: "playwright.config.ts"
      provides: "E2E database connection"
      contains: "ai-training-coach-e2e"
    - path: "e2e/global-setup.ts"
      provides: "Global setup DB fallback"
      contains: "ai-training-coach"
    - path: "e2e/auth.spec.ts"
      provides: "E2E heading assertion and DB fallbacks"
      contains: "AI Training Coach"
    - path: "api/src/__tests__/plan.test.ts"
      provides: "Representative API integration test (all 15 updated)"
      contains: "ai-training-coach"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/global-setup.ts"
      via: "MONGODB_CONNECTION_STRING env var sets isolated E2E DB name"
      pattern: "ai-training-coach-e2e"
    - from: "api/src/shared/db.ts"
      to: "MongoDB connection"
      via: "fallback DB name when no path in connection string"
      pattern: "ai-training-coach"
    - from: "e2e/auth.spec.ts"
      to: "LoginPage h1"
      via: "getByRole('heading') assertion"
      pattern: "AI Training Coach"
---

<objective>
Update package name fields in all three package.json files, change the MongoDB DB name fallback in db.ts, update the JSDoc in prompts.ts, update E2E config files (playwright.config.ts and global-setup.ts), fix the E2E heading assertion and DB fallbacks in auth.spec.ts, and bulk-replace 'running-coach' DB references in all 15 API integration test files.

Purpose: Package metadata, dev/E2E infrastructure, and test files all reflect the new 'ai-training-coach' identity. Existing production env vars are untouched — they override the fallback.
Output: 24 files updated; no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-app-rename/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package.json name fields, update prompts.ts JSDoc, and regenerate lock files</name>
  <files>
    package.json,
    api/package.json,
    web/package.json,
    api/src/shared/prompts.ts
  </files>
  <read_first>
    - package.json (line 2: `"name": "ai-running-coach"`)
    - api/package.json (line 2: `"name": "ai-running-coach-api"`)
    - web/package.json (line 1: `"name": "ai-running-coach-web"`)
    - api/src/shared/prompts.ts (line 4: `* Build the system prompt for the AI running coach.`)
  </read_first>
  <action>
    Make the following exact string substitutions — one per file, no surrounding content changes:

    **package.json** (line 2):
    - Before: `"name": "ai-running-coach"`
    - After:  `"name": "ai-training-coach"`

    **api/package.json** (line 2):
    - Before: `"name": "ai-running-coach-api"`
    - After:  `"name": "ai-training-coach-api"`

    **web/package.json** (line 1):
    - Before: `"name": "ai-running-coach-web"`
    - After:  `"name": "ai-training-coach-web"`

    **api/src/shared/prompts.ts** (line 4 JSDoc):
    - Before: ` * Build the system prompt for the AI running coach.`
    - After:  ` * Build the system prompt for the AI training coach.`

    Do NOT touch any other fields (version, scripts, dependencies, etc.).
    NOTE: Azure Functions deployment uses the Azure config for function app naming, not package.json `name` — this rename is safe.

    After updating the package.json files, regenerate the lock files so they reflect the new name:

    ```bash
    cd /c/dev/ai-running-coach && npm install
    cd /c/dev/ai-running-coach/api && npm install
    cd /c/dev/ai-running-coach/web && npm install
    ```
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -r "ai-running-coach\|AI running coach" package.json api/package.json web/package.json api/src/shared/prompts.ts && echo "FOUND - FAIL" || echo "NOT FOUND - PASS"</automated>
  </verify>
  <done>
    - grep returns "NOT FOUND - PASS" — none of those files contain the old name strings.
    - `npm install` completed in all three directories and regenerated package-lock.json files with the new package name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MongoDB DB name fallbacks in db.ts, playwright.config.ts, global-setup.ts, and auth.spec.ts</name>
  <files>
    api/src/shared/db.ts,
    playwright.config.ts,
    e2e/global-setup.ts,
    e2e/auth.spec.ts
  </files>
  <read_first>
    - api/src/shared/db.ts (line 14: `|| 'running-coach'` at end of dbName extraction regex)
    - playwright.config.ts (line 9: `process.env.MONGODB_CONNECTION_STRING = 'mongodb://localhost:27017/running-coach-e2e'`)
    - playwright.config.ts (lines 3-7: comment block explaining CI vs local DB name fallback)
    - e2e/global-setup.ts (line 5: `const MONGO_URI = process.env.MONGODB_CONNECTION_STRING || 'mongodb://localhost:27017/running-coach-e2e'`)
    - e2e/global-setup.ts (line 38: `const dbName = MONGO_URI.match(...)?.[1] || 'running-coach'`)
    - e2e/global-setup.ts (lines 36-37: comment block)
    - e2e/auth.spec.ts (line 18: `getByRole('heading', { name: 'AI Running Coach' })`)
    - e2e/auth.spec.ts (line 154: `|| 'mongodb://localhost:27017/running-coach-e2e'`)
    - e2e/auth.spec.ts (line 158: `|| 'running-coach'`)
  </read_first>
  <action>
    Make the following exact string substitutions:

    **api/src/shared/db.ts** (line 14):
    - Before: `|| 'running-coach'`
    - After:  `|| 'ai-training-coach'`
    The full line becomes:
    `  const dbName = connectionString.match(/\/\/[^/]+\/([^/?]+)/)?.[1] || 'ai-training-coach';`

    **playwright.config.ts** (line 9):
    - Before: `process.env.MONGODB_CONNECTION_STRING = 'mongodb://localhost:27017/running-coach-e2e'`
    - After:  `process.env.MONGODB_CONNECTION_STRING = 'mongodb://localhost:27017/ai-training-coach-e2e'`

    Also update the comment block (lines 3-7) in playwright.config.ts to replace:
    - `'running-coach-e2e' database` → `'ai-training-coach-e2e' database`
    - `'running-coach' in db.ts` → `'ai-training-coach' in db.ts`

    **e2e/global-setup.ts** (line 5):
    - Before: `|| 'mongodb://localhost:27017/running-coach-e2e'`
    - After:  `|| 'mongodb://localhost:27017/ai-training-coach-e2e'`

    **e2e/global-setup.ts** (line 38):
    - Before: `|| 'running-coach'`
    - After:  `|| 'ai-training-coach'`

    Also update the comment block (lines 36-37) in global-setup.ts to replace:
    - `running-coach-e2e` → `ai-training-coach-e2e`
    - `running-coach` (CI fallback example) → `ai-training-coach`

    **e2e/auth.spec.ts** (line 18):
    - Before: `await expect(page.getByRole('heading', { name: 'AI Running Coach' })).toBeVisible({ timeout: 10_000 })`
    - After:  `await expect(page.getByRole('heading', { name: 'AI Training Coach' })).toBeVisible({ timeout: 10_000 })`

    **e2e/auth.spec.ts** (line 154):
    - Before: `const uri = process.env.MONGODB_CONNECTION_STRING || 'mongodb://localhost:27017/running-coach-e2e'`
    - After:  `const uri = process.env.MONGODB_CONNECTION_STRING || 'mongodb://localhost:27017/ai-training-coach-e2e'`

    **e2e/auth.spec.ts** (line 158):
    - Before: `const dbName = uri.match(/\/\/[^/]+\/([^/?]+)/)?.[1] || 'running-coach'`
    - After:  `const dbName = uri.match(/\/\/[^/]+\/([^/?]+)/)?.[1] || 'ai-training-coach'`

    IMPORTANT: Do NOT change `MONGODB_CONNECTION_STRING` values in `local.settings.json` (gitignored, not in repo) or any production Azure config — those are intentionally left as-is.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -r "running-coach" api/src/shared/db.ts playwright.config.ts e2e/global-setup.ts e2e/auth.spec.ts && echo "FOUND - FAIL" || echo "NOT FOUND - PASS"</automated>
  </verify>
  <done>
    - grep returns "NOT FOUND - PASS" — no "running-coach" strings remain in those 4 files.
    - `grep "ai-training-coach" api/src/shared/db.ts` outputs the updated fallback line.
    - `grep "ai-training-coach-e2e" playwright.config.ts` outputs the updated connection string.
    - `grep "ai-training-coach" e2e/global-setup.ts` outputs both the URI default and the dbName fallback.
    - `grep "AI Training Coach" e2e/auth.spec.ts` outputs the updated heading assertion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bulk-replace 'running-coach' DB references in all 15 API integration test files</name>
  <files>
    api/src/__tests__/chat.integration.test.ts,
    api/src/__tests__/exerciseWeights.test.ts,
    api/src/__tests__/messages.isolation.test.ts,
    api/src/__tests__/messages.test.ts,
    api/src/__tests__/plan.isolation.test.ts,
    api/src/__tests__/plan.test.ts,
    api/src/__tests__/planArchive.isolation.test.ts,
    api/src/__tests__/planArchive.test.ts,
    api/src/__tests__/planDays.isolation.test.ts,
    api/src/__tests__/planDays.test.ts,
    api/src/__tests__/planPhases.isolation.test.ts,
    api/src/__tests__/planPhases.test.ts,
    api/src/__tests__/runs.isolation.test.ts,
    api/src/__tests__/runs.test.ts,
    api/src/__tests__/usageCapture.test.ts
  </files>
  <action>
    All 15 test files contain `mongoClient.db('running-coach')` calls that hardcode the old DB name. Replace every occurrence across all 15 files:

    - Before: `mongoClient.db('running-coach')`
    - After:  `mongoClient.db('ai-training-coach')`

    This is a pure string substitution — no other content changes. The pattern is identical in every file. Read each file to locate all occurrences (some files have multiple) before editing.

    After updating all files, run the API test suite to confirm no regressions:

    ```bash
    cd /c/dev/ai-running-coach/api && npx vitest run 2>&1 | tail -30
    ```
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -r "running-coach" api/src/__tests__/ && echo "FOUND - FAIL" || echo "NOT FOUND - PASS"</automated>
  </verify>
  <done>
    - grep returns "NOT FOUND - PASS" — no "running-coach" strings remain in any of the 15 test files.
    - `cd api && npx vitest run` exits 0 with all tests passing.
  </done>
</task>

</tasks>

<verification>
Run the API unit tests to confirm no regressions from the db.ts change and the test file updates:

```bash
cd /c/dev/ai-running-coach/api && npx vitest run 2>&1 | tail -20
```

All tests must pass. Confirm no "running-coach" references remain anywhere in the test files:

```bash
cd /c/dev/ai-running-coach && grep -r "running-coach" api/src/__tests__/ e2e/auth.spec.ts api/src/shared/db.ts playwright.config.ts e2e/global-setup.ts
```

Output must be empty.
</verification>

<success_criteria>
- `grep "ai-running-coach" package.json api/package.json web/package.json` → no output
- `grep "AI running coach" api/src/shared/prompts.ts` → no output
- `grep "running-coach" api/src/shared/db.ts playwright.config.ts e2e/global-setup.ts e2e/auth.spec.ts` → no output
- `grep -r "running-coach" api/src/__tests__/` → no output
- `grep "ai-training-coach" api/src/shared/db.ts` → matches the fallback line
- `grep "ai-training-coach-e2e" playwright.config.ts` → matches the connection string line
- `grep "AI Training Coach" e2e/auth.spec.ts` → matches the heading assertion
- `cd api && npx vitest run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-app-rename/17-02-SUMMARY.md`
</output>
