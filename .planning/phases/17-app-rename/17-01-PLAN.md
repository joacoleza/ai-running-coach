---
phase: 17-app-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/LoginPage.tsx
  - web/src/pages/ChangePasswordPage.tsx
  - web/src/pages/PasswordPage.tsx
  - web/src/components/layout/Sidebar.tsx
  - web/src/pages/Coach.tsx
  - web/index.html
  - web/public/unauthorized.html
  - web/src/__tests__/App.auth.test.tsx
autonomous: true
requirements:
  - RENAME-01

must_haves:
  truths:
    - "Browser tab title shows 'AI Training Coach' (not 'AI Running Coach')"
    - "The h1 heading on the login screen reads 'AI Training Coach'"
    - "The h1 heading on the change-password screen reads 'AI Training Coach'"
    - "The h1 heading on the password screen reads 'AI Training Coach'"
    - "Sidebar logo img alt text reads 'AI Training Coach'"
    - "Coach page subtitle no longer contains 'running coach'"
    - "The unauthorized.html page title reads 'AI Training Coach'"
    - "Web unit tests pass (App.auth.test.tsx assertion updated to match new heading)"
  artifacts:
    - path: "web/src/pages/LoginPage.tsx"
      provides: "Login page heading"
      contains: "AI Training Coach"
    - path: "web/src/pages/ChangePasswordPage.tsx"
      provides: "Change password page heading"
      contains: "AI Training Coach"
    - path: "web/src/pages/PasswordPage.tsx"
      provides: "Password page heading"
      contains: "AI Training Coach"
    - path: "web/src/components/layout/Sidebar.tsx"
      provides: "Sidebar logo alt text"
      contains: "AI Training Coach"
    - path: "web/src/pages/Coach.tsx"
      provides: "Coach page subtitle"
      contains: "training coach"
    - path: "web/index.html"
      provides: "Browser tab title"
      contains: "AI Training Coach"
    - path: "web/public/unauthorized.html"
      provides: "Unauthorized page title"
      contains: "AI Training Coach"
    - path: "web/src/__tests__/App.auth.test.tsx"
      provides: "Test assertion"
      contains: "AI Training Coach"
  key_links:
    - from: "web/index.html"
      to: "browser tab"
      via: "<title> element"
      pattern: "<title>AI Training Coach</title>"
    - from: "web/src/__tests__/App.auth.test.tsx"
      to: "LoginPage.tsx h1"
      via: "getByText assertion"
      pattern: "AI Training Coach"
---

<objective>
Replace every visible "AI Running Coach" / "running coach" UI string with "AI Training Coach" / "training coach" across the web frontend files and the HTML title tags.

Purpose: Users see "AI Training Coach" consistently — in the browser tab, login screen, change-password screen, sidebar, coach page subtitle, and the unauthorized fallback page.
Output: 8 files updated; no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-app-rename/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace "AI Running Coach" strings in React pages and Sidebar</name>
  <files>
    web/src/pages/LoginPage.tsx,
    web/src/pages/ChangePasswordPage.tsx,
    web/src/pages/PasswordPage.tsx,
    web/src/components/layout/Sidebar.tsx,
    web/src/pages/Coach.tsx
  </files>
  <read_first>
    - web/src/pages/LoginPage.tsx (line 79: `<h1 className="text-xl font-bold text-gray-900 mb-6">AI Running Coach</h1>`)
    - web/src/pages/ChangePasswordPage.tsx (line 79: `<h1 className="text-xl font-bold text-gray-900 mb-2">AI Running Coach</h1>`)
    - web/src/pages/PasswordPage.tsx (line 44: `<h1 className="text-xl font-bold text-gray-900 mb-6">AI Running Coach</h1>`)
    - web/src/components/layout/Sidebar.tsx (line 45: `alt="AI Running Coach"`)
    - web/src/pages/Coach.tsx (line 5: `Chat with your AI running coach here.`)
  </read_first>
  <action>
    Make the following exact string substitutions — each is a single line replacement, no surrounding code changes:

    **LoginPage.tsx** (line 79):
    - Before: `AI Running Coach`
    - After:  `AI Training Coach`

    **ChangePasswordPage.tsx** (line 79):
    - Before: `AI Running Coach`
    - After:  `AI Training Coach`

    **PasswordPage.tsx** (line 44):
    - Before: `AI Running Coach`
    - After:  `AI Training Coach`

    **Sidebar.tsx** (line 45):
    - Before: `alt="AI Running Coach"`
    - After:  `alt="AI Training Coach"`

    **Coach.tsx** (line 5):
    - Before: `Chat with your AI running coach here.`
    - After:  `Chat with your AI training coach here.`

    Do NOT touch any class names, imports, logic, or other content.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep -r "AI Running Coach\|running coach" web/src/pages/LoginPage.tsx web/src/pages/ChangePasswordPage.tsx web/src/pages/PasswordPage.tsx web/src/components/layout/Sidebar.tsx web/src/pages/Coach.tsx</automated>
  </verify>
  <done>grep returns no matches — none of those files contain "AI Running Coach" or "running coach" any longer.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML title tags and fix the unit test assertion</name>
  <files>
    web/index.html,
    web/public/unauthorized.html,
    web/src/__tests__/App.auth.test.tsx
  </files>
  <read_first>
    - web/index.html (line 13: `<title>AI Running Coach</title>`)
    - web/public/unauthorized.html (line 6: `<title>Access Denied — AI Running Coach</title>`)
    - web/src/__tests__/App.auth.test.tsx (lines 14 and 16: assertion `'AI Running Coach'`)
  </read_first>
  <action>
    Make the following exact string substitutions:

    **web/index.html** (line 13):
    - Before: `<title>AI Running Coach</title>`
    - After:  `<title>AI Training Coach</title>`

    **web/public/unauthorized.html** (line 6):
    - Before: `<title>Access Denied — AI Running Coach</title>`
    - After:  `<title>Access Denied — AI Training Coach</title>`

    **web/src/__tests__/App.auth.test.tsx** — update BOTH occurrences on lines 14 and 16:
    - Line 14 before: `it('renders LoginPage (AI Running Coach heading + Log In button) when no access_token in localStorage', () => {`
    - Line 14 after:  `it('renders LoginPage (AI Training Coach heading + Log In button) when no access_token in localStorage', () => {`
    - Line 16 before: `expect(screen.getByText('AI Running Coach')).toBeInTheDocument()`
    - Line 16 after:  `expect(screen.getByText('AI Training Coach')).toBeInTheDocument()`

    Do NOT touch any other lines.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && grep "AI Running Coach" web/index.html web/public/unauthorized.html web/src/__tests__/App.auth.test.tsx && echo "FOUND - FAIL" || echo "NOT FOUND - PASS"</automated>
  </verify>
  <done>
    - grep returns "NOT FOUND - PASS" (no "AI Running Coach" remains in those files).
    - Running `cd web && npx vitest run --reporter=verbose 2>&1 | grep -E "PASS|FAIL|App.auth"` shows the App.auth.test.tsx suite passes.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run web unit tests to confirm no regressions:

```bash
cd /c/dev/ai-running-coach/web && npx vitest run 2>&1 | tail -20
```

All tests must pass. Then run a build to catch any TypeScript errors:

```bash
cd /c/dev/ai-running-coach/web && npm run build 2>&1 | tail -20
```

Build must succeed with no errors.
</verification>

<success_criteria>
- `grep -r "AI Running Coach\|running coach" web/src/pages/LoginPage.tsx web/src/pages/ChangePasswordPage.tsx web/src/pages/PasswordPage.tsx web/src/components/layout/Sidebar.tsx web/src/pages/Coach.tsx` → no output
- `grep "AI Running Coach" web/index.html` → no output
- `grep "AI Running Coach" web/public/unauthorized.html` → no output
- `grep "AI Running Coach" web/src/__tests__/App.auth.test.tsx` → no output
- `cd web && npx vitest run` exits 0
- `cd web && npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-app-rename/17-01-SUMMARY.md`
</output>
