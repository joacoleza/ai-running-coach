---
phase: 04-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.tsx
  - web/src/components/layout/Sidebar.tsx
  - web/src/pages/Dashboard.tsx
  - web/package.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, IMP-01, IMP-02, IMP-03]

must_haves:
  truths:
    - "Visiting / redirects to /dashboard (not /plan)"
    - "Sidebar shows Dashboard as first nav item"
    - "Dashboard page renders its heading"
    - "Recharts is installed and importable"
  artifacts:
    - path: "web/src/App.tsx"
      provides: "/ → /dashboard redirect, * → /dashboard fallback"
    - path: "web/src/components/layout/Sidebar.tsx"
      provides: "Dashboard first in navItems array"
    - path: "web/src/pages/Dashboard.tsx"
      provides: "Dashboard scaffold with heading and filter/card/chart placeholders"
    - path: "web/package.json"
      provides: "recharts dependency"
  key_links:
    - from: "web/src/App.tsx"
      to: "web/src/pages/Dashboard.tsx"
      via: "Route path=/dashboard"
      pattern: "path=\"/dashboard\""
---

<objective>
Routing and navigation changes to make Dashboard the home page, install Recharts, and scaffold the Dashboard component with the correct structure.

Purpose: Dashboard becomes the app home per D-01/D-02. Recharts must be installed before the chart tasks can proceed.
Output: Updated App.tsx routes, reordered Sidebar.tsx, Dashboard.tsx scaffold with filter row + stat card slots + chart area, recharts in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard/04-CONTEXT.md
@.planning/phases/04-dashboard/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routing + Sidebar nav order</name>
  <files>web/src/App.tsx, web/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - web/src/App.tsx — current route config (has `/ → /plan`, `* → /plan`)
    - web/src/components/layout/Sidebar.tsx — current navItems order
  </read_first>
  <action>
    In web/src/App.tsx:
    - Change `<Route path="/" element={<Navigate to="/plan" replace />} />` to `<Route path="/" element={<Navigate to="/dashboard" replace />} />`
    - Change `<Route path="*" element={<Navigate to="/plan" replace />} />` to `<Route path="*" element={<Navigate to="/dashboard" replace />} />`
    No other changes to App.tsx.

    In web/src/components/layout/Sidebar.tsx:
    - Reorder the `navItems` array so Dashboard is first:
      ```
      const navItems = [
        { path: "/dashboard", label: "Dashboard", icon: "📊" },
        { path: "/plan", label: "Training Plan", icon: "📅" },
        { path: "/runs", label: "Runs", icon: "🏃" },
        { path: "/archive", label: "Archive", icon: "🗄️" },
      ];
      ```
    (per D-01 and D-02)
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx line containing Navigate to="/dashboard" appears twice (for "/" and "*" routes)
    - App.tsx does NOT contain Navigate to="/plan"
    - Sidebar.tsx navItems first entry is `path: "/dashboard"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Routes redirect to /dashboard; Dashboard is first sidebar item</done>
</task>

<task type="auto">
  <name>Task 2: Install Recharts + scaffold Dashboard page</name>
  <files>web/package.json, web/src/pages/Dashboard.tsx</files>
  <read_first>
    - web/package.json — current dependencies (confirm recharts absent)
    - web/src/pages/Dashboard.tsx — current placeholder content
    - .planning/phases/04-dashboard/04-UI-SPEC.md — layout, typography, color, copywriting decisions
    - .planning/phases/04-dashboard/04-CONTEXT.md — D-04 through D-16 filter/stats/chart decisions
  </read_first>
  <action>
    Run: `cd C:/dev/ai-running-coach/web && npm install recharts`

    Replace web/src/pages/Dashboard.tsx with a full scaffold that includes the correct structure for downstream implementation:

    ```tsx
    import { useState } from 'react'
    import { useNavigate } from 'react-router-dom'

    type FilterPreset =
      | 'current-plan'
      | 'last-4-weeks'
      | 'last-8-weeks'
      | 'last-3-months'
      | 'last-12-months'
      | 'this-year'
      | 'all-time'

    const FILTER_PRESETS: { id: FilterPreset; label: string }[] = [
      { id: 'current-plan', label: 'Current Plan' },
      { id: 'last-4-weeks', label: 'Last 4 weeks' },
      { id: 'last-8-weeks', label: 'Last 8 weeks' },
      { id: 'last-3-months', label: 'Last 3 months' },
      { id: 'last-12-months', label: 'Last 12 months' },
      { id: 'this-year', label: 'This year' },
      { id: 'all-time', label: 'All time' },
    ]

    export function Dashboard() {
      const [activeFilter, setActiveFilter] = useState<FilterPreset>('current-plan')
      const navigate = useNavigate()

      return (
        <div className="p-6 max-w-5xl mx-auto">
          {/* Page heading */}
          <h1 className="text-2xl font-bold text-gray-900 mb-4">Dashboard</h1>

          {/* Filter row */}
          <div className="mb-6">
            <span className="text-xs font-medium text-gray-600 mr-3">Filter by:</span>
            <div className="flex flex-wrap gap-2 mt-2">
              {FILTER_PRESETS.map((preset) => (
                <button
                  key={preset.id}
                  onClick={() => setActiveFilter(preset.id)}
                  className={`px-3 py-1.5 rounded-full text-sm font-medium transition-colors cursor-pointer ${
                    activeFilter === preset.id
                      ? 'bg-gray-200 text-gray-900 font-semibold'
                      : 'text-gray-600 border border-gray-300 hover:bg-gray-100'
                  }`}
                >
                  {preset.label}
                </button>
              ))}
            </div>
          </div>

          {/* Stats cards — 4 grid, populated by useDashboard in next plan */}
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
            {/* Total Distance */}
            <div className="bg-white border border-gray-200 rounded-lg p-4">
              <p className="text-xs font-medium text-gray-600 mb-1">Total Distance</p>
              <p className="text-2xl font-bold text-gray-900">—</p>
            </div>
            {/* Total Runs */}
            <div className="bg-white border border-gray-200 rounded-lg p-4">
              <p className="text-xs font-medium text-gray-600 mb-1">Total Runs</p>
              <p className="text-2xl font-bold text-gray-900">—</p>
            </div>
            {/* Total Time */}
            <div className="bg-white border border-gray-200 rounded-lg p-4">
              <p className="text-xs font-medium text-gray-600 mb-1">Total Time</p>
              <p className="text-2xl font-bold text-gray-900">—</p>
            </div>
            {/* Adherence — clickable, navigates to /plan per D-11 */}
            <div
              role="button"
              onClick={() => navigate('/plan')}
              className="bg-white border border-gray-200 rounded-lg p-4 cursor-pointer hover:bg-gray-50 hover:shadow-sm transition-all"
            >
              <p className="text-xs font-medium text-gray-600 mb-1">Adherence</p>
              <p className="text-2xl font-bold text-gray-900">—</p>
            </div>
          </div>

          {/* Charts section — populated by useDashboard + Recharts in plan 04-03 */}
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div className="bg-white border border-gray-200 rounded-lg p-4">
              <h2 className="text-lg font-semibold text-gray-900 mb-4">Weekly Volume</h2>
              <div className="h-72 flex items-center justify-center text-gray-400 text-sm">Chart coming soon</div>
            </div>
            <div className="bg-white border border-gray-200 rounded-lg p-4">
              <h2 className="text-lg font-semibold text-gray-900 mb-4">Pace Trend</h2>
              <div className="h-72 flex items-center justify-center text-gray-400 text-sm">Chart coming soon</div>
            </div>
          </div>
        </div>
      )
    }
    ```

    The `activeFilter` state and `FILTER_PRESETS` array are the exact interfaces that `useDashboard` (plan 04-02) will consume. The Adherence card uses `role="button"` and `cursor-pointer` per project conventions.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npm run build 2>&1 | tail -5 && npm ls recharts 2>/dev/null | grep recharts</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains `"recharts"` in dependencies
    - Dashboard.tsx exports `FilterPreset` type and `FILTER_PRESETS` array
    - Dashboard.tsx contains `activeFilter` useState with default `'current-plan'`
    - Dashboard.tsx contains `role="button"` on the Adherence card
    - Dashboard.tsx contains `navigate('/plan')` on click
    - `npm run build` exits 0
    - `npm ls recharts` shows recharts version installed
  </acceptance_criteria>
  <done>Recharts installed; Dashboard scaffold renders heading, filter row, 4 stat placeholders, chart placeholders</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update routing + sidebar tests</name>
  <files>web/src/__tests__/pages.test.tsx, web/src/components/layout/Sidebar.test.tsx</files>
  <read_first>
    - web/src/__tests__/pages.test.tsx — existing Dashboard test (render heading)
    - web/src/components/layout/Sidebar.test.tsx — existing sidebar nav tests
    - web/src/App.tsx — updated routes (for route test)
  </read_first>
  <behavior>
    - Dashboard page renders h1 "Dashboard"
    - Dashboard page renders all 7 filter preset buttons by text
    - Dashboard page renders "Total Distance", "Total Runs", "Total Time", "Adherence" stat card labels
    - Sidebar renders "Dashboard" as first nav link (appears before "Training Plan")
  </behavior>
  <action>
    Update web/src/__tests__/pages.test.tsx:
    - Keep existing Dashboard heading test
    - Add test: renders all 7 filter presets (FILTER_PRESETS labels: 'Current Plan', 'Last 4 weeks', 'Last 8 weeks', 'Last 3 months', 'Last 12 months', 'This year', 'All time') — use `screen.getByText()`
    - Add test: stat card labels render ('Total Distance', 'Total Runs', 'Total Time', 'Adherence')
    - The Dashboard component uses `useNavigate` — wrap render in MemoryRouter from react-router-dom or use a mock

    Update web/src/components/layout/Sidebar.test.tsx if it exists, or create it:
    - Test that "Dashboard" nav link appears before "Training Plan" nav link in the DOM (check `getAllByRole('link')` order)
    - Wrap in MemoryRouter

    Use existing test patterns from the codebase (MemoryRouter wrapper, @testing-library/react, vitest).
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes with no failures
    - pages.test.tsx contains test for Dashboard heading
    - pages.test.tsx contains test that queries 'Current Plan' filter button
    - pages.test.tsx contains test that queries 'Total Distance' stat label
    - Sidebar.test.tsx contains assertion that Dashboard link appears before Training Plan link
  </acceptance_criteria>
  <done>Tests pass; routing and sidebar order verified by automated tests</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` exits 0 (TypeScript clean)
- `cd web && npm test` passes
- `npm ls recharts` shows recharts installed
- App.tsx has `Navigate to="/dashboard"` for both "/" and "*" routes
- Sidebar.tsx navItems[0].path === "/dashboard"
</verification>

<success_criteria>
- / and * routes redirect to /dashboard
- Dashboard first in sidebar
- Recharts installed
- Dashboard scaffold has correct filter/stat/chart structure for downstream plans
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard/04-01-SUMMARY.md`
</output>
