# Phase 2.1: Training Plan Redesign - Context

**Gathered:** 2026-03-24
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace the calendar-based plan view with a structured, markdown-rendered training plan. Upgrade the data model from a flat sessions list to a hierarchical phases→weeks→days structure. Add ChatGPT URL import and an Archive section. All actions must be doable both via the agent (chat) and manually through the UI.

This phase does NOT include run logging or Apple Health upload (Phase 3). Completed/skipped day tracking is introduced here as data fields but completion via run upload is Phase 3.

</domain>

<decisions>
## Implementation Decisions

### Plan data model
- **D-01:** Plan structure is hierarchical JSON: `phases[] → weeks[] → days[]` (not a flat sessions list, not a markdown blob)
- **D-02:** Phase fields: `name` (string, e.g. "Base Building"), `description` (string)
- **D-03:** Week fields: `weekNumber` (int), `startDate` (ISO date string)
- **D-04:** Day fields: `date` (ISO date string), `type` (`run` | `rest` | `cross-train`), `objective` (`{ kind: 'distance' | 'time', value: number, unit: 'km' | 'min' }` — optional on rest days), `guidelines` (string, e.g. "Easy Zone 2 run"), `completed` (bool, default false), `skipped` (bool, default false)
- **D-05:** Plan-level fields: `objective` (enum: `marathon` | `half-marathon` | `15km` | `10km` | `5km`), `targetDate` (ISO date string), `status` (`onboarding` | `active` | `archived`)
- **D-06:** AI defines phase names and structure (base/build/peak/taper pattern) — user does not configure phases manually

### Plan view — display
- **D-07:** Remove `react-big-calendar` entirely — no calendar widget
- **D-08:** Plan is displayed as rendered markdown, computed from the structured JSON data (not stored as markdown)
- **D-09:** Rendering format: one section per phase, each phase shows weeks, each week shows its run days with objective and guidelines
- **D-10:** Completed days shown with a visual indicator (e.g. strikethrough or checkmark); skipped days shown differently

### Plan view — actions
- **D-11:** Training plan section has four actions: (1) Create new plan, (2) Import from ChatGPT URL, (3) Update plan, (4) Close & archive plan
- **D-12:** Project-wide principle: every action the agent can perform is also directly available in the UI — no agent-only operations

### Editing — manual
- **D-13:** Inline click-to-edit on each day's values (objective value, guidelines text). Click a field → becomes an input → save on blur/enter
- **D-14:** Mark day as completed or skipped via a toggle/button directly on the day row (no chat required)
- **D-15:** Editing is only available on days that are not yet completed — completed days are read-only

### Editing — via agent
- **D-16:** Agent emits a `<plan:update>` XML tag to modify specific days or restructure phases. Frontend parses and applies it to the stored structured JSON
- **D-17:** Agent can only update non-completed days (same rule as manual editing)
- **D-18:** Full plan regeneration is possible via "Create new plan" after archiving the current one — agent cannot silently replace an active plan

### ChatGPT URL import
- **D-19:** User pastes a `chatgpt.com/share/...` URL into the import field
- **D-20:** Backend performs a direct HTTP GET to the URL, extracts readable text from the HTML response (no headless browser)
- **D-21:** Extracted text is sent to Claude with a prompt instructing it to parse and normalize into the `phases[]` plan structure
- **D-22:** On import success, plan is saved as active plan (same flow as generation); existing active plan is archived first if one exists

### Archive
- **D-23:** New "Archive" entry in the sidebar navigation
- **D-24:** Archive list shows: plan objective (e.g. "5K") and completion status — no date range
- **D-25:** Clicking an archived plan shows a readonly markdown view using the same rendering as the active plan
- **D-26:** "Close & archive" action on the active plan moves it to `status: archived` and returns user to the empty plan state

### Claude's Discretion
- Exact markdown rendering format for each phase/week/day (heading levels, table vs. list)
- Visual treatment for completed vs. skipped vs. upcoming days
- `<plan:update>` tag schema (what fields it carries to specify the target day and the new values)
- How to handle partial or ambiguous ChatGPT HTML extraction failures (show error vs. fallback to paste)

</decisions>

<specifics>
## Specific Ideas

- ChatGPT import example URL format: `https://chatgpt.com/share/<share-id>`
- Phase names should match real training periodization: Base Building → Build → Peak → Taper
- Guidelines examples: "Easy Zone 2 run", "Tempo intervals 4×1km", "Long run — conversational pace"
- Day objective is either distance (e.g. 8km) OR time (e.g. 45 min) — not both
- Rest days have no objective, just `type: rest`

</specifics>

<canonical_refs>
## Canonical References

No external specs — requirements are fully captured in decisions above.

### Existing implementation to replace/extend
- `api/src/shared/types.ts` — Current `Plan` and `PlanSession` types to be replaced with new hierarchical schema
- `api/src/functions/chat.ts` — `<training_plan>` tag handling; needs a companion `<plan:update>` tag handler
- `api/src/shared/prompts.ts` — System prompt defines plan generation format; must be updated for new structure and `<plan:update>` protocol
- `web/src/components/coach/CoachPanel.tsx` — Existing plan generation trigger; import UI moves here or to plan page
- `web/src/hooks/useChat.ts` — `<training_plan>` stripping and `<plan:update>` execution logic lives here

### Phase 2 deliverables being replaced
- `web/src/components/plan/` — Calendar components (`react-big-calendar`) to be removed
- `api/src/functions/plan.ts` — `generatePlan` endpoint currently builds flat sessions; needs to build hierarchical structure

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `useChat.ts` XML tag parsing pattern (`<app:*/>`, `<training_plan>`): reuse same pattern for `<plan:update>`
- `ChatMessage.tsx` + `react-markdown`: already renders markdown — plan view can use same renderer
- `requirePassword` middleware: all new API endpoints reuse this
- `getDb()` shared module: all new endpoints use this

### Established Patterns
- SSE streaming via `ReadableStream` + `data: {...}\n\n` events — keep for all Claude responses
- App command protocol (`<app:*/>` tags stripped from display, executed after stream done) — extend for `<plan:update>`
- MongoDB `plans` collection already exists; schema migration needed (add `phases[]`, remove `sessions[]`)

### Integration Points
- Sidebar nav: add "Archive" item alongside Dashboard, Plan, Runs
- `AppShell` / routing: add `/archive` route and `/archive/:planId` route
- Plan page (`/plan`): replace calendar component with markdown renderer + action buttons

</code_context>

<deferred>
## Deferred Ideas

- Marking days complete via Apple Health run upload — Phase 3
- Date range display in archive list — future iteration
- Export plan to calendar format — noted in roadmap as v1.2
- Per-phase progress % in archive list — future iteration
- Editing completed-day notes/reflections after the fact — future iteration

</deferred>

---

*Phase: 02.1-training-plan-redesign*
*Context gathered: 2026-03-24*
