---
phase: 05-missing-features
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/__tests__/planPhases.test.ts
  - web/src/__tests__/useChat.trainingPlan.test.ts
autonomous: true
gap_closure: true
requirements: [FEAT-ADD-PHASE-API, FEAT-AGENT-COMMANDS, FEAT-AGENT-RUN-INSIGHT, FEAT-TEST-COVERAGE]

must_haves:
  truths:
    - "addWeekToPhase API endpoint is covered by unit tests (valid add, invalid phaseIndex, missing plan, out-of-range index)"
    - "run:create handler auto-saves coaching insight to created run — verified by test asserting PATCH /api/runs/:id is called with stripped response text"
    - "plan:add-week tag handler in useChat posts to /api/plan/phases/:index/weeks and strips tag from display"
    - "plan:update-feedback tag handler in useChat patches /api/plan with progressFeedback and dispatches plan-updated"
    - "All test layers (API, web, E2E) pass with no regressions"
  artifacts:
    - path: "api/src/__tests__/planPhases.test.ts"
      provides: "addWeekToPhase test suite"
      contains: "describe('POST /api/plan/phases/:phaseIndex/weeks'"
    - path: "web/src/__tests__/useChat.trainingPlan.test.ts"
      provides: "run:create auto-insight, plan:add-week, plan:update-feedback tests"
      contains: "auto-saves coaching insight"
  key_links:
    - from: "planPhases.test.ts addWeekToPhase suite"
      to: "api/src/functions/planPhases.ts addWeekToPhase handler"
      via: "handlers.get('addWeekToPhase')"
    - from: "useChat.trainingPlan.test.ts run:create auto-insight test"
      to: "useChat.ts run:create handler lines 523-548"
      via: "mockFetch PATCH /api/runs/:id assertion"
---

<objective>
Add missing Nyquist tests for three gap-closure features that were implemented in quick commits (7e9056e and 80df606) but shipped without test coverage: (1) addWeekToPhase API endpoint, (2) run:create auto-insight save, (3) plan:add-week and plan:update-feedback useChat tag handlers.

Purpose: The code is already live in the branch. These tests close the Nyquist gap — every implemented behavior must have an automated verify command. Without them the UAT cannot be formally closed.
Output: Additional test cases in planPhases.test.ts and useChat.trainingPlan.test.ts; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@api/src/__tests__/planPhases.test.ts
@api/src/functions/planPhases.ts
@web/src/__tests__/useChat.trainingPlan.test.ts
@web/src/hooks/useChat.ts
</context>

<interfaces>
<!-- Key patterns from planPhases.test.ts — reuse for addWeekToPhase tests -->

Handler registration (already done by side-effect import at top of file):
```typescript
import '../functions/planPhases.js';
// handlers Map has: 'patchPhase', 'addPhase', 'addWeekToPhase', 'deleteLastPhase'
```

Request factory pattern (add alongside existing helpers):
```typescript
function makeAddWeekReq(phaseIndex: string): HttpRequest {
  const url = `http://localhost/api/plan/phases/${phaseIndex}/weeks`;
  const req = new HttpRequest({
    method: 'POST',
    url,
    headers: { 'x-app-password': 'test-pw' },
    params: { phaseIndex },
  });
  return req;
}
```

validActivePlan fixture (already in file):
```typescript
// phases[0] = 'Base Building', weeks: [{ weekNumber: 1, days: [...] }]
// phases[1] = 'Build Phase',   weeks: [{ weekNumber: 2, days: [...] }]
```

<!-- Key patterns from useChat.trainingPlan.test.ts — add to existing Phase 5 describe block -->

Mock order for sendMessage with tag that calls one API + fetchPlan:
```
mockFetch[0] = stream response (makeStreamResponse)
mockFetch[1] = GET /api/plan (onDone fetchPlan)
mockFetch[2] = POST or PATCH (tag handler API call)
mockFetch[3] = GET /api/plan (applyPlanOperations fetchPlan)
```

For run:create auto-insight (stream → POST /api/runs returns { _id } → PATCH /api/runs/:id → fetchPlan):
```
mockFetch[0] = stream
mockFetch[1] = GET /api/plan (onDone)
mockFetch[2] = POST /api/runs → { ok: true, json: () => ({ _id: 'newRun99' }) }
mockFetch[3] = PATCH /api/runs/newRun99 (auto-insight save, non-fatal)
mockFetch[4] = GET /api/plan (applyPlanOperations)
```
Note: The auto-insight PATCH is fire-and-forget (wrapped in .catch(() => {})). The test must still mock it — mockFetch.mockResolvedValueOnce is consumed in order.

Filter helper for asserting calls:
```typescript
const patchInsightCalls = mockFetch.mock.calls.filter(([url, opts]: any[]) =>
  typeof url === 'string' && url === '/api/runs/newRun99' && opts?.method === 'PATCH'
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API unit tests for addWeekToPhase endpoint</name>
  <files>api/src/__tests__/planPhases.test.ts</files>
  <behavior>
    - Test: POST with valid phaseIndex=0 returns 201 and plan has an extra week in phases[0]
    - Test: POST with valid phaseIndex=1 returns 201 and plan has extra week in phases[1]; weekNumbers are reassigned by assignPlanStructure (phase 0 keeps weekNumber 1, phase 1 week 0 keeps weekNumber 2, phase 1 week 1 gets weekNumber 3)
    - Test: POST with phaseIndex='abc' (NaN) returns 400 with error about invalid phaseIndex
    - Test: POST with phaseIndex=99 (out of range) returns 404 with error about phase index not existing
    - Test: POST when no active plan exists returns 404
  </behavior>
  <action>
    Add a new `describe('POST /api/plan/phases/:phaseIndex/weeks', () => { ... })` block at the end of planPhases.test.ts (before the closing of the file).

    Add a `makeAddWeekReq(phaseIndex: string): HttpRequest` factory function alongside the existing helpers (after `makeDeleteReq`). The factory follows the same pattern — sets `params: { phaseIndex }`.

    The handler is already registered as `'addWeekToPhase'` in the handlers Map by the existing side-effect import (`import '../functions/planPhases.js'`) at the top of the file — do NOT add another import.

    Test structure (each test uses `beforeEach` from the outer scope which clears the collection):
    - For the "adds week" tests, insert `{ ...validActivePlan }` then call `handlers.get('addWeekToPhase')!(req, ctx)`.
    - For "no plan" test, skip the insert.

    The week added has `weekNumber: 0, days: []` before reassignment. After `assignPlanStructure`, the new week gets the next sequential number. Assert `result.jsonBody.plan.phases[phaseIndex].weeks` has length `validActivePlan.phases[phaseIndex].weeks.length + 1`.

    For the weekNumber reassignment test (add to phase index 1): assert `result.jsonBody.plan.phases[1].weeks` has length 2 and `result.jsonBody.plan.phases[1].weeks[1].weekNumber` equals 3.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/api && npx vitest run src/__tests__/planPhases.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All addWeekToPhase tests pass. planPhases.test.ts shows 5+ new passing tests in the new describe block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useChat tests — run:create auto-insight + plan:add-week + plan:update-feedback</name>
  <files>web/src/__tests__/useChat.trainingPlan.test.ts</files>
  <behavior>
    run:create auto-insight:
    - Test: when run:create tag is processed and POST /api/runs returns { _id: 'newRun99' }, a PATCH to /api/runs/newRun99 is called with { insight: <stripped response text> }. The insight text must NOT contain XML tags (run:create, app:*, plan:* all stripped).

    plan:add-week:
    - Test: plan:add-week tag strips from message, POSTs to /api/plan/phases/0/weeks, dispatches plan-updated
    - Test: plan:add-week missing phaseIndex shows ⚠️ error in message

    plan:update-feedback:
    - Test: plan:update-feedback tag strips from message, PATCHes /api/plan with { progressFeedback: '...' }, dispatches plan-updated
    - Test: plan:update-feedback with empty feedback attr is silently skipped (no PATCH call)
  </behavior>
  <action>
    Add these tests inside the existing `describe('sendMessage — phase 5 new tag handlers', () => { ... })` block at the bottom of useChat.trainingPlan.test.ts, before the closing `})`.

    **run:create auto-insight test:**
    Stream text: `'data: {"text":"Logged your run <run:create date=\\"2026-04-10\\" distance=\\"8\\" duration=\\"45:00\\"\\/>. Great effort!"}\n\n'`
    Mock order:
    1. makeStreamResponse (stream)
    2. GET /api/plan (onDone fetchPlan) → `{ plan: testPlan }`
    3. POST /api/runs → `{ ok: true, json: async () => ({ _id: 'newRun99' }) }`
    4. PATCH /api/runs/newRun99 (auto-insight) → `{ ok: true, json: async () => ({}) }`
    5. GET /api/plan (applyPlanOperations) → `{ plan: testPlan }`

    Assert: a PATCH call to `/api/runs/newRun99` exists, body has `insight` field, insight does NOT contain `run:create`, insight DOES contain "Great effort!" (the non-tag portion of response).

    **plan:add-week tests:**
    Stream text: `'data: {"text":"Added a week <plan:add-week phaseIndex=\\"0\\"\\/>."}\n\n'`
    Mock order (valid case):
    1. stream
    2. GET /api/plan (onDone)
    3. POST /api/plan/phases/0/weeks → `{ ok: true, json: async () => ({}) }`
    4. GET /api/plan (applyPlanOperations)

    Assert POST to `/api/plan/phases/0/weeks`, tag stripped from message, plan-updated dispatched (listen with window.addEventListener).

    Missing phaseIndex test: Stream `<plan:add-week/>` (no attrs). No POST should be made. Message should contain `⚠️`.

    **plan:update-feedback tests:**
    Stream text: `'data: {"text":"<plan:update-feedback feedback=\\"You are on track for your goal. Keep the easy runs easy.\\"/>"}\n\n'`
    Mock order (valid case):
    1. stream
    2. GET /api/plan (onDone)
    3. PATCH /api/plan → `{ ok: true, json: async () => ({}) }`
    4. GET /api/plan (applyPlanOperations)

    Assert PATCH to `/api/plan` with body `{ progressFeedback: 'You are on track for your goal. Keep the easy runs easy.' }`, tag stripped, plan-updated dispatched.

    Empty feedback test: Stream `<plan:update-feedback feedback=""/>`. No PATCH should be made (the handler has `if (!attrs.feedback) continue` guard).

    Use `mockFetch.mock.calls.filter(...)` pattern consistent with existing tests to assert specific calls.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npx vitest run src/__tests__/useChat.trainingPlan.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All new tests pass. useChat.trainingPlan.test.ts shows 6+ new passing tests. No regressions in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files></files>
  <action>
    Run all three test layers to confirm no regressions:

    1. API tests: `cd api && npm test`
    2. Web tests: `cd web && npm test`
    3. TypeScript build: `cd web && npm run build`

    Do NOT run E2E tests (they require a running server and Docker; the existing 62 E2E tests already cover the UI paths).

    If any test fails: diagnose from the output. The most likely failures are mock count mismatches in the useChat tests (the auto-insight PATCH adds one extra mockFetch call that must be accounted for in the mock queue).
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/api && npm test 2>&1 | grep -E "Tests|passed|failed" | tail -5 && cd /c/dev/ai-running-coach/web && npm test 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>API tests: all pass (198+ tests). Web tests: all pass (424+ tests). TypeScript build: clean (0 errors). No regressions introduced.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cd api && npm test` — all tests green, count increased by ~5 (addWeekToPhase suite)
2. `cd web && npm test` — all tests green, count increased by ~6 (new useChat tag tests)
3. `cd web && npm run build` — TypeScript compiles clean

Check that the new describes appear in test output:
- `describe('POST /api/plan/phases/:phaseIndex/weeks')` in API output
- New `it(...)` blocks in `describe('sendMessage — phase 5 new tag handlers')` in web output
</verification>

<success_criteria>
- addWeekToPhase endpoint has API unit tests covering: valid add (phase 0), valid add (phase 1 with weekNumber reassignment), invalid phaseIndex (NaN → 400), out-of-range index (→ 404), no plan (→ 404)
- run:create auto-insight PATCH is verified by a test asserting PATCH /api/runs/:id is called with stripped response text after successful POST /api/runs
- plan:add-week tag handler verified: POSTs to correct URL, strips tag, dispatches plan-updated, errors on missing phaseIndex
- plan:update-feedback tag handler verified: PATCHes /api/plan with progressFeedback, strips tag, dispatches plan-updated, silently skips empty feedback
- Full API and web test suites pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-missing-features/05-05-SUMMARY.md`
</output>
