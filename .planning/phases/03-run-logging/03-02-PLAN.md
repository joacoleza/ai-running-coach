---
phase: 03-run-logging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/functions/runs.ts
  - api/src/functions/planDays.ts
  - api/src/__tests__/runs.test.ts
  - api/src/__tests__/runs.integration.test.ts
autonomous: true
requirements: [RUN-01, RUN-04]

must_haves:
  truths:
    - "Manual run POST creates a run record in the runs collection"
    - "Run linked to active plan day marks the day completed"
    - "Run for a date with no plan entry stores unlinked"
    - "Undo on completed day with linked run unlinks the run but does not delete it"
    - "Linked runs cannot be deleted; unlinked runs can be deleted"
    - "GET /api/runs returns all runs in reverse date order"
    - "Plan replace guard blocks regeneration when days have linked run data"
  artifacts:
    - path: "api/src/functions/runs.ts"
      provides: "Run CRUD endpoints"
      exports: ["POST /api/runs", "GET /api/runs", "GET /api/runs/:id", "DELETE /api/runs/:id"]
    - path: "api/src/__tests__/runs.test.ts"
      provides: "Unit tests for run endpoints"
  key_links:
    - from: "api/src/functions/runs.ts"
      to: "api/src/shared/types.ts"
      via: "Run interface import"
      pattern: "import.*Run.*from.*types"
    - from: "api/src/functions/runs.ts"
      to: "plans collection"
      via: "run-to-plan linking updates plan day"
      pattern: "plans.*findOneAndUpdate"
---

<objective>
Create the Run CRUD API endpoints (POST, GET, GET/:id, DELETE/:id) with run-to-plan linking logic per D-04/D-05/D-06 rules. Extend the plan day undo (PATCH) to unlink runs per D-10. Extend plan replace guard per D-12.

Purpose: This is the core data layer for run logging — manual runs are created via POST, plan days get marked completed and linked, and the Runs list is served by GET.
Output: runs.ts Azure Function with all CRUD + linking, updated planDays.ts for undo-unlink, tests passing.
</objective>

<execution_context>
@C:\Users\joaco\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\joaco\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-run-logging/03-CONTEXT.md
@.planning/phases/03-run-logging/03-RESEARCH.md

<interfaces>
From api/src/functions/planDays.ts (pattern to follow):
```typescript
import { app, HttpRequest, HttpResponseInit, InvocationContext } from '@azure/functions';
import { requirePassword } from '../middleware/auth.js';
import { getDb } from '../shared/db.js';
import type { Plan } from '../shared/types.js';

app.http('patchDay', {
  methods: ['PATCH'],
  authLevel: 'anonymous',
  route: 'plan/days/{date}',
  handler: async (req: HttpRequest, context: InvocationContext): Promise<HttpResponseInit> => {
    const denied = await requirePassword(req);
    if (denied) return denied;
    // ...
  },
});
```

From api/src/middleware/auth.ts:
```typescript
export async function requirePassword(req: HttpRequest): Promise<HttpResponseInit | null>;
```

From api/src/shared/db.ts:
```typescript
export async function getDb(): Promise<Db>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run CRUD API endpoints with run-to-plan linking</name>
  <files>api/src/functions/runs.ts, api/src/__tests__/runs.test.ts, api/src/__tests__/runs.integration.test.ts</files>
  <read_first>api/src/functions/planDays.ts, api/src/shared/types.ts, api/src/shared/db.ts, api/src/middleware/auth.ts</read_first>
  <behavior>
    - POST /api/runs with { date, source:'manual', distance, duration, avgHR? } creates run, returns 201
    - POST /api/runs with date matching active plan day: links run (sets planId + planDayDate), marks day completed=true
    - POST /api/runs with date matching skipped active plan day: unskips day, marks completed, links run (D-04 rule 3)
    - POST /api/runs with date matching already-completed active plan day: returns 409 (D-04 rule 4)
    - POST /api/runs with date not in any plan: stores unlinked, returns 201 (D-06)
    - POST /api/runs with date in archived plan, day not completed: stores run unlinked (D-05 rule 1) — does NOT link to archived days
    - POST /api/runs with date in archived plan, day completed without run: links run to archived day (D-05 rule 2)
    - POST /api/runs with date in archived plan, day already has run linked: returns 409 (D-05 rule 3)
    - GET /api/runs returns all runs sorted by date desc
    - GET /api/runs supports query filters: dateFrom, dateTo, distanceMin, distanceMax, durationMin, durationMax
    - GET /api/runs/:id returns single run or 404
    - DELETE /api/runs/:id on unlinked run: deletes, returns 204 (D-11)
    - DELETE /api/runs/:id on linked run (planId set): returns 409 (D-11)
  </behavior>
  <action>
    Create `api/src/functions/runs.ts` with four Azure Functions v4 HTTP handlers:

    **1. POST /api/runs (`createRun`)**
    - Route: `runs`
    - Auth: `requirePassword(req)`
    - Body: `{ date: string, source: 'manual'|'apple-health', distance: number, duration: number, avgHR?: number, maxHR?: number, pace?: string, cadence?: number, elevationGain?: number, hrZone?: number, hrZoneLabel?: string }`
    - Validate: date is YYYY-MM-DD, distance > 0, duration > 0
    - Compute pace if not provided: `const totalSeconds = duration * 60; const paceSeconds = totalSeconds / distance; const paceMM = Math.floor(paceSeconds / 60); const paceSS = Math.round(paceSeconds % 60); pace = paceMM + ':' + String(paceSS).padStart(2, '0');`
    - Check for duplicate: query `runs` collection for existing run with same `date` and `source`. If found, return 409 `{ error: 'Run already exists for this date' }`.
    - **Run-to-plan linking (D-04, D-05, D-06):**
      1. Query `plans` collection for active plan (`status: { $in: ['active', 'onboarding'] }`) that has a day with this date
      2. If active plan day found:
         - If `day.completed === true`: return 409 `{ error: 'Plan day already completed' }`
         - If `day.skipped === true`: unskip it (set `skipped: false`), then mark `completed: true`
         - Else: mark `completed: true`
         - Set `planId` and `planDayDate` on the run
         - Use `findOneAndUpdate` with `arrayFilters` matching `phases.weeks.days` element by date
      3. If no active plan match, check archived plans (`status: 'archived'`):
         - Find the archived plan + day by date
         - If day is completed and no existing run linked to that planId+date: link the run (set planId, planDayDate)
         - If day is completed and already has a run linked: return 409
         - If day is not completed: store run unlinked (D-05 rule 1)
      4. If no plan match at all: store run unlinked (D-06)
    - Insert into `runs` collection with `createdAt: new Date()`
    - Return 201 with the created run document

    **2. GET /api/runs (`listRuns`)**
    - Route: `runs`
    - Auth: `requirePassword(req)`
    - Query params: `dateFrom`, `dateTo`, `distanceMin`, `distanceMax`, `durationMin`, `durationMax`
    - Build MongoDB filter from query params using `$gte`/`$lte` on respective fields
    - Sort: `{ date: -1 }` (newest first, per D-17)
    - Return all matching documents (no pagination — D-18 infinite scroll handled client-side)

    **3. GET /api/runs/:id (`getRun`)**
    - Route: `runs/{id}`
    - Auth: `requirePassword(req)`
    - Query by `_id: new ObjectId(id)`
    - Return 404 if not found

    **4. DELETE /api/runs/:id (`deleteRun`)**
    - Route: `runs/{id}`
    - Auth: `requirePassword(req)`
    - Find run by id
    - If `planId` is set (linked): return 409 `{ error: 'Cannot delete a run linked to a plan day. Undo the day completion first.' }` (D-11)
    - Else: delete and return 204

    Tests: Write unit tests mocking `getDb()` and `requirePassword`. Write integration tests using `mongodb-memory-server` for the linking logic.
  </action>
  <verify>
    <automated>cd api && npm test -- --run --grep "runs"</automated>
  </verify>
  <acceptance_criteria>
    - api/src/functions/runs.ts contains "app.http('createRun'"
    - api/src/functions/runs.ts contains "app.http('listRuns'"
    - api/src/functions/runs.ts contains "app.http('getRun'"
    - api/src/functions/runs.ts contains "app.http('deleteRun'"
    - api/src/functions/runs.ts contains "requirePassword"
    - api/src/functions/runs.ts contains logic checking "day.completed" for 409
    - api/src/functions/runs.ts contains "planId" linking logic
    - api/src/__tests__/runs.test.ts exits 0
  </acceptance_criteria>
  <done>All 4 run endpoints work with correct run-to-plan linking per D-04/D-05/D-06 rules. Unit + integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend planDays PATCH for run unlinking + plan replace guard for D-12</name>
  <files>api/src/functions/planDays.ts, api/src/functions/plan.ts</files>
  <read_first>api/src/functions/planDays.ts, api/src/functions/plan.ts, api/src/shared/types.ts</read_first>
  <action>
    **1. Extend `PATCH /api/plan/days/:date` in planDays.ts for undo-unlink (D-10):**
    When the PATCH handler processes `completed: 'false'` (undo):
    - After setting `completed: false` on the plan day, also unlink any run associated with this date
    - Query `runs` collection: `db.collection('runs').updateOne({ planDayDate: date, planId: planId }, { $unset: { planId: '', planDayDate: '' } })`
    - This ensures the run record stays in the runs list but is no longer linked to the plan day
    - Same logic for `skipped: 'false'` is not needed (skipped days don't have runs linked)

    **2. Extend plan replace guard in plan.ts for D-12:**
    Read `api/src/functions/plan.ts` to find the `POST /api/plan/generate` and `POST /api/plan` handlers.
    In addition to existing guards, add a check: query `runs` collection for any run with `planId` matching the active plan's `_id`. If found, return 409 `{ error: 'Cannot regenerate plan — runs are linked to training days. Use the coach to adjust specific days instead.' }`.

    This implements D-12: coach cannot discard a plan if any days have linked run data.
  </action>
  <verify>
    <automated>cd api && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - api/src/functions/planDays.ts contains "runs" collection query for unlinking on undo
    - api/src/functions/planDays.ts contains "$unset" for planId/planDayDate
    - api/src/functions/plan.ts contains check for linked runs before plan regeneration
    - api/src/functions/plan.ts contains "Cannot regenerate plan" error message
    - All existing planDays tests still pass
  </acceptance_criteria>
  <done>Undo on completed day with linked run unlinks the run (D-10). Plan regeneration blocked when runs are linked (D-12). All tests pass.</done>
</task>

</tasks>

<verification>
- `cd api && npm test -- --run` — all tests pass including new run CRUD + linking tests
- `cd api && npm run build` — TypeScript compiles
- POST /api/runs creates runs with correct linking behavior
- DELETE /api/runs/:id respects linked/unlinked status
- PATCH undo unlinks runs
- Plan regeneration blocked when runs exist
</verification>

<success_criteria>
- All 4 Run CRUD endpoints functional with auth
- Run-to-plan linking follows D-04 (active), D-05 (archived), D-06 (no plan) rules exactly
- Undo on completed day unlinks run but does not delete it (D-10)
- Linked runs cannot be deleted; unlinked runs can (D-11)
- Plan regeneration blocked when linked runs exist (D-12)
- All unit + integration tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-run-logging/03-02-SUMMARY.md`
</output>
