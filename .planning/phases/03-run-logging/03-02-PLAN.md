---
phase: 03-run-logging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/functions/chat.ts
  - api/src/shared/prompts.ts
autonomous: true
requirements: [COACH-03, COACH-04]

must_haves:
  truths:
    - "Synthetic context injection includes actual run date, distance, pace, and insight for completed days"
    - "Synthetic context injection includes plan.progressFeedback when set"
    - "Coach has full run data context for providing informed feedback"
  artifacts:
    - path: "api/src/functions/chat.ts"
      provides: "Extended synthetic plan-state injection with run data"
      contains: "collection('runs')"
    - path: "api/src/shared/prompts.ts"
      provides: "System prompt awareness of run data context format"
  key_links:
    - from: "api/src/functions/chat.ts"
      to: "MongoDB runs collection"
      via: "fetches linked runs for completed plan days"
      pattern: "collection\\('runs'\\)"
    - from: "api/src/functions/chat.ts"
      to: "synthetic user message"
      via: "injects run date/distance/pace/insight into plan state context"
      pattern: "progressFeedback|insight"
---

<objective>
Extend the synthetic plan-state context injection in chat.ts to include run data for completed days and progressFeedback, so the coach has full context when providing feedback.

Purpose: Per D-27 and D-28 in CONTEXT.md, the coach must see actual run performance data (date, distance, pace, insight) and existing feedback when responding. Without this, coaching feedback would be uninformed.
Output: Extended chat.ts synthetic injection, updated prompts.ts system prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-run-logging/03-CONTEXT.md

<interfaces>
<!-- From api/src/shared/types.ts (after Plan 01 adds Run type) -->
```typescript
export interface Run {
  _id?: ObjectId;
  date: string;
  distance: number;
  duration: string;
  pace: number;
  avgHR?: number;
  notes?: string;
  planId?: ObjectId;
  weekNumber?: number;
  dayLabel?: string;
  insight?: string;
  userId?: string;
  createdAt: Date;
  updatedAt: Date;
}

// Plan now has:
progressFeedback?: string;
```

<!-- Existing synthetic injection pattern in chat.ts -->
The synthetic user message currently contains plan state: all non-rest days with week number, label, status, and guidelines.
Format example: "Week 1 Day A [COMPLETED]: 5km - Easy run"
This needs to be extended with run data for completed days.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend synthetic plan-state injection with run data and progressFeedback</name>
  <files>api/src/functions/chat.ts, api/src/shared/prompts.ts</files>
  <read_first>
    - api/src/functions/chat.ts (full file — find the synthetic plan-state injection section where it builds the user message with plan days)
    - api/src/shared/prompts.ts (full file — system prompt, to add instruction about run data context format)
    - api/src/shared/types.ts (Run and Plan types)
    - .planning/phases/03-run-logging/03-CONTEXT.md (D-27, D-28 — exact requirements for what to inject)
  </read_first>
  <action>
**1. In `chat.ts`, extend the synthetic plan-state injection block:**

After fetching the active plan (which already happens for the synthetic injection), also fetch linked runs:

```typescript
const runs = await db.collection('runs').find({
  planId: plan._id
}).toArray();
```

Build a lookup map: `Map<string, Run>` keyed by `${weekNumber}-${dayLabel}` for quick access.

When building the synthetic user message, for each completed day that has a linked run, extend the line:
- Current format: `Week 1 Day A [COMPLETED]: 5km — Easy run`
- New format: `Week 1 Day A [COMPLETED]: 5km — Easy run | Ran: 03/04/2026, 5.2km @ 5:30/km` (+ ` | Insight: <first 150 chars>` if insight exists)

The run date should be formatted as DD/MM/YYYY (per D-04 in CONTEXT.md).
The pace should be formatted as M:SS/km (convert the decimal pace number to MM:SS format).

**2. Inject `plan.progressFeedback` near the top of the synthetic context:**

If `plan.progressFeedback` is set and non-empty, add it before the day listing:
```
Coach's previous progress assessment: <progressFeedback text>
```

**3. In `prompts.ts`, add a brief instruction in the system prompt** telling Claude about the run data context:

In the system prompt section that describes the plan state format, add:
```
Completed days may include actual run data: date, distance, pace, and a previous coaching insight (truncated). Use this data when providing feedback — do not ask the user to repeat information that's already in the context.
```

Also add:
```
If a "Coach's previous progress assessment" is shown, you've already given high-level plan feedback. Build on it rather than repeating the same observations.
```

**Pace formatting helper** (add in chat.ts or a shared util):
```typescript
function formatPace(paceDecimal: number): string {
  const minutes = Math.floor(paceDecimal);
  const seconds = Math.round((paceDecimal - minutes) * 60);
  return `${minutes}:${String(seconds).padStart(2, '0')}`;
}
```
  </action>
  <verify>
    <automated>cd api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `api/src/functions/chat.ts` contains `collection('runs')` — fetches runs for context injection
    - `api/src/functions/chat.ts` contains `progressFeedback` — injects plan progress feedback into synthetic context
    - `api/src/functions/chat.ts` contains a pace formatting operation (grep for `formatPace` or `Math.floor.*paceDecimal` or similar)
    - `api/src/functions/chat.ts` synthetic message includes run date in DD/MM/YYYY format (grep for `padStart` or date formatting near the injection block)
    - `api/src/shared/prompts.ts` contains text mentioning "run data" or "actual run" in the system prompt
    - `api/src/shared/prompts.ts` contains text mentioning "previous progress assessment" or "progressFeedback"
    - `npx tsc --noEmit` in api/ exits 0
  </acceptance_criteria>
  <done>Synthetic context injection includes run date, distance, pace, and insight for completed days. progressFeedback injected when set. System prompt updated to reference run data context. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `cd api && npx tsc --noEmit` passes
- Synthetic injection block fetches from runs collection
- Completed days in synthetic message include run date + distance + pace
- progressFeedback appears in synthetic context when set
- System prompt references run data and progress assessment
</verification>

<success_criteria>
- chat.ts fetches runs by planId and includes run data in the synthetic plan-state message
- Completed day lines include: actual run date (DD/MM/YYYY), distance, pace (M:SS format), and truncated insight
- progressFeedback injected at top of synthetic context when present on the plan
- System prompt tells Claude to use run data and not repeat observations from previous assessment
</success_criteria>

<output>
After completion, create `.planning/phases/03-run-logging/03-02-SUMMARY.md`
</output>
