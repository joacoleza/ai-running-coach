---
phase: 21-dashboard-discipline-sections
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - web/src/__tests__/Dashboard.test.tsx
  - web/src/__tests__/WeightProgressionChart.test.tsx
  - web/src/__tests__/WeeklyVolumeChart.test.tsx
  - web/src/__tests__/WeeklySpeedChart.test.tsx
  - web/src/__tests__/WeeklyDurationChart.test.tsx
  - web/src/__tests__/useDashboard.test.ts
autonomous: true
requirements: [DASH2-03, DASH2-04, DASH2-05]

must_haves:
  truths:
    - "npm test in web/ passes with 0 failing tests"
    - "Dashboard.test.tsx covers per-discipline section rendering, section headers with counts, section hide/show logic"
    - "WeeklySpeedChart.test.tsx covers empty state and chart rendering"
    - "WeeklyDurationChart.test.tsx covers empty state and chart rendering"
    - "WeightProgressionChart.test.tsx covers the defaultExercise auto-select behavior"
    - "WeeklyVolumeChart.test.tsx is deleted (the component no longer exists)"
    - "useDashboard.test.ts covers new per-discipline exports (runRuns, cycleRuns, gymRuns)"
    - "cd web && npm run build exits 0"
    - "npx playwright test passes with no regressions in existing dashboard E2E tests"
  artifacts:
    - path: "web/src/__tests__/Dashboard.test.tsx"
      provides: "Updated Dashboard tests for per-discipline section layout"
    - path: "web/src/__tests__/WeeklySpeedChart.test.tsx"
      provides: "New tests for WeeklySpeedChart component"
    - path: "web/src/__tests__/WeeklyDurationChart.test.tsx"
      provides: "New tests for WeeklyDurationChart component"
    - path: "web/src/__tests__/WeightProgressionChart.test.tsx"
      provides: "Updated tests including defaultExercise prop"
    - path: "web/src/__tests__/useDashboard.test.ts"
      provides: "Updated tests for new per-discipline hook exports"
  key_links:
    - from: "Dashboard.test.tsx mocks"
      to: "WeeklySpeedChart, WeeklyDurationChart"
      via: "vi.mock('../components/dashboard/WeeklySpeedChart') etc."
    - from: "WeightProgressionChart.test.tsx"
      to: "defaultExercise prop"
      via: "render(<WeightProgressionChart exerciseOptions={...} defaultExercise='Squat' />) + waitFor fetch"
---

<objective>
Update all affected test files to match the new Dashboard structure from Plan 02. Delete WeeklyVolumeChart.test.tsx. Add new test files for WeeklySpeedChart and WeeklyDurationChart. Verify the full web build passes. Run the full E2E suite to confirm no regressions.

Purpose: Tests are part of execution, not optional UAT. No test should reference removed components or old Dashboard structure. E2E must pass before this phase is considered done.

Output:
- web/src/__tests__/Dashboard.test.tsx (rewritten for per-discipline sections)
- web/src/__tests__/WeeklySpeedChart.test.tsx (new)
- web/src/__tests__/WeeklyDurationChart.test.tsx (new)
- web/src/__tests__/WeightProgressionChart.test.tsx (updated — defaultExercise tests)
- web/src/__tests__/WeeklyVolumeChart.test.tsx (DELETED)
- web/src/__tests__/useDashboard.test.ts (updated — new exports covered)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-dashboard-discipline-sections/21-CONTEXT.md
@web/src/pages/Dashboard.tsx
@web/src/hooks/useDashboard.ts
@web/src/components/dashboard/WeeklySpeedChart.tsx
@web/src/components/dashboard/WeeklyDurationChart.tsx
@web/src/components/dashboard/WeightProgressionChart.tsx
</context>

<interfaces>
<!-- Current test patterns (from existing Dashboard.test.tsx): -->
```typescript
// useDashboard mock shape — must be extended with new fields:
function makeDefaults(overrides = {}): ReturnType<typeof useDashboard> {
  return {
    activeFilter: 'current-plan',
    setActiveFilter: vi.fn(),
    activeDiscipline: 'all',
    setActiveDiscipline: vi.fn(),
    stats: { totalDistance: '42.5km', totalRuns: 8, totalTime: '3h25m', adherence: '75%', progress: '40%' },
    weeklyData: [...],
    multiWeeklyData: [],
    paceData: [...],
    paceBpmData: [],
    runs: [],
    // NEW fields added in Plan 02:
    runRuns: [],
    cycleRuns: [],
    gymRuns: [],
    runWeeklyBuckets: [],
    cycleWeeklyBuckets: [],
    gymWeeklyBuckets: [],
    isLoading: false,
    isPlanLoading: false,
    hasPlan: true,
    ...overrides,
  }
}
```

<!-- vi.mock patterns for new components: -->
```typescript
vi.mock('../components/dashboard/WeeklySpeedChart', () => ({
  WeeklySpeedChart: () => <div data-testid="weekly-speed-chart" />,
}))
vi.mock('../components/dashboard/WeeklyDurationChart', () => ({
  WeeklyDurationChart: () => <div data-testid="weekly-duration-chart" />,
}))
// WeightProgressionChart mock already exists — keep it
// WeeklyVolumeChart mock — REMOVE (component deleted)
```

<!-- Run stub for useDashboard tests: -->
```typescript
function makeDisciplineRun(date, distance, duration, discipline): Run {
  // Same helper already in useDashboard.test.ts
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete WeeklyVolumeChart.test.tsx + update Dashboard.test.tsx + update WeightProgressionChart.test.tsx + update useDashboard.test.ts</name>
  <files>
    web/src/__tests__/WeeklyVolumeChart.test.tsx
    web/src/__tests__/Dashboard.test.tsx
    web/src/__tests__/WeightProgressionChart.test.tsx
    web/src/__tests__/useDashboard.test.ts
  </files>
  <read_first>
    - web/src/__tests__/Dashboard.test.tsx (full file — current state to understand what needs replacing)
    - web/src/__tests__/WeightProgressionChart.test.tsx (current state — to add defaultExercise tests)
    - web/src/__tests__/useDashboard.test.ts (current state — to understand what exists before adding)
    - web/src/pages/Dashboard.tsx (read the final Dashboard.tsx from Plan 02 to know what to test)
    - web/src/hooks/useDashboard.ts (read the final useDashboard.ts from Plan 02 to know new exports)
  </read_first>
  <action>
**Step 1 — Delete WeeklyVolumeChart.test.tsx:**
Delete web/src/__tests__/WeeklyVolumeChart.test.tsx.

**Step 2 — Rewrite Dashboard.test.tsx:**

Read the current Dashboard.test.tsx. Completely rewrite it to match the new per-discipline section structure. Keep the same test patterns (vi.mock, makeDefaults, MemoryRouter, describe/it blocks).

Key changes needed:
1. Remove WeeklyVolumeChart mock (component deleted)
2. Add WeeklySpeedChart and WeeklyDurationChart mocks:
   ```typescript
   vi.mock('../components/dashboard/WeeklySpeedChart', () => ({
     WeeklySpeedChart: () => <div data-testid="weekly-speed-chart" />,
   }))
   vi.mock('../components/dashboard/WeeklyDurationChart', () => ({
     WeeklyDurationChart: () => <div data-testid="weekly-duration-chart" />,
   }))
   ```
3. Keep WeightProgressionChart mock as-is
4. Update makeDefaults to include the new useDashboard return values:
   ```typescript
   runRuns: [],
   cycleRuns: [],
   gymRuns: [],
   runWeeklyBuckets: [],
   cycleWeeklyBuckets: [],
   gymWeeklyBuckets: [],
   ```
5. Update the useDashboard mock import to include new fields (formatPaceToMMSS, parseDurationToMinutes, formatTotalTime, computeAvgSpeed as needed by the Dashboard component — check Dashboard.tsx imports)

Remove or update these test descriptions that no longer apply:
- "renders Weekly Volume chart section" — remove (WeeklyVolumeChart gone)
- "renders Total Runs label and value" — this now appears only inside the Run section; update test to work with new structure
- "does NOT render Weekly Volume chart section" — remove
- "WeeklyVolumeChart renders" describe block — remove

Add new test descriptions covering the per-discipline section behavior:

```typescript
describe('Run section', () => {
  it('renders Run section heading with session count when runRuns is non-empty', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      runRuns: [{ _id: 'r1', date: '2026-04-07', distance: 5, duration: '40:00', pace: 8, discipline: 'run', createdAt: '', updatedAt: '' }],
      runWeeklyBuckets: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByText(/^Run \(1 sessions?\)/)).toBeInTheDocument()
  })

  it('hides Run section when activeDiscipline is "gym"', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      activeDiscipline: 'gym',
      gymRuns: [{ _id: 'g1', date: '2026-04-07', distance: 0, duration: '45:00', pace: 0, discipline: 'gym', createdAt: '', updatedAt: '' }],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.queryByText(/^Run \(/)).not.toBeInTheDocument()
  })

  it('hides Run section when activeDiscipline is "all" and runRuns is empty', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      activeDiscipline: 'all',
      runRuns: [],
      cycleRuns: [],
      gymRuns: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.queryByText(/^Run \(/)).not.toBeInTheDocument()
  })
})

describe('Cycling section', () => {
  it('renders Cycling section heading with session count when cycleRuns is non-empty', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      cycleRuns: [{ _id: 'c1', date: '2026-04-07', distance: 20, duration: '60:00', pace: 0, discipline: 'cycle', createdAt: '', updatedAt: '' }],
      cycleWeeklyBuckets: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByText(/^Cycling \(1 sessions?\)/)).toBeInTheDocument()
  })

  it('renders WeeklySpeedChart inside Cycling section when cycleRuns has data', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      cycleRuns: [{ _id: 'c1', date: '2026-04-07', distance: 20, duration: '60:00', pace: 0, discipline: 'cycle', createdAt: '', updatedAt: '' }],
      cycleWeeklyBuckets: [{ weekKey: '2026-04-06', weekLabel: 'Apr 6', distance: 20, avgPace: null, totalDurationMinutes: 60, hrValues: [] }],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByTestId('weekly-speed-chart')).toBeInTheDocument()
  })

  it('shows Cycling section when activeDiscipline is "cycle" even with no data', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      activeDiscipline: 'cycle',
      cycleRuns: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByText(/^Cycling \(0 sessions?\)/)).toBeInTheDocument()
  })
})

describe('Gym section', () => {
  it('renders Gym section heading with session count when gymRuns is non-empty', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      gymRuns: [{ _id: 'g1', date: '2026-04-07', distance: 0, duration: '45:00', pace: 0, discipline: 'gym', createdAt: '', updatedAt: '' }],
      gymWeeklyBuckets: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByText(/^Gym \(1 sessions?\)/)).toBeInTheDocument()
  })

  it('renders WeeklyDurationChart when gymRuns has data', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      gymRuns: [{ _id: 'g1', date: '2026-04-07', distance: 0, duration: '45:00', pace: 0, discipline: 'gym', createdAt: '', updatedAt: '' }],
      gymWeeklyBuckets: [{ weekKey: '2026-04-06', weekLabel: 'Apr 6', distance: 0, avgPace: null, totalDurationMinutes: 45, hrValues: [] }],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByTestId('weekly-duration-chart')).toBeInTheDocument()
  })

  it('renders WeightProgressionChart when gymRuns has data', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      gymRuns: [{ _id: 'g1', date: '2026-04-07', distance: 0, duration: '45:00', pace: 0, discipline: 'gym', exercises: [{ name: 'Squat', sets: 3, reps: 10, weight: 80 }], createdAt: '', updatedAt: '' }],
      gymWeeklyBuckets: [],
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByTestId('weight-progression-chart')).toBeInTheDocument()
  })
})

describe('All discipline empty state', () => {
  it('renders "No sessions yet" when all sections are empty and activeDiscipline is all', () => {
    mockUseDashboard.mockReturnValue(makeDefaults({
      activeDiscipline: 'all',
      runRuns: [],
      cycleRuns: [],
      gymRuns: [],
      isLoading: false,
      isPlanLoading: false,
      hasPlan: true,
    }))
    render(<MemoryRouter><Dashboard /></MemoryRouter>)
    expect(screen.getByText('No sessions yet')).toBeInTheDocument()
  })
})
```

Keep existing tests that still apply:
- "renders h1 Dashboard" — unchanged
- No active plan empty state tests — these are for `showNoPlanEmpty` which still works the same
- Loading state tests — unchanged (spinner still shown)
- Adherence card guard tests — update to work with new section structure (adherence card is now inside each section rather than top-level)
- DisciplineSelector integration tests — unchanged

For the Adherence card tests, they will find `screen.getByText('Adherence')` inside a discipline section. This should still work with the current selectors since `getByText` searches the full DOM. Keep the test assertions the same but add context about which section shows adherence when needed.

Update imports at the top: remove WeeklyVolumeChart from vi.mock list, add WeeklySpeedChart and WeeklyDurationChart mocks. Update the makeDefaults function to include all new useDashboard fields.

Also update the `formatPaceToMMSS` mock in the vi.mock('../hooks/useDashboard') block to also export `parseDurationToMinutes`, `formatTotalTime`, `computeAvgSpeed` since Dashboard.tsx now imports them:
```typescript
vi.mock('../hooks/useDashboard', () => ({
  useDashboard: vi.fn(),
  formatPaceToMMSS: (v: number) => String(v),
  parseDurationToMinutes: (d: string) => {
    const parts = d.split(':').map(Number)
    if (parts.length === 2) return parts[0] + parts[1] / 60
    if (parts.length === 3) return parts[0] * 60 + parts[1] + parts[2] / 60
    return 0
  },
  formatTotalTime: (m: number) => m < 60 ? `${Math.round(m)}m` : `${Math.floor(m/60)}h${Math.round(m%60)}m`,
  computeAvgSpeed: () => '0.0 km/h',
  filterRunsByDiscipline: (runs: any[], d: string) => d === 'all' ? runs : runs.filter((r: any) => (r.discipline ?? 'run') === d),
}))
```

**Step 3 — Update WeightProgressionChart.test.tsx:**

Read the current file. Add one new test for the defaultExercise prop:

```typescript
describe('defaultExercise prop', () => {
  it('auto-fetches and selects the defaultExercise on mount', async () => {
    const mockFetch = vi.spyOn(global, 'fetch').mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        exercise: 'Squat',
        data: [{ date: '2026-04-01', maxWeight: 80, unit: 'kg' }],
      }),
    } as Response)

    render(<WeightProgressionChart exerciseOptions={['Squat', 'Bench Press']} defaultExercise="Squat" />)

    // Should auto-fetch on mount
    await waitFor(() => {
      expect(mockFetch).toHaveBeenCalledWith(
        '/api/runs/exercise-weights?exercise=Squat',
        expect.any(Object)
      )
    })

    // Chart should show data
    await waitFor(() => {
      expect(screen.getByTestId('line-chart')).toBeInTheDocument()
    })

    mockFetch.mockRestore()
  })

  it('renders without auto-fetch when defaultExercise is not provided', () => {
    render(<WeightProgressionChart exerciseOptions={['Squat']} />)
    expect(screen.getByText('Select an exercise to see weight progression')).toBeInTheDocument()
  })
})
```

**Step 4 — Update useDashboard.test.ts:**

Read the current file. Add a new describe block at the end covering the new exports:

```typescript
describe('useDashboard per-discipline exports', () => {
  // These are pure functions already tested above via groupRunsByWeek/filterRunsByDiscipline.
  // Here we verify the filterRunsByDiscipline combinations used in the hook.

  it('filterRunsByDiscipline with run returns runs with discipline run or undefined', () => {
    const runs = [
      makeDisciplineRun('2026-04-07', 5, '40:00', 'run'),
      makeDisciplineRun('2026-04-08', 0, '45:00', 'gym'),
      makeDisciplineRun('2026-04-09', 20, '60:00', 'cycle'),
      makeDisciplineRun('2026-04-10', 5, '40:00', undefined),
    ]
    const runRuns = filterRunsByDiscipline(runs, 'run')
    const cycleRuns = filterRunsByDiscipline(runs, 'cycle')
    const gymRuns = filterRunsByDiscipline(runs, 'gym')
    expect(runRuns).toHaveLength(2)   // run + undefined
    expect(cycleRuns).toHaveLength(1)
    expect(gymRuns).toHaveLength(1)
  })
})
```

Note: `filterRunsByDiscipline` is already imported at the top of useDashboard.test.ts. The new describe block only adds coverage for the combination. Check that makeDisciplineRun helper is already defined in the file — it is, from the existing describe blocks.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - WeeklyVolumeChart.test.tsx no longer exists
    - Dashboard.test.tsx contains "Run section", "Cycling section", "Gym section" describe blocks
    - Dashboard.test.tsx mock for useDashboard includes runRuns, cycleRuns, gymRuns, runWeeklyBuckets, cycleWeeklyBuckets, gymWeeklyBuckets in makeDefaults
    - WeightProgressionChart.test.tsx contains "defaultExercise prop" describe block
    - useDashboard.test.ts contains "per-discipline exports" describe block
    - `npm test` in web/ passes (0 failing tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create WeeklySpeedChart.test.tsx + WeeklyDurationChart.test.tsx + build verification</name>
  <files>
    web/src/__tests__/WeeklySpeedChart.test.tsx
    web/src/__tests__/WeeklyDurationChart.test.tsx
  </files>
  <read_first>
    - web/src/components/dashboard/WeeklySpeedChart.tsx (read to understand props and behavior)
    - web/src/components/dashboard/WeeklyDurationChart.tsx (read to understand props and behavior)
    - web/src/__tests__/WeightProgressionChart.test.tsx (pattern reference for chart component tests)
  </read_first>
  <action>
**Part A — Create WeeklySpeedChart.test.tsx:**

```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { WeeklySpeedChart } from '../components/dashboard/WeeklySpeedChart'
import type { SpeedDataPoint } from '../components/dashboard/WeeklySpeedChart'

vi.mock('recharts', () => ({
  LineChart: ({ children }: { children: React.ReactNode }) => <div data-testid="line-chart">{children}</div>,
  Line: () => null,
  XAxis: () => null,
  YAxis: () => null,
  CartesianGrid: () => null,
  Tooltip: () => null,
  ResponsiveContainer: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}))

const twoWeeks: SpeedDataPoint[] = [
  { weekLabel: 'Apr 6', speed: 20.5 },
  { weekLabel: 'Apr 13', speed: 22.0 },
]

describe('WeeklySpeedChart', () => {
  it('renders "No cycling sessions yet" when data is empty', () => {
    render(<WeeklySpeedChart data={[]} />)
    expect(screen.getByText('No cycling sessions yet')).toBeInTheDocument()
  })

  it('renders "No cycling sessions yet" when all speed values are null', () => {
    const nullData: SpeedDataPoint[] = [
      { weekLabel: 'Apr 6', speed: null },
      { weekLabel: 'Apr 13', speed: null },
    ]
    render(<WeeklySpeedChart data={nullData} />)
    expect(screen.getByText('No cycling sessions yet')).toBeInTheDocument()
  })

  it('renders LineChart when data has at least one non-null speed value', () => {
    render(<WeeklySpeedChart data={twoWeeks} />)
    expect(screen.getByTestId('line-chart')).toBeInTheDocument()
  })

  it('renders without crashing for a single data point with speed', () => {
    render(<WeeklySpeedChart data={[{ weekLabel: 'Apr 6', speed: 18.0 }]} />)
    expect(screen.getByTestId('line-chart')).toBeInTheDocument()
  })

  it('renders without crashing for mixed null and non-null speed values', () => {
    const mixed: SpeedDataPoint[] = [
      { weekLabel: 'Apr 6', speed: null },
      { weekLabel: 'Apr 13', speed: 20.0 },
    ]
    render(<WeeklySpeedChart data={mixed} />)
    expect(screen.getByTestId('line-chart')).toBeInTheDocument()
  })
})
```

**Part B — Create WeeklyDurationChart.test.tsx:**

```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { WeeklyDurationChart } from '../components/dashboard/WeeklyDurationChart'
import type { DurationDataPoint } from '../components/dashboard/WeeklyDurationChart'

vi.mock('recharts', () => ({
  BarChart: ({ children }: { children: React.ReactNode }) => <div data-testid="bar-chart">{children}</div>,
  Bar: () => null,
  XAxis: () => null,
  YAxis: () => null,
  CartesianGrid: () => null,
  Tooltip: () => null,
  ResponsiveContainer: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}))

const twoWeeks: DurationDataPoint[] = [
  { weekLabel: 'Apr 6', durationMinutes: 90 },
  { weekLabel: 'Apr 13', durationMinutes: 60 },
]

describe('WeeklyDurationChart', () => {
  it('renders "No gym sessions yet" when data is empty', () => {
    render(<WeeklyDurationChart data={[]} />)
    expect(screen.getByText('No gym sessions yet')).toBeInTheDocument()
  })

  it('renders BarChart when data is non-empty', () => {
    render(<WeeklyDurationChart data={twoWeeks} />)
    expect(screen.getByTestId('bar-chart')).toBeInTheDocument()
  })

  it('renders without crashing for a single data point', () => {
    render(<WeeklyDurationChart data={[{ weekLabel: 'Apr 6', durationMinutes: 45 }]} />)
    expect(screen.getByTestId('bar-chart')).toBeInTheDocument()
  })

  it('renders without crashing for zero-duration weeks', () => {
    const zeroData: DurationDataPoint[] = [
      { weekLabel: 'Apr 6', durationMinutes: 0 },
    ]
    render(<WeeklyDurationChart data={zeroData} />)
    expect(screen.getByTestId('bar-chart')).toBeInTheDocument()
  })
})
```

**Part C — Run build verification:**

After creating the test files, run the full web build to confirm no TypeScript errors remain:
```bash
cd /c/dev/ai-running-coach/web && npm run build
```

If the build fails, read the error output, identify which file has the issue, and fix it before marking the task done.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npm test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests|Duration)" | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - web/src/__tests__/WeeklySpeedChart.test.tsx exists with 5 test cases
    - web/src/__tests__/WeeklyDurationChart.test.tsx exists with 4 test cases
    - All tests in web/ pass (npm test exits 0)
    - npm run build in web/ exits 0 (TypeScript + Vite build succeeds)
    - No test references WeeklyVolumeChart
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full E2E suite</name>
  <files></files>
  <action>
Run the full Playwright E2E suite to confirm no existing tests regress after the dashboard restructure.

Before running, ensure:
1. The dev API server is NOT already running on port 7071 (stop it if so — per CLAUDE.md, a running dev server causes E2E DB isolation failures)
2. Docker is running (Playwright global-setup starts MongoDB via docker compose)

Run the full suite — do NOT scope with --grep or --project filters. CLAUDE.md explicitly requires the full suite:
```bash
cd /c/dev/ai-running-coach && npx playwright test
```

If any tests fail:
- Read the failure output carefully
- Distinguish between pre-existing failures (unrelated to this phase) and new regressions caused by the dashboard restructure
- Fix any regressions introduced by this phase's changes (Dashboard restructure, WeeklyVolumeChart removal)
- Do NOT fix pre-existing failures unrelated to Phase 21 changes — document them instead
- Re-run after fixes to confirm regressions are resolved
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach && npx playwright test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx playwright test completes
    - No new test failures introduced by Phase 21 changes
    - Any pre-existing failures (unrelated to dashboard restructure) are documented in the SUMMARY
  </done>
</task>

</tasks>

<verification>
- `cd /c/dev/ai-running-coach/web && npm test` exits 0 with no failing tests
- `cd /c/dev/ai-running-coach/web && npm run build` exits 0
- `ls web/src/__tests__/WeeklyVolumeChart.test.tsx` returns "No such file or directory"
- `ls web/src/__tests__/WeeklySpeedChart.test.tsx` returns the file
- `ls web/src/__tests__/WeeklyDurationChart.test.tsx` returns the file
- `grep -r "WeeklyVolumeChart" web/src/__tests__/` returns no matches
- `cd /c/dev/ai-running-coach && npx playwright test` completes with no regressions from Phase 21 changes
</verification>

<success_criteria>
All tests pass. Build succeeds. E2E suite has no regressions from Phase 21. The test suite correctly validates the per-discipline section structure with:
1. Dashboard.test.tsx — Run/Cycling/Gym section rendering, visibility logic, stat cards per discipline
2. WeeklySpeedChart.test.tsx — empty state + chart rendering
3. WeeklyDurationChart.test.tsx — empty state + chart rendering
4. WeightProgressionChart.test.tsx — existing tests + defaultExercise auto-select
5. useDashboard.test.ts — existing tests + per-discipline filter coverage
6. WeeklyVolumeChart.test.tsx — DELETED
7. npm run build exits 0 (no TypeScript errors)
8. npx playwright test passes with no regressions introduced by this phase
</success_criteria>

<output>
After completion, create .planning/phases/21-dashboard-discipline-sections/21-03-SUMMARY.md
</output>
