---
phase: 01.2-testing-strategy
verified: 2026-03-22T23:30:00Z
status: passed
score: 8/8 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 7/8
  gaps_closed:
    - "TEST-01 through TEST-06 are now tracked in REQUIREMENTS.md traceability table"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open a PR with a failing test and confirm the merge button is blocked"
    expected: "The 'test' job appears as a required status check; merge is disabled until CI passes"
    why_human: "Branch protection rules live in GitHub repository settings, not in code"
  - test: "Merge a PR to master and observe the coverage-badges job; check badges branch is created and SVG reflects current coverage"
    expected: "coverage-badges job runs after test job succeeds; SVG appears at raw.githubusercontent.com/joacoleza/ai-running-coach/badges/badges/coverage.svg"
    why_human: "badges branch does not yet exist and the badge pipeline cannot be verified without running the workflow against live GitHub"
---

# Phase 1.2: Testing Strategy & CI — Verification Report

**Phase Goal:** Establish a comprehensive testing foundation: unit tests (API + web), E2E tests, code coverage tracking, and GitHub Actions CI running on every PR.
**Verified:** 2026-03-22T23:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 01.2-04-PLAN.md added TEST-01 through TEST-06 to REQUIREMENTS.md)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | API unit tests pass with mocked MongoDB: correct password returns null, wrong password returns 401, blocked state returns 503 | VERIFIED | `api/src/__tests__/auth.test.ts` — 88 lines, 6 tests covering all three cases with `vi.mock('mongodb')` and `vi.hoisted()` |
| 2 | Integration tests pass with real in-memory MongoDB: failureCount increments, blocked=true after 30 failures, correct password resets to 0 | VERIFIED | `api/src/__tests__/lockout.integration.test.ts` — 115 lines, 5 tests with `MongoMemoryServer`, DB assertions on `failureCount` and `blocked` |
| 3 | auth.ts exports `_resetConnectionForTest` for test isolation | VERIFIED | `api/src/middleware/auth.ts` line 23: `export function _resetConnectionForTest(): void { client = null; db = null }` |
| 4 | Web unit tests: PasswordPage and App auth gate render and behave correctly | VERIFIED | `web/src/__tests__/PasswordPage.test.tsx` (78 lines, 6 tests) and `web/src/__tests__/App.auth.test.tsx` (48 lines, 3 tests) |
| 5 | E2E tests cover correct password, wrong password, and lockout | VERIFIED | `e2e/auth.spec.ts` (46 lines, 4 browser tests) and `e2e/lockout.spec.ts` (53 lines, 2 tests with direct MongoDB seeding/assertions) |
| 6 | CI workflow runs unit + E2E tests on every PR to master and on push to master | VERIFIED | `.github/workflows/ci.yml` — `on: pull_request: branches: [master]` and `on: push: branches: [master]`; single `test` job runs `vitest run` (API), `vitest run --coverage` (web), and `playwright test` |
| 7 | CI workflow generates coverage badges on push to master only, pushed to unprotected badges branch | VERIFIED | `coverage-badges` job has `if: github.ref == 'refs/heads/master' && github.event_name == 'push'`, uses `jpb06/coverage-badges-action` with `no-commit: true`, and pushes via `git push origin HEAD:badges --force` |
| 8 | TEST-01 through TEST-06 are tracked in REQUIREMENTS.md traceability table | VERIFIED | REQUIREMENTS.md lines 16-21: Testing Infrastructure section with all 6 definitions; lines 104-109: traceability rows mapping each to Phase 1.2 Complete; coverage count updated to 33 total. 13 total TEST-0 occurrences confirmed. |

**Score:** 8/8 truths verified

---

## Required Artifacts

| Artifact | Min Lines | Actual Lines | Status | Key Evidence |
|----------|-----------|--------------|--------|-------------|
| `api/vitest.config.ts` | — | 15 | VERIFIED | `environment: 'node'`, `provider: 'v8'`, `include: ['src/**/*.test.ts']` |
| `api/src/__tests__/auth.test.ts` | 40 | 88 | VERIFIED | `vi.mock('mongodb')`, `new HttpRequest(`, `expect(result).toBeNull()`, `expect(result?.status).toBe(401)`, `expect(result?.status).toBe(503)` |
| `api/src/__tests__/lockout.integration.test.ts` | 50 | 115 | VERIFIED | `MongoMemoryServer`, `_resetConnectionForTest`, `failureCount`, `blocked`, `as unknown as` ObjectId cast |
| `web/src/__tests__/PasswordPage.test.tsx` | 40 | 78 | VERIFIED | `import { PasswordPage }`, `render(`, `Enter password`, `Wrong password`, `Service locked`, `onSuccess` |
| `web/src/__tests__/App.auth.test.tsx` | 30 | 48 | VERIFIED | `import { App }`, `AI Running Coach`, `localStorage` |
| `web/vitest.config.ts` | — | 18 | VERIFIED | `provider: 'v8'`, `reporter: ['text', 'json-summary', 'html']`, `include: ['src/**/*.{ts,tsx}']` |
| `playwright.config.ts` | — | 29 | VERIFIED | `testDir: './e2e'`, `webServer` array, `APP_PASSWORD: 'e2e-test-password'`, `http://localhost:7071/api/health`, `http://localhost:5173` |
| `e2e/auth.spec.ts` | 25 | 46 | VERIFIED | `correct password grants access`, `wrong password shows error`, `e2e-test-password` |
| `e2e/lockout.spec.ts` | 20 | 53 | VERIFIED | `MongoClient`, `failureCount`, `blocked` |
| `.github/workflows/ci.yml` | — | 92 | VERIFIED | `vitest run`, `playwright test`, `mongodb-github-action@1.12.1`, `jpb06/coverage-badges-action`, `no-commit: true`, `git push.*badges` |
| `README.md` | — | — | VERIFIED | Line 1: `![CI](https://github.com/joacoleza/ai-running-coach/actions/workflows/ci.yml/badge.svg)`, Line 2: `![Coverage](https://raw.githubusercontent.com/joacoleza/ai-running-coach/badges/badges/coverage.svg)` |
| `.planning/REQUIREMENTS.md` (TEST-xx entries) | — | 13 occurrences | VERIFIED | Lines 16-21: 6 definitions under Testing Infrastructure; lines 104-109: 6 traceability rows; line 143: last-updated footer; 33 total requirement count |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `api/src/__tests__/auth.test.ts` | `api/src/middleware/auth.ts` | `import requirePassword, checkBlocked` | WIRED | Line 26: `import { requirePassword, checkBlocked, _resetConnectionForTest } from '../middleware/auth.js'` |
| `api/src/__tests__/lockout.integration.test.ts` | `mongodb-memory-server` | `MongoMemoryServer.create()` | WIRED | Lines 2, 13: `import { MongoMemoryServer }`, `mongod = await MongoMemoryServer.create()` |
| `web/src/__tests__/PasswordPage.test.tsx` | `web/src/pages/PasswordPage.tsx` | `import { PasswordPage }` | WIRED | Line 3: `import { PasswordPage } from '../pages/PasswordPage'` |
| `web/src/__tests__/App.auth.test.tsx` | `web/src/App.tsx` | `import { App }` | WIRED | Line 3: `import { App } from '../App'` |
| `playwright.config.ts` | `api/npm run start` | `webServer command` | WIRED | Line 13: `command: 'cd api && npm run start'` |
| `playwright.config.ts` | `web/npm run dev` | `webServer command` | WIRED | Line 22: `command: 'cd web && npm run dev'` |
| `.github/workflows/ci.yml` | `api/npx vitest run` | step command | WIRED | Line 34: `run: cd api && npx vitest run` |
| `.github/workflows/ci.yml` | `npx playwright test` | step command | WIRED | Line 46: `run: npx playwright test` |
| `.github/workflows/ci.yml` | `badges branch` | `git push origin HEAD:badges` | WIRED | Line 92: `git push origin HEAD:badges --force` |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| TEST-01 | 01.2-01-PLAN.md | Unit tests for API (Azure Functions) | SATISFIED | `api/src/__tests__/auth.test.ts` — 6 unit tests with mocked MongoDB; all cases covered |
| TEST-02 | 01.2-02-PLAN.md | Unit tests for web (React components) | SATISFIED | `web/src/__tests__/PasswordPage.test.tsx` (6 tests) + `web/src/__tests__/App.auth.test.tsx` (3 tests) |
| TEST-03 | 01.2-03-PLAN.md | E2E tests covering auth flow and lockout | SATISFIED | `e2e/auth.spec.ts` (4 tests) + `e2e/lockout.spec.ts` (2 tests) |
| TEST-04 | 01.2-03-PLAN.md | GitHub Actions CI: unit + E2E on every PR | SATISFIED | `ci.yml` triggers on `pull_request: branches: [master]`; single `test` job runs all three layers |
| TEST-05 | 01.2-03-PLAN.md | Code coverage tracking with badge in README | SATISFIED | `coverage-badges` job + `jpb06/coverage-badges-action` + README badges at lines 1-2 |
| TEST-06 | 01.2-01-PLAN.md | DB assertions in tests (MongoDB state checks) | SATISFIED | `lockout.integration.test.ts` asserts on `failureCount` and `blocked` in real MongoDB; `lockout.spec.ts` asserts `failureCount === 1` via direct MongoClient |

All 6 TEST requirements are now present in REQUIREMENTS.md with definitions and traceability rows. No orphaned requirements.

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None | — | — | — |

No stubs, placeholder returns, or empty implementations found across all test files, config files, or the CI workflow. All test files assert on real behaviors. The CI workflow contains no placeholder steps.

One design note (not a blocker): In `playwright.config.ts`, `APP_PASSWORD` is hardcoded as `'e2e-test-password'` in the webServer env block. In CI, the `Run E2E tests` step sets `APP_PASSWORD: ci-test-password` at the step level, which does not propagate into the webServer subprocess. The E2E tests always use `e2e-test-password`, which matches what the tests fill in. This is consistent and intentional.

---

## Human Verification Required

### 1. Branch Protection: CI Required Status Check

**Test:** Open a PR against `master` with a failing test (e.g., break one unit test), push, and observe whether GitHub blocks the merge button until CI passes.
**Expected:** The `test` job appears as a required status check; the "Merge pull request" button is disabled until CI passes.
**Why human:** Branch protection rules are configured in GitHub repository settings, not in code. The `ci.yml` file exists and triggers correctly, but whether it is configured as a required check in GitHub UI cannot be verified from the codebase.

### 2. Coverage Badge Updates on Push to Master

**Test:** Merge a PR to `master` and observe the `coverage-badges` GitHub Actions job. Check that the `badges` branch is created/updated and the coverage badge in the README reflects the current coverage percentage.
**Expected:** The `coverage-badges` job runs after `test` job succeeds, SVG files appear in the `badges` branch under `badges/`, and the badge URL `https://raw.githubusercontent.com/joacoleza/ai-running-coach/badges/badges/coverage.svg` serves the updated SVG.
**Why human:** The `badges` branch does not yet exist (this is the first push to master with this CI workflow). The badge pipeline cannot be verified without running the workflow against the live GitHub repository.

---

## Gap Closure Summary

The single gap from initial verification has been closed:

**Gap (initial):** TEST-01 through TEST-06 were absent from `.planning/REQUIREMENTS.md`. The central requirements registry was never updated when Phase 1.2 was inserted.

**Closure (plan 01.2-04):** REQUIREMENTS.md now contains a "Testing Infrastructure" section (lines 14-22) with all 6 TEST requirement definitions marked complete, and 6 traceability rows (lines 104-109) mapping each to Phase 1.2 with status Complete. The coverage count was updated from 27 to 33. The last-updated footer records the change date.

**Verified closure:** `grep -c "TEST-0" .planning/REQUIREMENTS.md` returns 13 (6 definitions + 6 traceability rows + 1 footer reference). All other previously-verified artifacts and key links remain intact — no regressions detected.

The full testing pyramid is complete and all requirements are traceable:
- 11 API tests (6 unit + 5 integration) with mocked and real MongoDB
- 9 web unit tests covering auth flow and UI behavior
- 6 E2E tests with browser automation and direct MongoDB state assertions
- CI workflow covering all three layers on PRs and pushes to master
- Coverage badge pipeline pushing SVGs to unprotected badges branch
- All 6 TEST requirements registered and traceable in REQUIREMENTS.md

---

_Verified: 2026-03-22T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
