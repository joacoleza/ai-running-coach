---
phase: 01.2-testing-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/vitest.config.ts
  - web/src/__tests__/PasswordPage.test.tsx
  - web/src/__tests__/App.auth.test.tsx
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "PasswordPage renders a password input and submit button"
    - "PasswordPage calls onSuccess after successful fetch to /api/health"
    - "PasswordPage shows 'Wrong password' error on 401 response"
    - "PasswordPage shows 'Service locked' error on 503 response"
    - "App renders PasswordPage when localStorage has no app_password"
    - "App renders AppShell when localStorage has app_password"
    - "App clears auth and shows PasswordPage on 401 fetch response"
  artifacts:
    - path: "web/src/__tests__/PasswordPage.test.tsx"
      provides: "Unit tests for PasswordPage component"
      min_lines: 40
    - path: "web/src/__tests__/App.auth.test.tsx"
      provides: "Unit tests for App auth gate and 401 interceptor"
      min_lines: 30
  key_links:
    - from: "web/src/__tests__/PasswordPage.test.tsx"
      to: "web/src/pages/PasswordPage.tsx"
      via: "import { PasswordPage }"
      pattern: "import.*PasswordPage"
    - from: "web/src/__tests__/App.auth.test.tsx"
      to: "web/src/App.tsx"
      via: "import { App }"
      pattern: "import.*App"
---

<objective>
Add coverage configuration to the web Vitest setup and write unit tests for PasswordPage and the App auth gate.

Purpose: Validates the frontend auth flow: password form behavior, localStorage integration, 401 interceptor, and conditional rendering based on auth state.

Output: Updated `web/vitest.config.ts` with coverage, `web/src/__tests__/PasswordPage.test.tsx`, `web/src/__tests__/App.auth.test.tsx`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-testing-strategy/01.2-RESEARCH.md

<interfaces>
<!-- From web/src/pages/PasswordPage.tsx -->
interface PasswordPageProps { onSuccess: () => void }
export function PasswordPage({ onSuccess }: PasswordPageProps)
  - Renders: form with type="password" input, submit button ("Unlock")
  - On submit: fetches /api/health with X-App-Password header
  - On 200: localStorage.setItem('app_password', password), calls onSuccess()
  - On 401: shows "Wrong password" error
  - On 503: shows "Service locked. Contact administrator." error

<!-- From web/src/App.tsx -->
export function App()
  - State: authenticated = !!localStorage.getItem('app_password')
  - If !authenticated: renders <PasswordPage onSuccess={...} />
  - If authenticated: renders <BrowserRouter><AppShell>...</AppShell></BrowserRouter>
  - useEffect: overrides window.fetch to intercept 401 -> clears localStorage, setAuthenticated(false)

<!-- Existing test setup: web/src/test/setup.ts imports @testing-library/jest-dom -->
<!-- Already installed: @testing-library/react, @testing-library/jest-dom, jsdom, vitest -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage config to web vitest and install @vitest/coverage-v8</name>
  <files>web/package.json, web/vitest.config.ts</files>
  <read_first>
    - web/vitest.config.ts (current config — need to add coverage section)
    - web/package.json (current deps — need to add @vitest/coverage-v8)
  </read_first>
  <action>
    1. **Install coverage dep**:
       Run: `cd web && npm install -D @vitest/coverage-v8@^3.2.4`
       (Match vitest version ^3.2.4 already in web/package.json)

    2. **Update `web/vitest.config.ts`** to add coverage section:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import react from '@vitejs/plugin-react'

       export default defineConfig({
         plugins: [react()],
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: ['./src/test/setup.ts'],
           passWithNoTests: true,
           coverage: {
             provider: 'v8',
             reporter: ['text', 'json-summary', 'html'],
             include: ['src/**/*.{ts,tsx}'],
             exclude: ['src/test/**', 'src/**/*.test.*', 'src/main.tsx'],
           },
         },
       })
       ```

    3. **Add coverage script to `web/package.json`**:
       Add to "scripts": `"test:coverage": "vitest run --coverage"`
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npx vitest run --coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains `"@vitest/coverage-v8"` in devDependencies
    - web/package.json scripts contains `"test:coverage": "vitest run --coverage"`
    - web/vitest.config.ts contains `provider: 'v8'`
    - web/vitest.config.ts contains `reporter: ['text', 'json-summary', 'html']`
    - web/vitest.config.ts contains `include: ['src/**/*.{ts,tsx}']`
    - `cd web && npx vitest run --coverage` exits 0
  </acceptance_criteria>
  <done>Web vitest has coverage configured with v8 provider, json-summary reporter (needed for badge generation), and html reporter for local viewing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write PasswordPage and App auth gate unit tests</name>
  <files>web/src/__tests__/PasswordPage.test.tsx, web/src/__tests__/App.auth.test.tsx</files>
  <read_first>
    - web/src/pages/PasswordPage.tsx (exact component: form, fetch /api/health, localStorage.setItem, error states)
    - web/src/App.tsx (auth gate: useState with localStorage check, useEffect 401 interceptor, conditional PasswordPage vs BrowserRouter)
    - web/src/test/setup.ts (imports @testing-library/jest-dom — matchers like toBeInTheDocument available)
    - web/vitest.config.ts (jsdom environment, globals: true)
  </read_first>
  <behavior>
    PasswordPage.test.tsx:
    - Test 1: Renders password input with placeholder "Enter password"
    - Test 2: Renders "Unlock" submit button
    - Test 3: Submit button is disabled when input is empty
    - Test 4: On submit with correct password (mock fetch returns 200): calls onSuccess, sets localStorage
    - Test 5: On submit with wrong password (mock fetch returns 401): shows "Wrong password" text
    - Test 6: On submit with locked service (mock fetch returns 503): shows "Service locked. Contact administrator."

    App.auth.test.tsx:
    - Test 7: When localStorage has no app_password, renders PasswordPage (look for "AI Running Coach" heading)
    - Test 8: When localStorage has app_password, renders AppShell content (look for navigation links like "Dashboard")
    - Test 9: After 401 response from fetch, clears localStorage and shows PasswordPage
  </behavior>
  <action>
    **Create `web/src/__tests__/PasswordPage.test.tsx`**:

    ```typescript
    import { render, screen, fireEvent, waitFor } from '@testing-library/react'
    import { describe, it, expect, vi, beforeEach } from 'vitest'
    import { PasswordPage } from '../pages/PasswordPage'
    ```

    Mock `window.fetch` using `vi.fn()` — NOT `vi.spyOn(global, 'fetch')`. Set `global.fetch = vi.fn()` in beforeEach.

    For localStorage: use `vi.stubGlobal('localStorage', { getItem: vi.fn(), setItem: vi.fn(), removeItem: vi.fn() })` OR use the real jsdom localStorage and call `localStorage.clear()` in beforeEach.

    For Test 4 (success flow):
    - Mock fetch to return `{ ok: true, status: 200 }`
    - Fire change on password input, then submit the form
    - `await waitFor(() => expect(onSuccess).toHaveBeenCalled())`
    - Verify `localStorage.setItem` was called with `'app_password'`

    For Test 5 (401 error):
    - Mock fetch to return `{ ok: false, status: 401 }`
    - Submit form
    - `await waitFor(() => expect(screen.getByText('Wrong password')).toBeInTheDocument())`

    For Test 6 (503 error):
    - Mock fetch to return `{ ok: false, status: 503 }`
    - Submit form
    - `await waitFor(() => expect(screen.getByText('Service locked. Contact administrator.')).toBeInTheDocument())`

    **Create `web/src/__tests__/App.auth.test.tsx`**:

    ```typescript
    import { render, screen } from '@testing-library/react'
    import { describe, it, expect, beforeEach } from 'vitest'
    import { App } from '../App'
    ```

    For Test 7 (no auth):
    - `localStorage.clear()` in beforeEach
    - Render `<App />`
    - `expect(screen.getByText('AI Running Coach')).toBeInTheDocument()` (PasswordPage heading)

    For Test 8 (authenticated):
    - `localStorage.setItem('app_password', 'test')`
    - Render `<App />`
    - `expect(screen.getByText('Dashboard')).toBeInTheDocument()` (nav link in AppShell/Sidebar)

    For Test 9 (401 interceptor):
    - `localStorage.setItem('app_password', 'test')`
    - Render `<App />`
    - Trigger a 401: call `window.fetch('/api/test')` where fetch is mocked to return 401
    - `await waitFor(() => expect(screen.getByText('AI Running Coach')).toBeInTheDocument())`
    - Verify `localStorage.getItem('app_password')` is null

    NOTE: The App component overrides `window.fetch` in a useEffect. For Test 9, after rendering App, the real fetch override is active. Mock the underlying `originalFetch` by setting up the mock BEFORE rendering, or mock at a lower level. The simplest approach: mock `global.fetch` to return 401 for any URL, render App (which wraps it), then call fetch from inside a component or via `act()`.

    If the BrowserRouter in App causes issues in tests (no router context needed for auth gate since PasswordPage renders before the router), the tests for the unauthenticated state should work fine since `<PasswordPage>` renders before `<BrowserRouter>`.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npx vitest run 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - web/src/__tests__/PasswordPage.test.tsx exists
    - web/src/__tests__/PasswordPage.test.tsx contains `import { PasswordPage }`
    - web/src/__tests__/PasswordPage.test.tsx contains `render(`
    - web/src/__tests__/PasswordPage.test.tsx contains `Enter password` (placeholder check)
    - web/src/__tests__/PasswordPage.test.tsx contains `Wrong password` (401 error text)
    - web/src/__tests__/PasswordPage.test.tsx contains `Service locked` (503 error text)
    - web/src/__tests__/PasswordPage.test.tsx contains `onSuccess`
    - web/src/__tests__/App.auth.test.tsx exists
    - web/src/__tests__/App.auth.test.tsx contains `import { App }` or `import App`
    - web/src/__tests__/App.auth.test.tsx contains `AI Running Coach` (PasswordPage heading)
    - web/src/__tests__/App.auth.test.tsx contains `localStorage`
    - `cd web && npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>PasswordPage tests cover form rendering, successful login, 401 error, and 503 error. App auth tests cover unauthenticated gate, authenticated passthrough, and 401 interceptor. All pass in jsdom environment.</done>
</task>

</tasks>

<verification>
- `cd web && npx vitest run` passes all tests (0 failures)
- `cd web && npx vitest run --coverage` produces coverage output and exits 0
- `ls web/coverage/coverage-summary.json` confirms json-summary output exists after coverage run
- `grep "PasswordPage" web/src/__tests__/PasswordPage.test.tsx` confirms component under test
- `grep "App" web/src/__tests__/App.auth.test.tsx` confirms component under test
</verification>

<success_criteria>
- Coverage configured with v8 provider and json-summary reporter
- PasswordPage tests validate form rendering, success flow, 401 error, 503 error
- App auth gate tests validate conditional rendering and 401 interceptor
- All web tests pass
- Coverage report generates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-testing-strategy/01.2-02-SUMMARY.md`
</output>
