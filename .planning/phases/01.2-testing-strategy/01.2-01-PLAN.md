---
phase: 01.2-testing-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/middleware/auth.ts
  - api/vitest.config.ts
  - api/package.json
  - api/src/__tests__/auth.test.ts
  - api/src/__tests__/lockout.integration.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-06
must_haves:
  truths:
    - "API unit tests pass with mocked MongoDB: correct password returns null, wrong password returns 401, blocked state returns 503"
    - "Integration tests pass with real in-memory MongoDB: failureCount increments on wrong password, blocked=true after 30 failures, correct password resets failureCount to 0"
    - "auth.ts exports _resetConnectionForTest for test isolation"
  artifacts:
    - path: "api/vitest.config.ts"
      provides: "Vitest config for API tests in Node environment"
      contains: "environment: 'node'"
    - path: "api/src/__tests__/auth.test.ts"
      provides: "Unit tests for requirePassword and checkBlocked with mocked MongoDB"
      min_lines: 40
    - path: "api/src/__tests__/lockout.integration.test.ts"
      provides: "Integration tests asserting MongoDB state via mongodb-memory-server"
      min_lines: 50
  key_links:
    - from: "api/src/__tests__/auth.test.ts"
      to: "api/src/middleware/auth.ts"
      via: "import requirePassword, checkBlocked"
      pattern: "import.*requirePassword.*from.*middleware/auth"
    - from: "api/src/__tests__/lockout.integration.test.ts"
      to: "mongodb-memory-server"
      via: "MongoMemoryServer.create()"
      pattern: "MongoMemoryServer"
---

<objective>
Set up Vitest for the API project and write unit tests for the auth middleware (mocked MongoDB) plus integration tests for the lockout algorithm (real in-memory MongoDB with DB assertions).

Purpose: Validates the auth middleware and lockout logic that protects every API endpoint. Integration tests assert on actual MongoDB document state, not just return values.

Output: `api/vitest.config.ts`, `api/src/__tests__/auth.test.ts`, `api/src/__tests__/lockout.integration.test.ts`, updated `api/package.json` with test deps, updated `api/src/middleware/auth.ts` with `_resetConnectionForTest`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-testing-strategy/01.2-RESEARCH.md

<interfaces>
<!-- Source of truth: api/src/middleware/auth.ts -->
export async function checkBlocked(): Promise<boolean>
export async function requirePassword(req: HttpRequest): Promise<HttpResponseInit | null>

<!-- Module-level singleton in auth.ts -->
let client: MongoClient | null = null
let db: Db | null = null
async function getDb(): Promise<Db>  // private, reads process.env.MONGODB_CONNECTION_STRING

<!-- MongoDB document shape in auth collection -->
{ _id: 'lockout', failureCount: number, blocked: boolean }

<!-- API tsconfig: module=CommonJS, moduleResolution=Node -->
<!-- Web vitest version: ^3.2.4 — API should match for consistency -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install API test deps, create vitest config, add _resetConnectionForTest to auth.ts</name>
  <files>api/package.json, api/vitest.config.ts, api/src/middleware/auth.ts, api/tsconfig.json</files>
  <read_first>
    - api/package.json (current deps — need to add vitest, @vitest/coverage-v8, mongodb-memory-server as devDeps)
    - api/src/middleware/auth.ts (module-level singleton: client/db variables — need to export reset function)
    - api/tsconfig.json (module=CommonJS, moduleResolution=Node — vitest config must work with this)
    - web/vitest.config.ts (reference for consistent vitest config style)
  </read_first>
  <action>
    1. **Install devDependencies in api/**:
       Run: `cd api && npm install -D vitest@^3.2.4 @vitest/coverage-v8@^3.2.4 mongodb-memory-server@^11.0.1`
       (Match vitest major version with web/ for consistency per RESEARCH open question 1)

    2. **Create `api/vitest.config.ts`**:
       ```typescript
       import { defineConfig } from 'vitest/config'

       export default defineConfig({
         test: {
           globals: true,
           environment: 'node',
           include: ['src/**/*.test.ts'],
           coverage: {
             provider: 'v8',
             reporter: ['text', 'json-summary'],
             include: ['src/**/*.ts'],
             exclude: ['src/**/*.test.ts'],
           },
         },
       })
       ```

    3. **Add test scripts to `api/package.json`**:
       Add to "scripts":
       - `"test": "vitest run"`
       - `"test:coverage": "vitest run --coverage"`

    4. **Update `api/tsconfig.json`** — add `"src/**/*.test.ts"` to `exclude` array so test files are not compiled to dist:
       Change `"exclude": ["node_modules", "dist"]` to `"exclude": ["node_modules", "dist", "src/**/*.test.ts"]`

    5. **Add `_resetConnectionForTest` to `api/src/middleware/auth.ts`**:
       After the existing `getDb()` function, add:
       ```typescript
       export function _resetConnectionForTest(): void {
         client = null
         db = null
       }
       ```
       This allows integration tests to reset the module-level singleton between tests so each test can set its own MONGODB_CONNECTION_STRING pointing to a fresh MongoMemoryServer instance.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/api && npx tsc --noEmit && npx vitest run --passWithNoTests 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - api/vitest.config.ts exists and contains `environment: 'node'`
    - api/vitest.config.ts contains `include: ['src/**/*.test.ts']`
    - api/vitest.config.ts contains `provider: 'v8'`
    - api/package.json contains `"vitest"` in devDependencies
    - api/package.json contains `"mongodb-memory-server"` in devDependencies
    - api/package.json contains `"@vitest/coverage-v8"` in devDependencies
    - api/package.json scripts contains `"test": "vitest run"`
    - api/package.json scripts contains `"test:coverage": "vitest run --coverage"`
    - api/src/middleware/auth.ts contains `export function _resetConnectionForTest`
    - api/src/middleware/auth.ts contains `client = null` inside _resetConnectionForTest
    - api/tsconfig.json exclude array contains `src/**/*.test.ts`
    - `cd api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>API has Vitest configured in Node environment with coverage support, test deps installed, auth.ts exports _resetConnectionForTest for test isolation, test files excluded from tsc compilation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write auth unit tests (mocked MongoDB) and lockout integration tests (mongodb-memory-server)</name>
  <files>api/src/__tests__/auth.test.ts, api/src/__tests__/lockout.integration.test.ts</files>
  <read_first>
    - api/src/middleware/auth.ts (exact function signatures, MongoDB operations, _id cast pattern, MAX_FAILURES=30)
    - api/vitest.config.ts (just created — confirms test file pattern)
    - .planning/phases/01.2-testing-strategy/01.2-RESEARCH.md (Pattern 1: Azure Functions v4 unit test, Pattern 2: MongoDB integration test, Pitfall 1: module singleton, Pitfall 2: _id type cast)
  </read_first>
  <behavior>
    auth.test.ts (unit tests with vi.mock('mongodb')):
    - Test 1: requirePassword returns null when password matches process.env.APP_PASSWORD
    - Test 2: requirePassword returns { status: 401 } when password is wrong
    - Test 3: requirePassword returns { status: 401 } when x-app-password header is missing
    - Test 4: requirePassword returns { status: 503 } when checkBlocked finds blocked=true
    - Test 5: checkBlocked returns false when no lockout document exists (findOne returns null)
    - Test 6: checkBlocked returns true when lockout document has blocked=true

    lockout.integration.test.ts (real MongoDB via mongodb-memory-server):
    - Test 7: Wrong password increments failureCount to 1 in auth collection (DB assertion)
    - Test 8: 3 wrong passwords set failureCount to 3 in auth collection (DB assertion)
    - Test 9: 30 wrong passwords set blocked=true in auth collection (DB assertion)
    - Test 10: Correct password resets failureCount to 0 and blocked to false in auth collection (DB assertion)
    - Test 11: When blocked=true, even correct password returns 503 (blocked check runs first)
  </behavior>
  <action>
    **Create `api/src/__tests__/auth.test.ts`**:

    Use `vi.mock('mongodb', ...)` to mock MongoClient at the module level. The mock must return a chain: `MongoClient() -> connect() -> db('running-coach') -> collection('auth') -> findOne/findOneAndUpdate/updateOne`.

    Import `HttpRequest` from `@azure/functions` and construct real requests:
    ```typescript
    new HttpRequest({
      method: 'GET',
      url: 'http://localhost/api/test',
      headers: password ? { 'x-app-password': password } : {},
    })
    ```

    Set `process.env.APP_PASSWORD = 'test-secret'` in `beforeEach`.
    Call `vi.resetModules()` and `vi.clearAllMocks()` in `beforeEach`.
    After `vi.resetModules()`, dynamically import `requirePassword` and `checkBlocked`:
    ```typescript
    const { requirePassword, checkBlocked } = await import('../middleware/auth.js')
    ```

    For each test, configure the mock's `findOne` return value to simulate blocked/unblocked state, and verify the returned `HttpResponseInit` status.

    **Create `api/src/__tests__/lockout.integration.test.ts`**:

    Use `mongodb-memory-server` to start a real in-memory MongoDB. Do NOT mock mongodb.

    ```typescript
    import { MongoMemoryServer } from 'mongodb-memory-server'
    import { MongoClient, ObjectId } from 'mongodb'
    ```

    In `beforeAll`: `mongod = await MongoMemoryServer.create()`, get URI, set `process.env.MONGODB_CONNECTION_STRING` and `process.env.APP_PASSWORD = 'correct-pw'`.

    In `beforeEach`: Connect to mongod, drop the `auth` collection, close the assertion client. Call the imported `_resetConnectionForTest()` to reset the module singleton so it reconnects with the current URI.

    After each `requirePassword` call, connect a separate `MongoClient` to the same URI to assert on the `auth` collection's `lockout` document:
    ```typescript
    const assertClient = new MongoClient(uri)
    await assertClient.connect()
    const doc = await assertClient.db('running-coach').collection('auth').findOne({ _id: 'lockout' as unknown as ObjectId })
    expect(doc?.failureCount).toBe(expectedCount)
    await assertClient.close()
    ```

    For Test 9 (30 failures), use a loop calling requirePassword 30 times with wrong password, then assert `doc.blocked === true`.

    For Test 11 (blocked + correct password), manually insert `{ _id: 'lockout', blocked: true, failureCount: 30 }` into the auth collection before calling requirePassword with the correct password, and assert status 503.

    Use dynamic import after `vi.resetModules()` in `beforeEach` for module isolation:
    ```typescript
    let requirePassword: typeof import('../middleware/auth.js').requirePassword
    let _resetConnectionForTest: typeof import('../middleware/auth.js')._resetConnectionForTest
    ```

    Set timeout on the integration test file: `describe('lockout integration', () => { ... })` or use `vitest.config.ts` `testTimeout` — the 30-iteration test may take a few seconds. Add `{ timeout: 30_000 }` to the describe block.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/api && npx vitest run 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - api/src/__tests__/auth.test.ts exists and contains `vi.mock('mongodb'`
    - api/src/__tests__/auth.test.ts contains `new HttpRequest(`
    - api/src/__tests__/auth.test.ts contains `requirePassword`
    - api/src/__tests__/auth.test.ts contains `checkBlocked`
    - api/src/__tests__/auth.test.ts contains `expect(result).toBeNull()` (correct password test)
    - api/src/__tests__/auth.test.ts contains `expect(result?.status).toBe(401)` (wrong password test)
    - api/src/__tests__/auth.test.ts contains `expect(result?.status).toBe(503)` (blocked test)
    - api/src/__tests__/lockout.integration.test.ts exists and contains `MongoMemoryServer`
    - api/src/__tests__/lockout.integration.test.ts contains `_resetConnectionForTest`
    - api/src/__tests__/lockout.integration.test.ts contains `failureCount` (DB assertion)
    - api/src/__tests__/lockout.integration.test.ts contains `blocked` (DB assertion)
    - api/src/__tests__/lockout.integration.test.ts contains `as unknown as` (ObjectId cast pattern matching auth.ts)
    - `cd api && npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All auth unit tests pass (6 tests with mocked MongoDB). All lockout integration tests pass (5 tests with real in-memory MongoDB asserting on document state). Tests cover correct password, wrong password, missing header, blocked state, failure counting, and lockout threshold.</done>
</task>

</tasks>

<verification>
- `cd api && npx vitest run` passes all tests (0 failures)
- `cd api && npx tsc --noEmit` compiles cleanly
- `grep "_resetConnectionForTest" api/src/middleware/auth.ts` finds the exported function
- `grep "MongoMemoryServer" api/src/__tests__/lockout.integration.test.ts` confirms real DB usage
- `grep "vi.mock" api/src/__tests__/auth.test.ts` confirms mock-based unit tests
</verification>

<success_criteria>
- Vitest configured for API in Node environment with coverage support
- Auth unit tests pass with mocked MongoDB (requirePassword + checkBlocked)
- Lockout integration tests pass with real in-memory MongoDB (DB state assertions on failureCount and blocked)
- auth.ts exports _resetConnectionForTest for test isolation
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-testing-strategy/01.2-01-SUMMARY.md`
</output>
