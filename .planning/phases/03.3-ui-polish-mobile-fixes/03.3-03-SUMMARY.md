---
plan: 03.3-03
phase: 03.3-ui-polish-mobile-fixes
status: complete
started: 2026-04-06
completed: 2026-04-06
---

# Plan 03.3-03: Visual Polish

## What Was Built

- **Favicon** — `web/public/favicon.svg`: running shoe SVG on blue (#1d4ed8) rounded background, visible in browser tab
- **theme-color meta tag** — `web/index.html`: `<meta name="theme-color" content="#1d4ed8">` + `apple-mobile-web-app-status-bar-style` so Safari address bar blends with app
- **Removed Update Plan button** — `PlanActions.tsx` simplified to a single compact Archive button; `onUpdate` prop removed entirely
- **Inline Archive button** — `TrainingPlan.tsx` now wraps the `<h1>` and `<PlanActions>` in a flex row so Archive sits next to the heading, small and unobtrusive
- **PlanActions tests updated** — test file rewritten to match new single-button component

## Key Files

- `web/public/favicon.svg` — running shoe icon
- `web/index.html` — favicon link updated, theme-color and apple meta tags added
- `web/src/components/plan/PlanActions.tsx` — removed Update Plan, compact Archive only
- `web/src/pages/TrainingPlan.tsx` — flex row title + inline PlanActions, removed handleUpdate
- `web/src/__tests__/PlanActions.test.tsx` — updated to reflect new component API

## Self-Check: PASSED

- [x] favicon.svg exists with `<svg` and `fill="#1d4ed8"`
- [x] index.html contains `href="/favicon.svg"` and does NOT contain `vite.svg`
- [x] index.html contains `name="theme-color"` with `content="#1d4ed8"`
- [x] PlanActions does NOT contain "Update Plan" text or `onUpdate` prop
- [x] TrainingPlan does NOT call `handleUpdate` or pass `onUpdate`
- [x] `npm run build` passes with 0 TypeScript errors
