---
plan: 03.3-04
phase: 03.3-ui-polish-mobile-fixes
status: complete
started: 2026-04-06
completed: 2026-04-06
---

# Plan 03.3-04: Coach Feedback Panel + DayRow Log Run

## What Was Built

- **"Get plan feedback" moved to Coach Feedback panel header** — removed from inside the blue objective box; now lives in the panel header as a small underline button
- **Conditional button label** — shows "Get plan feedback" when no feedback exists, "Refresh feedback" after first use
- **Panel non-expandable when empty** — when `plan.progressFeedback` is falsy, only the header row renders (no phantom collapse toggle)
- **Paragraph spacing** — feedback content split on `\n\n` and rendered as `<p>` elements inside `space-y-3` div (removed ReactMarkdown dependency for this section)
- **"Log run" on completed days without linked run** — DayRow shows "Log run" button for `day.completed && !linkedRun` cases, opens RunEntryForm with weekNumber/dayLabel pre-filled
- **Undo button confirmed consistent** — already in the correct position before the `!isReadOnly` block; no change needed
- **Removed unused ReactMarkdown import** from TrainingPlan.tsx (eliminated TS6133 error)

## Key Files

- `web/src/pages/TrainingPlan.tsx` — redesigned Coach Feedback panel with header button
- `web/src/components/plan/DayRow.tsx` — added "Log run" for completed days without linked run

## Self-Check: PASSED

- [x] TrainingPlan contains `Refresh feedback` text
- [x] TrainingPlan contains `Get plan feedback` in the Coach Feedback panel header
- [x] TrainingPlan does NOT have `Get plan feedback` inside `bg-blue-50` objective block
- [x] TrainingPlan contains `split('\n\n')` for paragraph rendering
- [x] Panel expand toggle only renders when `plan.progressFeedback` is truthy
- [x] DayRow contains `day.completed && !linkedRun && !readonly` guard for "Log run"
- [x] `npm run build` passes with 0 TypeScript errors
