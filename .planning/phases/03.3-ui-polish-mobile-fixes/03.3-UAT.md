---
status: complete
phase: 03.3-ui-polish-mobile-fixes
source: [03.3-01-SUMMARY.md, 03.3-02-SUMMARY.md, 03.3-03-SUMMARY.md, 03.3-04-SUMMARY.md]
started: 2026-04-06T00:00:00Z
updated: 2026-04-06T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Auto-scroll to last completed day
expected: Open the Training Plan page when you have a plan with some completed days. The page should automatically scroll so the last completed day is centered in view — no manual scrolling needed.
result: pass

### 2. Clickable run date opens RunDetailModal
expected: On a plan day that has a linked run, click the run date shown in the day row. A RunDetailModal should open showing the full details of that run.
result: pass

### 3. Plan badge in RunDetailModal navigates to day row
expected: Inside RunDetailModal for a linked run, click the plan badge (e.g. "Week 2 · Day B"). The modal closes, you're taken to /plan, and the page scrolls to that specific day row.
result: pass

### 4. CoachPanel scrolls to latest message on mobile open
expected: On a mobile-sized screen (or narrow browser), open the Coach panel via the FAB button. The chat history should already be scrolled to the bottom (showing the most recent message) without any manual scrolling.
result: pass

### 5. Unlink run from plan (two-step confirm)
expected: Open RunDetailModal for a linked run. An "Unlink from plan" button is visible. Clicking it changes to a confirm/cancel prompt. Confirming removes the link — the run no longer shows plan info, and the plan day reverts to incomplete.
result: pass

### 6. Notes textarea larger (rows=4)
expected: Open RunDetailModal or the Log Run form. The Notes field should be noticeably taller than before — 4 rows instead of 2.
result: pass

### 7. Favicon visible in browser tab
expected: The browser tab for the app shows a running shoe icon (blue background) instead of the default Vite favicon.
result: pass

### 8. "Update Plan" button removed, Archive inline with heading
expected: On the Training Plan page, there is no "Update Plan" button. The Archive button appears as a small, compact button next to the plan heading — not as a large standalone button below it.
result: pass

### 9. Get plan feedback in Coach Feedback panel header
expected: The "Get plan feedback" button is in the header of the Coach Feedback panel, not inside the blue objective box. If no feedback exists yet, the panel shows only the header row (no phantom empty expand area). After getting feedback the button label changes to "Refresh feedback".
result: pass

### 10. Feedback text rendered with paragraph spacing
expected: After getting plan feedback, multi-paragraph feedback text is rendered as properly spaced paragraphs — not as a wall of text.
result: pass

### 11. "Log run" button on completed day without linked run
expected: For a plan day marked as completed but with no linked run, a "Log run" button appears in the day row. Clicking it opens the Run Entry form pre-filled with the week number and day label.
result: pass

### 12. Plan badge color reflects plan status
expected: In the Runs list and RunDetailModal, the plan badge (e.g. "Week 1 · Day A") is green when the linked plan is the active plan, and gray when the linked plan has been archived.
result: pass

### 13. Undo button position on completed linked days
expected: On a completed day that also has a linked run, the Undo button appears next to the day guidelines — same position as on completed days without a linked run. It should NOT be below the run date.
result: pass

### 14. Run modal layout consistency (create vs view)
expected: The Log Run (create) modal and View Run (RunDetailModal) share the same field layout: Date and Distance in one row, Duration and Avg HR in the next row. The view modal currently shows each field on its own row instead.
result: pass

## Summary

total: 14
passed: 14
issues: 0
pending: 0
skipped: 0

## Gaps

[none — tests 12, 13, 14 all passed; gaps section below was stale from mid-session capture before fixes were applied]
