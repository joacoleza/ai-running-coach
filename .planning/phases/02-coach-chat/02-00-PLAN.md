---
phase: 02-coach-chat
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - api/src/__tests__/chat.test.ts
  - api/src/__tests__/chat.integration.test.ts
  - api/src/__tests__/plan.test.ts
  - api/src/__tests__/sessions.test.ts
  - api/src/__tests__/onboarding.test.ts
  - web/src/__tests__/PlanCalendar.test.tsx
autonomous: true
requirements:
  - COACH-01
  - COACH-06
  - PLAN-01
  - PLAN-02
  - PLAN-04

must_haves:
  truths:
    - "All 6 test stub files exist and can be run without errors"
    - "Each stub has at least one describe block with a placeholder test"
  artifacts:
    - path: "api/src/__tests__/chat.test.ts"
      provides: "Test stubs for COACH-06 rolling window and message storage"
      contains: "describe"
    - path: "api/src/__tests__/chat.integration.test.ts"
      provides: "Integration test stubs for COACH-01 with MongoDB"
      contains: "describe"
    - path: "api/src/__tests__/plan.test.ts"
      provides: "Test stubs for PLAN-01 JSON extraction, PLAN-02 schema, D-15 error"
      contains: "describe"
    - path: "api/src/__tests__/sessions.test.ts"
      provides: "Test stubs for PLAN-04 PATCH endpoint"
      contains: "describe"
    - path: "api/src/__tests__/onboarding.test.ts"
      provides: "Test stubs for D-03 onboarding resume logic"
      contains: "describe"
    - path: "web/src/__tests__/PlanCalendar.test.tsx"
      provides: "Test stubs for PLAN-03 calendar rendering"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 2 test requirements identified in RESEARCH.md Validation Architecture.

Purpose: These stubs define the test structure before implementation begins. Each stub contains describe blocks and placeholder (skipped or todo) tests so the test runner can discover them. Implementation plans will fill in the actual test logic as they build the corresponding production code.
Output: 6 test stub files that run green (all tests skipped/todo).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-coach-chat/02-RESEARCH.md (Validation Architecture section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API test stubs</name>
  <files>api/src/__tests__/chat.test.ts, api/src/__tests__/chat.integration.test.ts, api/src/__tests__/plan.test.ts, api/src/__tests__/sessions.test.ts, api/src/__tests__/onboarding.test.ts</files>
  <read_first>
    - api/vitest.config.ts (test config to understand test discovery patterns)
    - api/src/__tests__/ (existing test files for pattern reference)
    - .planning/phases/02-coach-chat/02-RESEARCH.md (Validation Architecture - Wave 0 Gaps)
  </read_first>
  <action>
Create 5 API test stub files in `api/src/__tests__/`. Each file should follow the existing test file patterns (import `describe`, `it`, `expect` from `vitest`). Use `it.todo()` for placeholder tests.

**api/src/__tests__/chat.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Chat - Rolling 20-message window (COACH-06)', () => {
  it.todo('buildContextMessages returns last 20 messages sorted by timestamp');
  it.todo('buildContextMessages returns fewer than 20 when fewer exist');
  it.todo('buildContextMessages returns empty array when no messages exist');
});

describe('Chat - Summary generation (COACH-06)', () => {
  it.todo('maybeSummarize does nothing when count < 25');
  it.todo('maybeSummarize generates summary when count >= 25');
  it.todo('summary is prepended to system prompt when available');
});
```

**api/src/__tests__/chat.integration.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Chat Integration (COACH-01)', () => {
  it.todo('POST /api/chat saves user message to messages collection');
  it.todo('POST /api/chat saves assistant message after stream completes');
  it.todo('POST /api/chat returns 400 when planId is missing');
  it.todo('POST /api/chat returns 401 when password is wrong');
});
```

**api/src/__tests__/plan.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Plan Generation - JSON extraction (PLAN-01)', () => {
  it.todo('extracts JSON from <training_plan> XML tags');
  it.todo('returns 400 when no <training_plan> tags found');
  it.todo('returns 400 on malformed JSON inside tags (D-15)');
  it.todo('generates UUID id for each session');
  it.todo('sets completed: false on all sessions');
});

describe('Plan Schema (PLAN-02)', () => {
  it.todo('plan document has correct schema fields');
  it.todo('session subdocument matches D-05 schema');
});

describe('Plan CRUD', () => {
  it.todo('POST /api/plan creates plan with status onboarding');
  it.todo('POST /api/plan discards existing onboarding plan (D-02)');
  it.todo('GET /api/plan returns active or onboarding plan');
  it.todo('GET /api/plan does not return completed plans');
});
```

**api/src/__tests__/sessions.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Session PATCH (PLAN-04)', () => {
  it.todo('PATCH /api/sessions/:id updates session fields');
  it.todo('PATCH /api/sessions/:id marks session completed');
  it.todo('PATCH /api/sessions/:id returns 404 for unknown session');
  it.todo('PATCH /api/sessions/:id does not overwrite session id');
  it.todo('PATCH /api/sessions/:id returns 401 without password');
});
```

**api/src/__tests__/onboarding.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Onboarding Resume (D-03)', () => {
  it.todo('plan with onboardingStep=3 resumes from step 3 in system prompt');
  it.todo('onboardingStep increments after each chat exchange');
  it.todo('onboarding completes after step 6');
  it.todo('start over creates new plan and discards current onboarding');
});
```
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/api && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>5 API test stub files exist with todo tests. `npm test` in api/ runs without errors (todo tests are skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Create web test stub</name>
  <files>web/src/__tests__/PlanCalendar.test.tsx</files>
  <read_first>
    - web/vitest.config.ts (test config)
    - web/src/__tests__/ (existing test patterns)
    - .planning/phases/02-coach-chat/02-RESEARCH.md (PLAN-03 test mapping)
  </read_first>
  <action>
Create `web/src/__tests__/PlanCalendar.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest';

describe('PlanCalendar (PLAN-03)', () => {
  it.todo('renders weekly calendar view');
  it.todo('maps sessions to calendar events with date + distance');
  it.todo('calls onSelectSession when event is clicked');
  it.todo('color codes sessions by type from notes field');
  it.todo('navigates between weeks with prev/next');
});
```
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/web && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Web test stub exists with todo tests. `npm test` in web/ runs without errors.</done>
</task>

</tasks>

<verification>
- `cd api && npm test` passes (todo tests skipped, no failures)
- `cd web && npm test` passes (todo tests skipped, no failures)
- All 6 test files exist in their respective `__tests__/` directories
</verification>

<success_criteria>
- 6 test stub files created matching RESEARCH.md Validation Architecture
- Each file has describe blocks covering the mapped requirements
- All tests use `it.todo()` (skipped, not failing)
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-coach-chat/02-00-SUMMARY.md`
</output>
