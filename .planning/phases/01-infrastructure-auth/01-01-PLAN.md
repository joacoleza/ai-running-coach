---
phase: 01-infrastructure-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - api/package.json
  - api/tsconfig.json
  - api/host.json
  - api/local.settings.json
  - shared/types/index.ts
  - docker-compose.yml
  - swa-cli.config.json
  - .gitignore
  - vitest.config.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "npm install succeeds for both root and api packages"
    - "npx tsc --noEmit passes with zero errors for both frontend and api"
    - "npx vitest run exits 0 (test infrastructure works)"
    - "Shared types are importable from both src/ and api/src/ via @shared/* alias"
    - "docker compose config validates without errors"
  artifacts:
    - path: "package.json"
      provides: "Root package with dev scripts, frontend deps, test deps"
      contains: "concurrently"
    - path: "api/package.json"
      provides: "API package with Azure Functions deps"
      contains: "@azure/functions"
    - path: "shared/types/index.ts"
      provides: "Shared type definitions"
      exports: ["UserProfile"]
    - path: "vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins"
      contains: "@tailwindcss/vite"
    - path: "docker-compose.yml"
      provides: "Cosmos DB emulator + Azurite containers"
      contains: "azure-cosmos-emulator"
    - path: "vitest.config.ts"
      provides: "Test configuration"
      contains: "vitest"
  key_links:
    - from: "tsconfig.json"
      to: "shared/types/index.ts"
      via: "paths alias @shared/*"
      pattern: "@shared/\\*"
    - from: "api/tsconfig.json"
      to: "shared/types/index.ts"
      via: "paths alias @shared/* with ../shared/*"
      pattern: "@shared/\\*"
---

<objective>
Scaffold the complete project structure for both frontend (React+Vite+Tailwind v4) and backend (Azure Functions v4), shared types, local dev toolchain (Docker Compose for emulators), and test infrastructure (Vitest).

Purpose: Every subsequent plan depends on this scaffold existing. This establishes the file structure, dependencies, build configuration, and path aliases that all Phase 1 and future phase work builds into.

Output: Working npm project with frontend, api, shared types, Docker Compose, and passing test infrastructure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-auth/1-CONTEXT.md
@.planning/phases/01-infrastructure-auth/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend project with Vite + React + Tailwind v4 and test infra</name>
  <files>
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts,
    vitest.config.ts, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts,
    src/test/setup.ts, .gitignore
  </files>
  <read_first>
    .gitignore
    .planning/phases/01-infrastructure-auth/1-RESEARCH.md (Pattern 1: Tailwind v4, Pattern 7: tsconfig path aliases, Validation Architecture section)
  </read_first>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` to scaffold the project in the repo root. If Vite prompts about existing files, allow overwrite for its template files.

    2. Install frontend dependencies:
       ```
       npm install react-router-dom
       npm install -D tailwindcss @tailwindcss/vite concurrently
       ```

    3. Install test dependencies:
       ```
       npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
       ```

    4. Update `vite.config.ts` to use both React and Tailwind v4 plugins (per D-01, per research Pattern 1):
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [
           react(),
           tailwindcss(),
         ],
       })
       ```

    5. Update `src/index.css` to use Tailwind v4 import (no tailwind.config.js needed):
       ```css
       @import "tailwindcss";
       ```

    6. Replace `src/App.tsx` with a minimal placeholder:
       ```tsx
       export function App() {
         return <div className="p-4">AI Running Coach</div>
       }
       export default App
       ```

    7. Ensure `src/main.tsx` imports `./index.css` and renders `<App />`.

    8. Update root `tsconfig.json` to add path alias for shared types (per D-03):
       ```json
       {
         "compilerOptions": {
           "baseUrl": ".",
           "paths": {
             "@shared/*": ["shared/*"]
           }
         }
       }
       ```
       Merge this into whatever Vite generated (keep Vite's `references` array pointing to tsconfig.app.json and tsconfig.node.json). Also add `"@shared/*": ["shared/*"]` to the `paths` in `tsconfig.app.json` (which is the one used for source compilation).

    9. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import react from '@vitejs/plugin-react'

       export default defineConfig({
         plugins: [react()],
         test: {
           globals: true,
           environment: 'jsdom',
           setupFiles: ['./src/test/setup.ts'],
         },
       })
       ```

    10. Create `src/test/setup.ts`:
        ```typescript
        import '@testing-library/jest-dom'
        ```

    11. Add test script to `package.json`:
        ```json
        "scripts": {
          "test": "vitest run"
        }
        ```
        (Keep existing scripts from Vite scaffold like `dev`, `build`, `preview`.)

    12. Update `.gitignore` — append these entries if not already present:
        ```
        # Azure Functions
        api/node_modules/
        api/dist/
        api/local.settings.json

        # Docker
        .azurite/

        # Environment
        .env
        .env.local
        ```

    Per AUTH-03: No hardcoded GitHub username (`joacoleza`) anywhere in source code. The username only appears in `api/local.settings.json` (gitignored) and the deploy script.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach && npm install && npx tsc --noEmit && npx vitest run 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react", "react-dom", "react-router-dom", "tailwindcss", "@tailwindcss/vite", "concurrently", "vitest"
    - vite.config.ts contains `import tailwindcss from '@tailwindcss/vite'` and `tailwindcss()` in plugins array
    - src/index.css contains `@import "tailwindcss"`
    - vitest.config.ts contains `environment: 'jsdom'` and `setupFiles: ['./src/test/setup.ts']`
    - src/test/setup.ts contains `import '@testing-library/jest-dom'`
    - tsconfig.app.json contains `"@shared/*": ["shared/*"]` in paths
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
    - .gitignore contains `api/local.settings.json`
  </acceptance_criteria>
  <done>Frontend scaffold builds and type-checks cleanly. Vitest runs (even with zero tests). Tailwind v4 configured via Vite plugin. Path aliases set up for @shared/*.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold API project, shared types, Docker Compose, and dev scripts</name>
  <files>
    api/package.json, api/tsconfig.json, api/host.json, api/local.settings.json,
    api/src/functions/.gitkeep,
    shared/types/index.ts,
    docker-compose.yml, swa-cli.config.json, package.json
  </files>
  <read_first>
    package.json (to see current scripts from Task 1)
    .planning/phases/01-infrastructure-auth/1-RESEARCH.md (Pattern 4: Functions v4, Pattern 5: concurrently, Pattern 6: Docker Compose, Pattern 7: tsconfig aliases)
  </read_first>
  <action>
    1. Create `api/` directory structure: `api/src/functions/`.

    2. Create `api/package.json`:
       ```json
       {
         "name": "ai-running-coach-api",
         "version": "1.0.0",
         "private": true,
         "type": "module",
         "main": "dist/src/functions/*.js",
         "scripts": {
           "build": "tsc",
           "start": "func start",
           "prestart": "npm run build"
         },
         "dependencies": {
           "@azure/functions": "^4.11.2",
           "@azure/cosmos": "^4.9.2",
           "@azure/storage-blob": "^12.31.0"
         },
         "devDependencies": {
           "typescript": "^5.9.3",
           "@types/node": "^22.0.0"
         }
       }
       ```

    3. Create `api/tsconfig.json` with path alias to shared types (per D-03, per research Pattern 7):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "Node16",
           "moduleResolution": "Node16",
           "outDir": "dist",
           "rootDir": ".",
           "sourceMap": true,
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "resolveJsonModule": true,
           "declaration": true,
           "declarationMap": true,
           "baseUrl": ".",
           "paths": {
             "@shared/*": ["../shared/*"]
           }
         },
         "include": ["src/**/*.ts"],
         "exclude": ["node_modules", "dist"]
       }
       ```

    4. Create `api/host.json` (per research Pattern 4):
       ```json
       {
         "version": "2.0",
         "extensionBundle": {
           "id": "Microsoft.Azure.Functions.ExtensionBundle",
           "version": "[4.*, 5.0.0)"
         },
         "logging": {
           "applicationInsights": {
             "samplingSettings": {
               "isEnabled": true
             }
           }
         }
       }
       ```

    5. Create `api/local.settings.json` (per research Pattern 6 — this file is gitignored):
       ```json
       {
         "IsEncrypted": false,
         "Values": {
           "AzureWebJobsStorage": "UseDevelopmentStorage=true",
           "FUNCTIONS_WORKER_RUNTIME": "node",
           "COSMOS_DB_ENDPOINT": "http://localhost:8081",
           "COSMOS_DB_KEY": "C2y6yDjf5/R+ob0N8A7Cgv30VRDJIWEHLM+4QDU5DE2nQ9nDuVTqobD4b58Oeouq+U3+GqxvM/oIJlcFByXGvA==",
           "COSMOS_DB_DATABASE": "running-coach",
           "BLOB_CONNECTION_STRING": "UseDevelopmentStorage=true",
           "OWNER_GITHUB_USERNAME": "joacoleza"
         },
         "Host": {
           "CORS": "*"
         }
       }
       ```

    6. Create `api/src/functions/.gitkeep` (empty placeholder so directory is tracked).

    7. Create `shared/types/index.ts` with initial shared types (per D-03):
       ```typescript
       /** Core user profile stored in Cosmos DB */
       export interface UserProfile {
         id: string;
         userId: string;
         displayName: string;
         githubUsername: string;
         createdAt: string;
         updatedAt: string;
       }

       /** Auth client principal from SWA */
       export interface ClientPrincipal {
         identityProvider: string;
         userId: string;
         userDetails: string;
         userRoles: string[];
       }

       /** Standard API error response */
       export interface ApiError {
         error: string;
         message: string;
       }

       /** Navigation route definition */
       export interface NavRoute {
         path: string;
         label: string;
         icon: string;
       }
       ```

    8. Create `docker-compose.yml` (per D-18, D-19, per research Pattern 6):
       ```yaml
       services:
         cosmos:
           image: mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:vnext-preview
           ports:
             - "8081:8081"
             - "1234:1234"
           environment:
             ENABLE_EXPLORER: "true"
           healthcheck:
             test: ["CMD", "curl", "-f", "http://localhost:8081/_explorer/emulator.pem"]
             interval: 10s
             timeout: 5s
             retries: 10
             start_period: 30s

         azurite:
           image: mcr.microsoft.com/azure-storage/azurite
           ports:
             - "10000:10000"
             - "10001:10001"
             - "10002:10002"
           command: azurite --blobHost 0.0.0.0 --queueHost 0.0.0.0 --tableHost 0.0.0.0
       ```

    9. Create `swa-cli.config.json` (per research Pattern 5):
       ```json
       {
         "configurations": {
           "ai-running-coach": {
             "appLocation": ".",
             "apiLocation": "api",
             "outputLocation": "dist",
             "appDevserverUrl": "http://localhost:3000",
             "apiDevserverUrl": "http://localhost:7071"
           }
         }
       }
       ```

    10. Update root `package.json` scripts to add dev orchestration (per D-17, per research Pattern 5):
        ```json
        "scripts": {
          "dev": "concurrently --names \"docker,func,swa\" --prefix-colors \"cyan,yellow,green\" \"docker compose up\" \"npm run dev:func\" \"npm run dev:swa\"",
          "dev:vite": "vite",
          "dev:func": "cd api && npm run start",
          "dev:swa": "swa start http://localhost:5173 --api-location http://localhost:7071",
          "build": "tsc -b && vite build",
          "preview": "vite preview",
          "test": "vitest run"
        }
        ```
        Note: Vite's default dev port is 5173, so `dev:swa` points there.

    11. Run `cd api && npm install` to install API dependencies.
  </action>
  <verify>
    <automated>cd C:/dev/ai-running-coach/api && npm install && npx tsc --noEmit && cd .. && docker compose config --quiet 2>&1 && echo "docker-compose valid"</automated>
  </verify>
  <acceptance_criteria>
    - api/package.json contains "@azure/functions", "@azure/cosmos", "@azure/storage-blob"
    - api/tsconfig.json contains `"@shared/*": ["../shared/*"]` in paths
    - api/tsconfig.json contains `"module": "Node16"`
    - api/host.json contains `"version": "2.0"` and `extensionBundle`
    - api/local.settings.json contains `OWNER_GITHUB_USERNAME` and `COSMOS_DB_ENDPOINT`
    - shared/types/index.ts contains `export interface UserProfile` and `export interface ClientPrincipal`
    - docker-compose.yml contains `azure-cosmos-emulator:vnext-preview` and `azure-storage/azurite`
    - docker-compose.yml contains `healthcheck` for cosmos service
    - swa-cli.config.json contains `appDevserverUrl` and `apiDevserverUrl`
    - package.json contains `"dev":` script with `concurrently`
    - `cd api && npx tsc --noEmit` exits 0
    - `docker compose config --quiet` exits 0
    - `grep -r "joacoleza" api/src/ shared/ src/ 2>/dev/null` returns 0 matches (AUTH-03)
  </acceptance_criteria>
  <done>API project scaffolded with v4 Functions SDK. Shared types importable via @shared/*. Docker Compose ready for Cosmos emulator + Azurite. Dev orchestration via concurrently configured. No hardcoded username in source code (AUTH-03).</done>
</task>

</tasks>

<verification>
- `npm install` succeeds in root
- `cd api && npm install` succeeds
- `npx tsc --noEmit` passes in root
- `cd api && npx tsc --noEmit` passes
- `npx vitest run` exits 0
- `docker compose config --quiet` exits 0
- `grep -r "joacoleza" api/src/ shared/ src/` returns no matches (AUTH-03)
- File structure matches D-04 layout
</verification>

<success_criteria>
Both frontend and API projects compile cleanly. Test infrastructure runs. Docker Compose validates. Shared types exist and are importable via @shared/* from both frontend and API tsconfigs. No hardcoded owner username in source code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-auth/01-01-SUMMARY.md`
</output>
