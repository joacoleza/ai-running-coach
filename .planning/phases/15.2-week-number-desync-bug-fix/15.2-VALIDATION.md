---
phase: 15.2-week-number-desync-bug-fix
validated: 2026-05-14
status: passed
tests_added: 0
---

# Phase 15.2 — Nyquist Validation

## Coverage Table

| Requirement | Test | File | Status |
|-------------|------|------|--------|
| `addWeekToPhase`: increments weekNumber on runs whose weekNumber >= newly inserted week number | `increments weekNumber on runs whose weekNumber >= newly inserted week number` | `planPhases.test.ts` | green |
| `addWeekToPhase`: does NOT increment weekNumber on runs whose weekNumber < newly inserted week number | `does NOT increment weekNumber on runs whose weekNumber < newly inserted week number` | `planPhases.test.ts` | green |
| `deleteLastWeekOfPhase`: decrements weekNumber on runs whose weekNumber > deleted week number | `decrements weekNumber on runs whose weekNumber > deleted week number` | `planPhases.test.ts` | green |
| `deleteLastWeekOfPhase`: does NOT decrement weekNumber on runs whose weekNumber <= deleted week number | `does NOT decrement weekNumber on runs whose weekNumber <= deleted week number` | `planPhases.test.ts` | green |
| `addWeekToPhase`: runs collection cleaned up in beforeEach to prevent cross-test contamination | Top-level `beforeEach` calls `deleteMany({})` on both `plans` and `runs` collections | `planPhases.test.ts` | green |
| `deleteLastWeekOfPhase`: runs collection cleaned up in beforeEach | Migration describe's `beforeEach` calls `deleteMany({})` on `runs` | `planPhases.test.ts` | green |
| `addWeekToPhase` uses `$gte` (inclusive) for shifting — new week takes slot of displaced week | Filter `{ weekNumber: { $gte: newWeekNumber } }` with `$inc: { weekNumber: 1 }` | Implementation at `planPhases.ts` line 141 | green |
| `deleteLastWeekOfPhase` uses `$gt` (exclusive) — deleted week cannot have runs (guard enforced) | Filter `{ weekNumber: { $gt: deletedWeekNumber } }` with `$inc: { weekNumber: -1 }` | Implementation at `planPhases.ts` line 206 | green |
| updateMany executes BEFORE findOneAndUpdate in both handlers | Both `updateMany` calls precede `findOneAndUpdate` in handler execution order | Implementation at `planPhases.ts` lines 141, 206 (before lines 147, 212) | green |
| planId scoping prevents cross-plan run contamination | Filter includes `planId: plan._id` | Implementation at `planPhases.ts` | green |

## Gaps Found

None. All 4 migration-specific tests were written as part of phase 15.2 execution and were already present in `api/src/__tests__/planPhases.test.ts` at validation time. The test suite includes:

- Two dedicated `describe` blocks for migration testing:
  - `POST /api/plan/phases/:phaseIndex/weeks — weekNumber migration` (2 tests)
  - `DELETE /api/plan/phases/:phaseIndex/weeks/last — weekNumber migration` (2 tests)
- All 4 migration tests use MongoMemoryServer with real MongoDB operations (not mocks), testing the actual `updateMany` calls end-to-end
- The top-level `beforeEach` clears both `plans` and `runs` collections, providing clean test isolation

No additional tests were needed.

## Tests Added

None — all required tests were present before this validation pass.

## Final Status

**No gaps. All pre-existing tests pass. No new tests required.**

| Suite | Tests | Status |
|-------|-------|--------|
| `api/src/__tests__/planPhases.test.ts` — full suite | 41/41 | green |
| Migration tests (addWeek) | 2/2 | green |
| Migration tests (deleteWeek) | 2/2 | green |

Run command: `cd api && npm test -- planPhases.test`

All 41 tests pass in ~1.7s with MongoMemoryServer.
