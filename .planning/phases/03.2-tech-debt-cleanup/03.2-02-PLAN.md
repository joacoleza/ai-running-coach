---
phase: 03.2-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/hooks/useChat.ts
autonomous: true
requirements: [COACH-02]

must_haves:
  truths:
    - "startPlan() and sendMessage() share a single SSE streaming loop implementation (no duplicated reader/buffer/payload logic)"
    - "All existing useChat behavior is preserved: streaming, plan generation, plan:update, plan:add, phase ops, error handling, alive() guard in startPlan"
    - "npm test in web/ passes"
    - "npm run build in web/ passes (TypeScript compiles)"
  artifacts:
    - path: "web/src/hooks/useChat.ts"
      provides: "useChat with extracted streamChatResponse helper"
      contains: "streamChatResponse\|processSSEStream"
  key_links:
    - from: "startPlan"
      to: "shared streaming helper"
      via: "function call replacing inline reader loop"
      pattern: "streamChatResponse\|await.*stream"
    - from: "sendMessage"
      to: "shared streaming helper"
      via: "function call replacing inline reader loop"
      pattern: "streamChatResponse\|await.*stream"
---

<objective>
Deduplicate the SSE streaming loop in `useChat.ts`. The `sendMessage()` function (lines 153–463) and `startPlan()` function (lines 465–783) both contain an independent copy of the same reader loop: fetch response, create TextDecoder/buffer, read chunks, parse `data:` lines, handle `text`/`done`/`error` payloads, strip XML tags, apply plan:update/plan:add/phase operations, and handle errors. Extract this into a single shared helper function used by both.

Purpose: ~200 lines of duplicated streaming logic is a maintenance hazard. Any future change (new payload type, bug fix) must be applied in two places.
Output: `web/src/hooks/useChat.ts` with a `streamChatResponse` (or similar) internal helper that both `sendMessage` and `startPlan` delegate to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared SSE streaming helper in useChat.ts</name>
  <files>web/src/hooks/useChat.ts</files>
  <read_first>
    - web/src/hooks/useChat.ts (read the ENTIRE file — all 814 lines — before making any changes)
  </read_first>
  <action>
Read the full file. Identify the duplicated streaming logic between `sendMessage` (lines ~190–462) and `startPlan` (lines ~532–757).

**Extract a shared helper function** inside the `useChat` hook body (or above it as a module-level helper — your choice based on what closures it needs). Name it `streamChatResponse`.

**Signature (adapt as needed for TypeScript):**
```typescript
async function streamChatResponse(
  response: Response,
  opts: {
    alive?: () => boolean;
    onAccumulatedText: (text: string) => void;   // called on each setMessages update
    onDone: (accumulatedText: string, planGenerated: boolean) => Promise<void>;
    onError: (message: string) => void;
  }
): Promise<string>  // returns accumulatedText
```

**The helper must handle:**
1. `reader = response.body!.getReader()` + TextDecoder + buffer accumulation
2. Line-by-line `data: ` prefix stripping + JSON parse (skip bad JSON)
3. `payload.text` → accumulate, strip XML display tags, call state setters (via `onAccumulatedText`)
4. `payload.done` → call `onDone(accumulatedText, payload.planGenerated ?? false)`
5. `payload.error` → call `onError(payload.error)`
6. `alive()` guard: if provided, check before each `reader.read()` and cancel if stale

**The plan:update/plan:add/phase operation logic** (the long blocks that call `fetch('/api/plan/days/...')` etc.) is the part that lives INSIDE `onDone`. The caller (`sendMessage` or `startPlan`'s done handler) provides `onDone` which contains that logic. This avoids passing too many state-setter references into the helper.

**Refactor `sendMessage`** to:
1. Set up optimistic messages + streaming state (same as now)
2. Call `fetch('/api/chat', ...)` to get the response
3. Call `await streamChatResponse(response, { onAccumulatedText, onDone, onError })`
4. Return `accumulatedText`
5. Remove its own reader/buffer/decoder/while-loop

**Refactor `startPlan`** to:
1. After getting `chatResponse`, call `await streamChatResponse(chatResponse, { alive, onAccumulatedText, onDone, onError })`
2. Remove its own reader/buffer/decoder/while-loop (the second copy)

**Preserve all behavior:**
- `planUpdateDetected` / `planUpdateDetected2` flag (merge into one in the helper or keep per-caller)
- XML tag stripping from displayed messages during streaming: `.replace(/<training_plan>[\s\S]*/g, '').replace(/<plan:update-phase[^/]*\/>/g, '').replace(/<plan:delete-phase[^/]*\/>/g, '').replace(/<plan:update[^/]*\/>/g, '').replace(/<plan:add[^/]*\/>/g, '').trim()`
- `<app:*/>` command stripping and execution after done
- `planGenerated` path: re-fetch plan, navigate to /plan
- `plan:update` / `plan:add` / `plan:update-phase` / `plan:delete-phase` PATCH/DELETE calls with error collection + display
- `plan-updated` window event dispatch
- Error path: remove empty placeholder message
- `alive()` check in `startPlan`'s loop (cancel reader if session invalidated)

**Do NOT change:**
- The function signatures of `sendMessage` or `startPlan` (same params, same return types)
- Any state variables, refs, or other hooks
- Anything outside the streaming loop sections

After the refactor, the hook should be noticeably shorter (target: remove ~150–200 duplicate lines).
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npm run build 2>&1 | tail -20</automated>
    <automated>cd /c/dev/ai-running-coach/web && npm test -- --run 2>&1 | tail -20</automated>
    <automated>cd /c/dev/ai-running-coach && npx playwright test e2e/ --reporter=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - A function named `streamChatResponse` (or equivalent) exists in `web/src/hooks/useChat.ts`: `grep -c "streamChatResponse\|processSSEStream\|streamResponse" web/src/hooks/useChat.ts` returns >= 1
    - `sendMessage` no longer contains `const decoder = new TextDecoder()` (the reader loop was extracted): `grep -n "TextDecoder" web/src/hooks/useChat.ts` returns exactly 1 line (inside the shared helper, not duplicated)
    - `startPlan` no longer contains its own `TextDecoder` instantiation (reader loop moved to shared helper)
    - `npm run build` in `web/` exits with code 0 (TypeScript compiles without errors)
    - `npm test` in `web/` exits with code 0 (all tests pass)
    - Total line count of `web/src/hooks/useChat.ts` is less than 750 (was 814 — ~150+ lines removed)
    - `npx playwright test` in the project root exits with code 0 (all E2E tests pass)
  </acceptance_criteria>
  <done>SSE streaming loop deduplicated into a single shared helper; sendMessage and startPlan delegate to it; build, unit tests, and E2E tests pass</done>
</task>

</tasks>

<verification>
- `npm run build` in `web/` passes — TypeScript accepts the refactored hook
- `npm test` in `web/` passes — all existing hook and component tests green
- `grep -c "new TextDecoder" web/src/hooks/useChat.ts` returns 1 (single location, not 2)
- Behavior is unchanged: streaming, plan generation, plan updates, error handling all work via the shared helper
- `npx playwright test` passes — streaming behavior verified end-to-end via E2E tests
</verification>

<success_criteria>
- Single `streamChatResponse` helper replaces two independent reader loops
- `web/src/hooks/useChat.ts` is ~150+ lines shorter than 814
- `npm run build` and `npm test` pass in `web/`
- `npx playwright test` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-tech-debt-cleanup/03.2-02-SUMMARY.md`
</output>
