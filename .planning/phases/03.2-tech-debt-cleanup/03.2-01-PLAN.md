---
phase: 03.2-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/functions/sessions.ts
  - api/src/functions/plan.ts
  - api/src/functions/index.ts
  - api/src/__tests__/sessions.test.ts
  - api/src/__tests__/plan.test.ts
autonomous: true
requirements: [PLAN-04]

must_haves:
  truths:
    - "PATCH /api/sessions/:sessionId returns 404 (route removed, not registered)"
    - "POST /api/plan/generate returns 404 (route removed, not registered)"
    - "npm test in api/ passes with all tests green"
  artifacts:
    - path: "api/src/functions/sessions.ts"
      provides: "Dead sessions PATCH route — deleted"
    - path: "api/src/functions/plan.ts"
      provides: "plan.ts without the generatePlan handler"
      contains: "getPlan\ncreatedPlan\npatchPlan"
    - path: "api/src/__tests__/sessions.test.ts"
      provides: "sessions test — deleted or gutted"
    - path: "api/src/__tests__/plan.test.ts"
      provides: "plan tests without generatePlan test cases"
  key_links:
    - from: "api/src/functions/index.ts"
      to: "api/src/functions/sessions.ts"
      via: "import statement"
      pattern: "import.*sessions"
---

<objective>
Remove two dead-code API endpoints: `PATCH /api/sessions/:sessionId` (superseded by `PATCH /api/plan/days/:week/:day`) and `POST /api/plan/generate` (superseded by server-side plan saving in `chat.ts`). Delete or update their tests so the suite stays green.

Purpose: These endpoints have no frontend callers and exist only as dead weight. Removing them prevents confusion and shrinks the surface area.
Output: `sessions.ts` deleted, `generatePlan` handler removed from `plan.ts`, test files updated, `index.ts` import cleaned up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete sessions.ts and remove generatePlan from plan.ts</name>
  <files>
    api/src/functions/sessions.ts
    api/src/functions/plan.ts
    api/src/functions/index.ts
  </files>
  <read_first>
    - api/src/functions/sessions.ts (understand the full file before deleting)
    - api/src/functions/plan.ts (understand which handler to remove — keep getPlan, createPlan, patchPlan; remove only the generatePlan http registration at line 133 to end of file)
    - api/src/functions/index.ts (find import for sessions.ts to remove)
  </read_first>
  <action>
**Step 1 — Delete sessions.ts:**
Delete the file `api/src/functions/sessions.ts` entirely. It registers `app.http('updateSession', { methods: ['PATCH'], route: 'sessions/{sessionId}' })`. No frontend code calls this route.

**Step 2 — Remove generatePlan from plan.ts:**
In `api/src/functions/plan.ts`, remove the entire `app.http('generatePlan', ...)` block. This block starts at the line `app.http('generatePlan', {` and ends at the closing `});` for that registration. Keep the three remaining handlers: `getPlan`, `createPlan`, and `patchPlan`. Also remove the now-unused import `assignPlanStructure` from `../shared/planUtils.js` if it is only used by generatePlan (check usage — if `createPlan` or others also use it, keep it).

**Step 3 — Clean up index.ts imports:**
In `api/src/functions/index.ts`, remove any `import` line that references `./sessions.js` or `./sessions`. Do NOT remove imports for plan.ts, chat.ts, or any other module.
  </action>
  <verify>
    <automated>grep -r "updateSession\|sessions/{sessionId}\|route: 'sessions" /c/dev/ai-running-coach/api/src/functions/ && echo "FOUND - BAD" || echo "CLEAN - GOOD"</automated>
    <automated>grep -n "generatePlan" /c/dev/ai-running-coach/api/src/functions/plan.ts && echo "FOUND - BAD" || echo "CLEAN - GOOD"</automated>
    <automated>grep -n "sessions" /c/dev/ai-running-coach/api/src/functions/index.ts && echo "FOUND - CHECK" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `api/src/functions/sessions.ts` does not exist: `test ! -f api/src/functions/sessions.ts`
    - `api/src/functions/plan.ts` does NOT contain the string `generatePlan`: `grep -c "generatePlan" api/src/functions/plan.ts` returns 0
    - `api/src/functions/plan.ts` still contains `getPlan`, `createPlan`, `patchPlan` handlers
    - `api/src/functions/index.ts` does NOT contain `sessions` import
  </acceptance_criteria>
  <done>sessions.ts deleted, generatePlan removed from plan.ts, index.ts cleaned of sessions import</done>
</task>

<task type="auto">
  <name>Task 2: Update tests for removed endpoints</name>
  <files>
    api/src/__tests__/sessions.test.ts
    api/src/__tests__/plan.test.ts
  </files>
  <read_first>
    - api/src/__tests__/sessions.test.ts (full file — 139 lines, tests PATCH /api/sessions/:id which no longer exists)
    - api/src/__tests__/plan.test.ts (find all generatePlan test cases — grep shows lines 111, 126, 140, 154, 168, 190, 206, 236, 326, 345, 364, 399, 413, 435, 470, 483)
  </read_first>
  <action>
**Step 1 — Delete sessions.test.ts:**
Delete the file `api/src/__tests__/sessions.test.ts` entirely. It only tests the now-deleted `updateSession` handler. Keeping it would cause a vitest import error since `sessions.js` no longer exists.

**Step 2 — Remove generatePlan tests from plan.test.ts:**
In `api/src/__tests__/plan.test.ts`, remove all test cases that call `handlers.get('generatePlan')`. This includes the entire describe blocks and individual `it()` calls that reference `generatePlan`. Also:
- Remove the side-effect import for `../functions/plan.js` if it is now only needed to register `generatePlan` (check: if getPlan/createPlan/patchPlan are still tested, keep the import).
- Remove any helper functions used exclusively by generatePlan tests (e.g., `makeGeneratePlanReq`, plan JSON fixtures, etc.).
- Keep all `getPlan`, `createPlan`, `patchPlan` test cases intact — do not touch those.
- Remove the `import { assignPlanStructure }` line if it was only used by generatePlan test helpers.

After removal, the plan.test.ts should still test: GET /api/plan, POST /api/plan (createPlan), PATCH /api/plan (patchPlan).

**Step 3 — Verify test suite compiles and passes:**
Run `cd /c/dev/ai-running-coach/api && npm test -- --run 2>&1 | tail -30` to confirm all tests pass with no import errors.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/api && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `api/src/__tests__/sessions.test.ts` does not exist: `test ! -f api/src/__tests__/sessions.test.ts`
    - `api/src/__tests__/plan.test.ts` does NOT contain the string `generatePlan`: `grep -c "generatePlan" api/src/__tests__/plan.test.ts` returns 0
    - `npm test` in `api/` exits with code 0 (all tests pass, no import errors)
    - `npm test` output does NOT contain `Cannot find module.*sessions`
    - `npm test` output does NOT contain `FAIL` on plan.test.ts
  </acceptance_criteria>
  <done>sessions.test.ts deleted, generatePlan test cases removed from plan.test.ts, npm test passes</done>
</task>

</tasks>

<verification>
- `PATCH /api/sessions/:sessionId` is fully removed: no handler registered, no test, no import in index.ts
- `POST /api/plan/generate` is fully removed: no handler in plan.ts, no test cases in plan.test.ts
- `npm test` in `api/` passes with all remaining tests green
- `npm run build` in `web/` still passes (no frontend changes, but run as sanity check)
</verification>

<success_criteria>
- `api/src/functions/sessions.ts` deleted
- `generatePlan` handler removed from `api/src/functions/plan.ts`
- `api/src/__tests__/sessions.test.ts` deleted
- `generatePlan` test cases removed from `api/src/__tests__/plan.test.ts`
- `api/src/functions/index.ts` has no sessions import
- `npm test` in `api/` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-tech-debt-cleanup/03.2-01-SUMMARY.md`
</output>
