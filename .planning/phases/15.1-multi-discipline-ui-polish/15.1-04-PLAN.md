---
phase: 15.1-multi-discipline-ui-polish
plan: "04"
type: execute
wave: 3
depends_on: ["15.1-01", "15.1-02", "15.1-03"]
files_modified:
  - web/src/components/layout/Sidebar.test.tsx
  - web/src/__tests__/LinkRunModal.test.tsx
  - web/src/__tests__/DayRow.events.test.tsx
  - web/src/__tests__/RunDetailModal.test.tsx
autonomous: true
requirements: [DISC-UX-01]

must_haves:
  truths:
    - "npm run build in web/ exits 0 with no TypeScript errors"
    - "All vitest unit tests pass (no references to 'Runs', 'Log run', 'Link run', 'Add feedback to run', 'Delete run' in test assertions)"
    - "Sidebar test expects 'Activities' label"
    - "LinkRunModal test expects 'Link a session' header and 'Activities page' in empty state"
    - "DayRow tests that referenced 'Log run' title now reference 'Log session'"
    - "RunDetailModal tests use '/get coaching feedback/i' and '/delete session/i' button queries"
  artifacts:
    - path: "web/src/components/layout/Sidebar.test.tsx"
      provides: "Updated Sidebar test expecting 'Activities'"
    - path: "web/src/__tests__/LinkRunModal.test.tsx"
      provides: "Updated LinkRunModal tests with 'Link a session' header and 'Activities page' empty state"
    - path: "web/src/__tests__/DayRow.events.test.tsx"
      provides: "Updated DayRow tests with 'Log session' titles"
    - path: "web/src/__tests__/RunDetailModal.test.tsx"
      provides: "Updated RunDetailModal tests with 'Get coaching feedback' and 'Delete session' button labels"
  key_links:
    - from: "web/src/components/layout/Sidebar.test.tsx"
      to: "web/src/components/layout/Sidebar.tsx"
      via: "screen.getByText('Activities')"
      pattern: "Activities"
    - from: "web/src/__tests__/LinkRunModal.test.tsx"
      to: "web/src/components/runs/LinkRunModal.tsx"
      via: "empty state and header assertions"
      pattern: "Link a session"
    - from: "web/src/__tests__/RunDetailModal.test.tsx"
      to: "web/src/components/runs/RunDetailModal.tsx"
      via: "button role queries"
      pattern: "get coaching feedback|delete session"
---

<objective>
Fix all failing unit tests caused by the label changes in Plans 01–03, run the full web test suite, run the TypeScript build, and run E2E smoke tests to confirm the end-to-end flows work correctly.

Purpose: Tests are part of execution, not UAT. This plan makes the test suite green and ensures no TypeScript errors crept in across all changed files.

Output: Four updated test files, green CI, passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-multi-discipline-ui-polish/15.1-CONTEXT.md
@.planning/phases/15.1-multi-discipline-ui-polish/15.1-01-SUMMARY.md
@.planning/phases/15.1-multi-discipline-ui-polish/15.1-02-SUMMARY.md
@.planning/phases/15.1-multi-discipline-ui-polish/15.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unit tests broken by label changes</name>
  <read_first>
    - web/src/components/layout/Sidebar.test.tsx
    - web/src/__tests__/LinkRunModal.test.tsx
    - web/src/__tests__/DayRow.events.test.tsx
    - web/src/__tests__/RunDetailModal.test.tsx
  </read_first>
  <files>
    web/src/components/layout/Sidebar.test.tsx,
    web/src/__tests__/LinkRunModal.test.tsx,
    web/src/__tests__/DayRow.events.test.tsx,
    web/src/__tests__/RunDetailModal.test.tsx
  </files>
  <action>
    **Step 1 — Run existing tests first to identify exact failures:**
    ```bash
    cd /c/dev/ai-running-coach/web && npx vitest run 2>&1 | grep -E "FAIL|✗|×|Error" | head -40
    ```
    This reveals which tests are actually failing. Then fix each one:

    **Sidebar.test.tsx changes:**
    1. Change the test named `'renders three navigation links ...'`:
       - Before: `expect(screen.getByText('Runs')).toBeInTheDocument()`
       - After: `expect(screen.getByText('Activities')).toBeInTheDocument()`
    2. Update the test description string if it still says "Runs" — change to mention "Activities".
    3. Keep all other assertions unchanged.

    **LinkRunModal.test.tsx changes:**
    1. Test `'renders header with week/day info and guidelines'` — already checks `/week 2 day B/i` which is still valid (the header now says "Link a session to Week 2 Day B"). No change needed there.
    2. Test `'shows empty state when no unlinked runs'` — currently asserts `screen.getByText(/no unlinked runs available/i)`. Change to: `expect(screen.getByText(/no unlinked run sessions available/i)).toBeInTheDocument();`
       (The empty state now reads "No unlinked run sessions available. Log one from the Activities page first." — when dayDiscipline is undefined, it defaults to 'run', so the text will be "No unlinked run sessions available...")
    3. Check if any test asserts on "No unlinked runs available. Log a run from the Runs page first." — update to match the new copy.
    4. DO NOT change the `'shows loading spinner initially'` test — it checks `/loading runs/i` which is unchanged.
    5. Add a new test to verify discipline prop passes through and filters:
    ```typescript
    it('passes dayDiscipline to fetchUnlinkedRuns when provided', async () => {
      await act(async () => {
        render(<LinkRunModal {...defaultProps} dayDiscipline="gym" />);
      });
      expect(mockFetchUnlinkedRuns).toHaveBeenCalledWith(100, 'gym');
    });
    ```

    **DayRow.events.test.tsx changes:**
    The DayRow tests that reference button titles/text for "Log run":
    1. `describe('DayRow — UX-DAY-01: "Log run" button ...')` — The describe block name can stay, but the actual assertions against title attributes must change:
       - Before: `screen.getByTitle('Log run data for this completed day')` → After: `screen.getByTitle('Log session data for this completed day')`
       - Before: `screen.getAllByTitle('Log run data')` → After: `screen.getAllByTitle('Log session data')`
    2. Update the describe block descriptions if they contain "Log run" → "Log session" for clarity (optional but recommended).
    3. Leave all other assertions unchanged — the open-run-detail event tests, Undo button test, and readonly test do not reference these titles.

    **RunDetailModal.test.tsx changes (Plan 02 renamed two labels):**

    Plan 02 changed RunDetailModal.tsx so that:
    - "Add feedback to run" → "Get coaching feedback"
    - "Delete run" → "Delete session" (both disabled and active button variants)

    The test file at `web/src/__tests__/RunDetailModal.test.tsx` has 9 references to the old labels across two describe blocks. Make the following targeted replacements:

    In the first `describe('RunDetailModal', ...)` block:

    **Line 79** — Update the test description:
    - Before: `it('shows Add feedback to run button', () => {`
    - After: `it('shows Get coaching feedback button', () => {`

    **Line 85** — Update button role query:
    - Before: `expect(screen.getByRole('button', { name: /add feedback to run/i })).toBeInTheDocument();`
    - After: `expect(screen.getByRole('button', { name: /get coaching feedback/i })).toBeInTheDocument();`

    **Line 98** — Update fireEvent click:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /add feedback to run/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /get coaching feedback/i }));`

    **Line 114** — Update fireEvent click:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /add feedback to run/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /get coaching feedback/i }));`

    **Line 132** — Update fireEvent click:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /add feedback to run/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /get coaching feedback/i }));`

    **Line 177** — Update getByRole (inside swallows updateRun error test):
    - Before: `const btn = screen.getByRole('button', { name: /add feedback to run/i });`
    - After: `const btn = screen.getByRole('button', { name: /get coaching feedback/i });`

    **Line 181** — Update waitFor assertion:
    - Before: `await waitFor(() => expect(screen.getByRole('button', { name: /add feedback to run/i })).not.toBeDisabled());`
    - After: `await waitFor(() => expect(screen.getByRole('button', { name: /get coaching feedback/i })).not.toBeDisabled());`

    **Line 199** — Update fireEvent click (inside 'uses editNotes' test):
    - Before: `fireEvent.click(screen.getByRole('button', { name: /add feedback to run/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /get coaching feedback/i }));`

    In the second `describe('RunDetailModal — delete run', ...)` block:

    **Line 275** — Update test description:
    - Before: `it('clicking Delete run shows browser confirm dialog', () => {`
    - After: `it('clicking Delete session shows browser confirm dialog', () => {`

    **Line 282** — Update button role query:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /^delete run$/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /^delete session$/i }));`

    **Line 283** — Update confirm dialog message if it references "Delete this run":
    Check what the actual confirm message is in RunDetailModal.tsx after Plan 02 executes. If Plan 02 also updated the confirm message text (e.g. from "Delete this run? This cannot be undone." to "Delete this session? This cannot be undone."), update line 283 to match. If the confirm message was NOT changed in Plan 02, leave line 283 as-is.

    **Line 294** — Update button role query:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /^delete run$/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /^delete session$/i }));`

    **Line 287** — Update test description:
    - Before: `it('confirming delete calls deleteRun and onDeleted', async () => {`
    - After: `it('confirming delete calls deleteSession and onDeleted', async () => {`

    **Line 301** — Update test description:
    - Before: `it('cancelling delete does not call deleteRun', () => {`
    - After: `it('cancelling delete does not call deleteSession', () => {`

    **Line 307** — Update button role query:
    - Before: `fireEvent.click(screen.getByRole('button', { name: /^delete run$/i }));`
    - After: `fireEvent.click(screen.getByRole('button', { name: /^delete session$/i }));`

    **After all edits, run the full test suite:**
    ```bash
    cd /c/dev/ai-running-coach/web && npx vitest run 2>&1 | tail -30
    ```
    Fix any remaining failures before proceeding.
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with 0 failed tests
    - `grep "getByText.*Runs" web/src/components/layout/Sidebar.test.tsx` returns 0 matches for navigation item assertions (the word "Runs" should not appear in toBeInTheDocument assertions)
    - `grep "Activities" web/src/components/layout/Sidebar.test.tsx` returns at least 1 match
    - `grep "no unlinked runs available" web/src/__tests__/LinkRunModal.test.tsx` returns 0 matches (old text gone)
    - `grep "Log session" web/src/__tests__/DayRow.events.test.tsx` returns at least 2 matches
    - `grep "Log run data" web/src/__tests__/DayRow.events.test.tsx` returns 0 matches (old titles gone)
    - `grep "add feedback to run" web/src/__tests__/RunDetailModal.test.tsx` returns 0 matches (case-insensitive)
    - `grep "get coaching feedback" web/src/__tests__/RunDetailModal.test.tsx` returns at least 8 matches
    - `grep "delete run" web/src/__tests__/RunDetailModal.test.tsx` returns 0 matches (case-insensitive)
    - `grep "delete session" web/src/__tests__/RunDetailModal.test.tsx` returns at least 3 matches
  </acceptance_criteria>
  <done>All vitest unit tests pass; test assertions match the updated component label text</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript build verification + E2E smoke</name>
  <read_first>
    - web/src/components/layout/Sidebar.tsx
    - web/src/components/runs/RunDetailModal.tsx
    - web/src/components/plan/DayRow.tsx
  </read_first>
  <files>
    []
  </files>
  <action>
    **Step 1 — TypeScript build (mandatory per CLAUDE.md):**
    ```bash
    cd /c/dev/ai-running-coach/web && npm run build 2>&1
    ```
    Expected: exits 0. If TypeScript errors appear, fix them before continuing.

    Common TypeScript issues to watch for from Plans 01–03:
    - `RunBadge discipline` prop: ensure `(run.discipline ?? 'run') as 'run' | 'gym' | 'cycle'` cast is present everywhere RunBadge is used with `run.discipline` (which is typed as `string | undefined`)
    - `defaultDiscipline` prop cast in DayRow: `(day.discipline ?? 'run') as 'run' | 'gym' | 'cycle'`
    - `dayDiscipline` is `string | undefined` in PlanView → LinkRunModal, which expects `string | undefined` — should be fine

    **Step 2 — E2E smoke test (CLAUDE.md requirement: mandatory):**
    ```bash
    cd /c/dev/ai-running-coach && npx playwright test --grep "sidebar|runs|Activities" 2>&1 | tail -30
    ```
    If no matching tests exist by those tags, run the full E2E suite:
    ```bash
    cd /c/dev/ai-running-coach && npx playwright test 2>&1 | tail -40
    ```
    Fix any E2E failures before considering work done.

    **Step 3 — API unit tests (unchanged but verify they still pass):**
    ```bash
    cd /c/dev/ai-running-coach/api && npm test 2>&1 | tail -20
    ```
    Expected: all pass (no backend changes in this phase).
  </action>
  <verify>
    <automated>cd /c/dev/ai-running-coach/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in web/ exits with code 0 and no TypeScript errors
    - `npx vitest run` in web/ exits with 0 failed tests
    - `npx playwright test` exits with 0 failed tests (or any pre-existing failures are documented as pre-existing)
    - `npm test` in api/ exits with 0 failed tests
  </acceptance_criteria>
  <done>Full build passes, unit tests green, E2E tests pass (or pre-existing failures are noted), API tests unaffected</done>
</task>

</tasks>

<verification>
Final gate — all four test layers green:
```bash
cd /c/dev/ai-running-coach/web && npm run build && npx vitest run && echo "WEB OK"
cd /c/dev/ai-running-coach/api && npm test && echo "API OK"
cd /c/dev/ai-running-coach && npx playwright test && echo "E2E OK"
```
</verification>

<success_criteria>
- web/npm run build exits 0
- web/vitest run exits with 0 failures
- api/npm test exits with 0 failures
- npx playwright test exits with 0 failures (pre-existing failures documented if any)
- No test file still asserts on "Runs" as nav label, "Log run", "Link run", "Add feedback to run", or "Delete run"
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-multi-discipline-ui-polish/15.1-04-SUMMARY.md`
</output>
