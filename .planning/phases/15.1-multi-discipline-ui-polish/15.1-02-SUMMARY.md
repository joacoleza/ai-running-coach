---
phase: 15.1-multi-discipline-ui-polish
plan: "02"
subsystem: ui
tags: [react, typescript, discipline, RunDetailModal, RunBadge]

# Dependency graph
requires:
  - phase: 15-cycling-support
    provides: RunBadge component, discipline field on Run type, isCycle pattern
  - phase: 14-gym-support
    provides: ExerciseList component, gym session data model, isGym pattern
provides:
  - Discipline-aware RunDetailModal: RunBadge in header, conditional Distance/Pace fields for gym, updated labels, gym-specific feedback message
affects: [15.1-04-PLAN, e2e tests that check modal labels]

# Tech tracking
tech-stack:
  added: []
  patterns: [isGym/isCycle parallel variable pattern for discipline branching, !isGym conditional render for field hiding]

key-files:
  created: []
  modified:
    - web/src/components/runs/RunDetailModal.tsx
    - web/src/__tests__/RunDetailModal.test.tsx

key-decisions:
  - "RunBadge rendered inline in header flex container next to date h2 — no layout rearrangement"
  - "Distance and Pace/Speed fields both wrapped in !isGym conditional — gym sessions show only Date, Duration, Avg HR"
  - "handleAddFeedback gym branch uses local isGymSession variable (not the component-level isGym) for clarity"
  - "Test labels updated in Plan 02 (not deferred to Plan 04) since tests were failing due to label rename"

patterns-established:
  - "!isGym conditional wrapping: wrap entire grid cell div to hide a field, not just the label"
  - "Discipline branching in feedback messages: if (isGymSession) { gym-specific } else { run/cycle with isCycleSession check }"

requirements-completed: [DISC-UX-01]

# Metrics
duration: 15min
completed: 2026-05-08
---

# Phase 15.1 Plan 02: RunDetailModal Discipline-Aware Updates Summary

**RunDetailModal now shows discipline badge in header, hides Distance/Pace for gym sessions, uses neutral button labels, and sends gym-appropriate coaching feedback with session type and exercise count.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-08T02:37:00Z
- **Completed:** 2026-05-08T02:45:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- RunBadge discipline badge (Run/Gym/Cycling pill) now appears in the modal header next to the session date
- Gym sessions hide both Distance and Pace/Speed grid cells — only Date, Duration, and Avg HR remain
- Feedback button label changed from "Add feedback to run" to "Get coaching feedback" for all disciplines
- Both delete button variants changed from "Delete run" to "Delete session"
- handleAddFeedback sends a gym-appropriate message (type + exercise count) instead of distance/pace for gym sessions

## Task Commits

Each task was committed atomically:

1. **Task 1: RunDetailModal — RunBadge in header, hide gym fields, fix labels and gym feedback message** - `377dd0f` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `web/src/components/runs/RunDetailModal.tsx` — Five targeted changes: RunBadge import and render, isGym variable, field conditionals, button labels, gym feedback branch
- `web/src/__tests__/RunDetailModal.test.tsx` — Updated label assertions to match new button text (16/16 passing)

## Decisions Made
- Test label updates applied in Plan 02 rather than deferring to Plan 04, because the tests were actively failing and Plan 04 was flagged as a separate test-fix plan that would have needed to duplicate this work
- Used inline `isGymSession` variable inside `handleAddFeedback` (same pattern as `isCycleSession`) rather than using the component-level `isGym` const, for local clarity and symmetry with existing code

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated RunDetailModal test assertions to match renamed labels**
- **Found during:** Task 1 (label renames triggered 9 test failures)
- **Issue:** Tests referenced "Add feedback to run" and "Delete run" labels that no longer exist after the rename
- **Fix:** Updated test descriptions and `getByRole` queries to use "Get coaching feedback" and "Delete session"
- **Files modified:** web/src/__tests__/RunDetailModal.test.tsx
- **Verification:** All 16 RunDetailModal tests pass
- **Committed in:** 377dd0f (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** Necessary fix — tests would have blocked CI. No scope creep.

## Issues Encountered
- Worktree lacked node_modules — installed via npm install before TypeScript build check. Build passed (1073 modules, no errors).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- RunDetailModal is fully discipline-aware — ready for E2E verification in Plan 04
- No stubs: all five changes are complete and wired

---
*Phase: 15.1-multi-discipline-ui-polish*
*Completed: 2026-05-08*
