---
phase: 15.1-multi-discipline-ui-polish
plan: "01"
subsystem: ui
tags: [react, typescript, sidebar, exercise, discipline, runs]

# Dependency graph
requires: []
provides:
  - Sidebar navItems entry for /runs uses label "Activities"
  - ExerciseForm save button labeled "Add" (local-state-only action)
  - ExerciseList done button labeled "Save exercises" with border-t separator above it
  - fetchUnlinkedRuns accepts optional discipline param appended to query string
  - RunEntryForm accepts defaultDiscipline prop and initializes discipline state from it
affects: [15.1-02, 15.1-03, 15.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - defaultDiscipline prop pattern for pre-selecting discipline in RunEntryForm
    - discipline query param forwarding pattern in fetchUnlinkedRuns

key-files:
  created: []
  modified:
    - web/src/components/layout/Sidebar.tsx
    - web/src/components/runs/ExerciseList.tsx
    - web/src/components/runs/ExerciseForm.tsx
    - web/src/hooks/useRuns.ts
    - web/src/components/runs/RunEntryForm.tsx

key-decisions:
  - "Sidebar label changed to Activities (path /runs unchanged) — no route refactor needed"
  - "ExerciseForm Add label conveys local-state-only operation; Save exercises on ExerciseList conveys DB write"
  - "fetchUnlinkedRuns discipline param uses encodeURIComponent for safety"

patterns-established:
  - "defaultDiscipline?: 'run' | 'gym' | 'cycle' prop pattern: interface field + destructuring + useState initializer"
  - "Optional query param appended as template literal: disciplineParam = discipline ? '&discipline=...' : ''"

requirements-completed: [DISC-UX-01]

# Metrics
duration: 5min
completed: 2026-05-08
---

# Phase 15.1 Plan 01: Sidebar Rename + Exercise UX + Discipline Param Summary

**Sidebar renamed to Activities, ExerciseForm/List button labels polished, fetchUnlinkedRuns extended with discipline filter, RunEntryForm gains defaultDiscipline prop for plan-day pre-selection**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-08T00:34:08Z
- **Completed:** 2026-05-08T00:36:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Sidebar nav item for /runs now displays "Activities" instead of "Runs"
- ExerciseForm "Save Exercise" -> "Add" clarifies it only adds to local list (no DB write)
- ExerciseList "Done" -> "Save exercises" with visual border separator from exercise list above
- fetchUnlinkedRuns extended with optional discipline param for discipline-filtered unlinked session queries
- RunEntryForm gains defaultDiscipline prop so plan days can pre-select the correct discipline

## Task Commits

Each task was committed atomically:

1. **Task 1: Sidebar rename + ExerciseList/ExerciseForm UX fixes** - `ea42957` (feat)
2. **Task 2: useRuns.ts fetchUnlinkedRuns discipline param + RunEntryForm defaultDiscipline prop** - `bcd6e62` (feat)

## Files Created/Modified
- `web/src/components/layout/Sidebar.tsx` - navItems label "Runs" -> "Activities" for /runs
- `web/src/components/runs/ExerciseForm.tsx` - save button text "Save Exercise" -> "Add"
- `web/src/components/runs/ExerciseList.tsx` - done button "Done" -> "Save exercises", border-t separator wrapper div
- `web/src/hooks/useRuns.ts` - fetchUnlinkedRuns(limit, discipline?) with disciplineParam query appending
- `web/src/components/runs/RunEntryForm.tsx` - RunEntryFormProps.defaultDiscipline, useState initializer uses defaultDiscipline ?? 'run'

## Decisions Made
- Sidebar path /runs kept unchanged — only the label changes; avoids breaking any navigation or routing logic
- "Add" label for ExerciseForm distinguishes from ExerciseList "Save exercises" which writes to the DB
- border-t separator wrapper uses mt-4 pt-3 border-t border-gray-100 as specified in CONTEXT.md

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wave 1 foundational building blocks are ready
- Wave 2 plans (DayRow, PlanView, LinkRunModal, RunDetailModal) can consume defaultDiscipline, fetchUnlinkedRuns discipline param, and Activities label
- TypeScript build passes with no errors

---
*Phase: 15.1-multi-discipline-ui-polish*
*Completed: 2026-05-08*
